# Local development only. Override all three values in .env.minio.local
# before publishing storage, and synchronize S3_* in apps/platform-api/.env.
MINIO_API_CORS_ALLOW_ORIGIN=http://localhost:5666
MINIO_ROOT_USER=railminio
MINIO_ROOT_PASSWORD=railminio-dev-secret
