# 甲方修改意见迭代提交记录

本文件独立记录 `2026-08-20` 甲方模块说明对应的迭代提交。原始材料保存在同目录的 `original/` 下；用户已于 2026-08-24 授权将专题分支同步至 `origin/codex/customer-feedback-frontend`。

## `76e137268` `chore(project): capture customer feedback baseline`

- 改造动机：建立甲方原始需求材料的不可变追溯基线，清晰区分原生材料与后续人工整理、设计和代码修改。
- 改动范围：原样复制甲方提供的 DOCX；从 DOCX 压缩包中直接提取 22 张 PNG 图片和 1 个 WDP 资源，未改写、压缩、重命名图片内容。
- 校验结果：仓库副本与甲方源 DOCX 的 SHA-256 均为 `3ae9c58db091988165fb157a2c654400c7d3a6e742802abf1f0f7ee5e45a5dac`。
- 注意事项：该提交只包含原始材料，不包含需求整理、实现代码或说明文件；后续不得覆盖 `original/` 中的内容。

## `refactor(@rail/web): add extensible design mode catalog`

- 改造动机：甲方提出 CMF、客室零部件、客室效果和报告四类入口；现有设计页按单个应用平铺，继续在页面中硬编码会导致同类能力和多版本变体重复开发。
- 改动范围：新增独立的设计模式目录，集中定义模式名称、说明、提示词占位、图标和有序应用键；新增纯函数，根据后端返回的可见应用与能力契约筛选可用应用。
- 扩展方式：后续新增同类能力或版本时，优先在模式目录追加应用键；页面继续消费平台 API 返回的应用与能力，不引入静态业务结果或旁路执行逻辑。
- 注意事项：报告生成目前只有应用目录记录，没有已发布能力契约；目录允许其未来自动进入可用状态，但当前不得将其展示为已执行成功。
- 验证范围：新增单元测试覆盖四类模式的稳定顺序、不可见应用过滤、缺失能力契约过滤和模式内排序。

## `feat(@rail/web): adapt customer-facing design modes`

- 改造动机：将甲方提出的四类业务入口落到现有设计会话中，同时保持项目、会话、任务、资产与外部执行器的既有真实链路不变。
- 改动范围：在统一输入器下方增加“客室零部件生成、CMF 生成、客室效果生成、报告生成”模式切换；每个模式按目录筛选和排序已发布能力，切换后复用原有草稿保存、参数加载、任务提交和结果流转逻辑。
- 交互适配：模式切换同步更新场景说明和提示词占位；从 URL 或结果流转进入特定应用时自动匹配适用模式；共享应用优先保留当前模式语境。
- 真实性边界：模式是否可用由平台 API 返回的 `visible` 与 `capabilityCode` 共同决定；报告等缺失能力契约的模式显示“待接入”，点击只给出明确说明，不创建任务、不生成静态结果。
- 品牌适配：沉浸式设计页使用合同对应的平台全称；由于甲方材料没有提供可独立使用且已授权的中车 PNG 标识文件，当前继续使用仓库现有标识，避免从截图裁切或自行仿制商标。
- 验证范围：模式目录单元测试扩展到共享能力和专属能力的模式解析；前端 31 项单元测试、目标类型检查、 Oxlint、ESLint 和差异空白检查作为提交前门禁。

## `feat(@rail/web): add multimodal result gallery`

- 改造动机：甲方要求一轮生成多张图片时使用适应数量的网格展示，并在全屏预览中连续查看，避免以技术化结果编号标签反复切换。
- 改动范围：结果卡片在存在多张可预览图片时按两列网格呈现，三张结果使用一大两小布局，四张以上显示四格和 `+N` 聚合；单图和多图均可点击进入全屏预览。
- 公共组件复用：扩展既有图片查看器，增加可选的上一张、下一张按钮及键盘左右方向键；原有缩放、拖拽、滚轮和单图调用保持兼容，资产中心与分区标记预览无需另建实现。
- 结果操作：全屏切换会同步当前结果，下载、保存到资产、继续设计、重新运行和局部重绘继续作用于当前结果；结果头部将“工作流版本”调整为面向用户的“能力版本”，不展示底层工作流名称。
- 验证范围：目标类型检查、Oxlint、ESLint、Stylelint 与差异空白检查通过；多图布局和全屏键盘交互纳入后续真实浏览器验收。

## `feat(@rail/web): align customer design entry points`

- 改造动机：落实甲方对登录首页欢迎区、六个快捷入口、新建设计项目和历史设计续接的要求，同时不削弱现有设计工作台的真实统计与闭环能力。
- 首页入口：新增“开始新设计、查看我的设计、查看资产中心、开始模型训练、开始报告生成、设计工作台”六个快捷入口；新设计使用真实项目创建接口并在成功后进入设计会话，历史设计弹窗使用首页 API 返回的真实最近会话并保留项目切换。
- 服务边界：模型训练和报告生成仅在应用可见且存在已发布能力契约时进入设计页；当前环境缺少契约时明确标记“待接入”，点击不会写入任务或伪造结果。
- 品牌与导航：Web 标题、版权名称、构建默认标题和设计页品牌统一为“客运装备内装模块化分区快速设计平台”；菜单“开始设计”调整为“设计生成”。甲方未提供独立授权 PNG 标识，因此未替换现有标识资源。
- 兼容性：原有设计闭环、统计图表、最近项目、最近任务和成果区域继续保留；快捷入口中的“设计工作台”平滑定位到原闭环区域。
- 验证范围：目标类型检查、Oxlint、ESLint、Stylelint 与差异空白检查通过；项目创建、历史续接、待接入提示和响应式入口布局纳入真实浏览器验收。

## `fix(@rail/web): keep unavailable entries explainable`

- 改造动机：真实浏览器验收发现，给“待接入”入口声明 `aria-disabled` 后，语义化操作与辅助技术会把它视为不可交互，用户无法稳定点击查看未接入原因。
- 改动范围：首页模型训练、报告生成快捷入口和设计页报告模式改用只读 `data-unavailable` 状态标记；视觉待接入样式、文字和真实能力判断保持不变。
- 行为边界：待接入入口仍可触发明确说明，但不会选择应用、创建任务或产生资产；已接入入口继续按原链路导航或切换。
- 验证范围：真实浏览器需复验待接入入口可点击、说明可见、当前模式与 URL 不发生错误切换，并检查控制台错误。

## `fix(@rail/web): enforce current platform identity`

- 改造动机：真实浏览器刷新后页面标题已使用新平台全称，但侧栏仍可能由旧版本偏好缓存恢复旧名称；要求用户清空全部缓存会同时丢失其主题与布局设置。
- 改动范围：偏好初始化完成后仅同步应用名称和版权公司名称两个发布身份字段，现有主题、字号、侧栏、顶栏和快捷键偏好继续保留。
- 注意事项：该同步不读取、不清空浏览器存储，也不改变用户业务数据；平台名称不作为用户可自定义偏好处理。
- 验证范围：真实浏览器刷新后复验文档标题、侧栏品牌与登录布局品牌统一使用当前平台全称。

## `docs(project): record customer feedback iteration`

- 改造动机：同步产品需求、逻辑架构、质量门禁、开发日志和交接入口，避免代码行为与项目事实来源不一致。
- 改动范围：记录首页六入口、四类设计模式、多图结果查看、能力契约待接入边界、平台名称和本轮 Git 基线；更新“设计生成”页面职责与浏览器验收项目。
- 验证证据：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 31 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、`pnpm build:rail` 和真实浏览器关键路径均通过。
- 已知限制：本地没有可执行 ComfyUI 服务及包含多图片成功输出的现成会话，多图网格与连续全屏查看尚未用本轮真实推理输出做视觉复验；报告生成、LoRA 训练和独立授权中车 PNG 标识仍是外部依赖。

## `feat(@rail/web): align home with customer layout`

- 改造动机：甲方确认 `/home` 不再定位为“设计工作台”，要求收敛为 0820 文档中的登录首页布局，并使用当前平台主题配色。
- 改动范围：菜单、页签和路由标题统一为“首页”；主区只保留居中欢迎区与 3×2 六个业务入口，移除设计闭环、任务趋势、最近项目和最近成果等首页看板内容。
- 主题适配：布局尺寸、居中关系、入口顺序与甲方示意图一致；色彩改用平台 `rail-red`、`rail-red-soft`、`rail-mist` 和灰色边界，不直接照搬文档的绿、黄色标注块。
- 真实链路：新设计与历史设计继续使用真实项目/会话 API；资产和项目工作区使用真实路由；模型训练与报告生成缺少能力契约时点击显示待接入，不创建假任务。
- 验证范围：目标 ESLint、Stylelint 和 `@rail/web` 类型检查通过；真实浏览器确认居中欢迎区、3×2 入口、平台主题配色、新设计/历史弹窗和待接入提示。

## `feat(@rail/web): align design workspace with customer layout`

- 改造动机：继续逐页落实 0820 文档中的设计主界面、历史任务栏、底部工具和报告结构，同时遵守用户要求使用当前系统主题配色。
- 布局改造：历史任务栏增加收起/展开控制，收起后释放完整结果区；平台全称不再省略。历史结果操作统一显示“深化设计”。
- 工具组织：客室零部件、CMF 和客室效果三类模式的前七个快捷工具按“文本、文生图、理解、重绘、扩图、多图、放大”固定排序，其余既有能力进入“更多”；应用是否出现仍由平台 API 的可见性与能力契约决定。
- 报告边界：按需求图展示报告类型、Word/PPT 交付格式、标题说明和三项项目图片素材，但当前控件全部禁用并明确标识服务未接入，不创建任务、不伪造输出。
- 训练边界：首页模型训练入口按需求参考呈现 Repeat、Epoch、触发词、预览提示词、图片打标/裁剪和训练选项；因 LoRA 服务与数据协议尚未接入，控件全部禁用且不能开始训练。
- 影响边界：未修改后端 API、数据库、适配器、能力参数 Schema 或 18 份原始 ComfyUI 工作流 JSON。
- 验证范围：模式目录单测、目标 ESLint、Stylelint、Web 类型检查通过；真实浏览器确认平台全称、工具顺序、任务栏折叠与报告待接入布局。

## `test(@rail/web): align browser checks with design modes`

- 改造动机：全量浏览器验收仍按改造前“单一界面平铺至少 18 项应用”的口径检查，与 0820 要求的四类业务模式目录冲突。
- 改动范围：客室效果模式改为核对目录中的 15 项真实能力完整存在，并继续精确断言前七个快捷工具的名称和顺序；其他模式的专属能力仍由既有切换验收覆盖。
- 注意事项：只调整自动化验收口径，不修改应用、能力、工作流、任务或资产运行逻辑。

## `test(@rail/web): harden design composer acceptance`

- 改造动机：统一输入器加入 0820 业务模式栏后，长文本状态总高度包含模式栏，旧的 330px 绝对阈值不再能准确判断页面是否被撑破；本地未配置 ComfyUI 时，旧提交失败信息也不足以区分前端交互和外部适配器状态。
- 改动范围：长文本继续严格断言文本区不超过 221px、内容在内部滚动、工具栏不越过视口，同时将输入器总高度限制改为不超过视口 40%；提交验收增加真实 `/jobs` 响应校验和会话、应用、提示词、按钮状态诊断。
- 环境结论：本地实际提交返回 `ADAPTER_NOT_CONFIGURED`，符合未配置 ComfyUI 时明确失败的发布规则；没有为通过浏览器脚本写入假服务地址或伪造任务成功。

## `fix(@rail/web): restore two-level workspace layout`

- 改造动机：上一版只对齐了功能区域，没有严格实现甲方图中的“平台功能侧栏 + 可折叠任务栏 + 结果/输入区”空间骨架，属于实现偏差。
- 结构纠正：设计路由重新使用平台基础布局，保留平台 Logo、功能导航、当前项目、搜索、设置和用户中心；页面内部移除重复品牌、用户卡片和会话标题顶栏，只保留独立任务栏及主设计区。
- 折叠边界：任务栏收起后结果区扩展到设计内容左边界，平台功能侧栏和顶部栏始终保留；自动化验收由“全屏脱离后台壳层”改为精确检查两级侧栏和顶部栏同时存在。
- 响应式修复：为结果滚动区、欢迎区、时间线和输入器增加最小宽度与盒模型约束，防止双侧栏在窄视口下把主区撑出屏幕。
- 影响边界：未修改 API、数据库、权限、能力 Schema、适配器或 18 份原始 ComfyUI 工作流 JSON。

## `docs(project): add customer feedback handoff`

- 改造动机：为后续开发者提供单一、可追溯的本轮交接入口，避免重新从需求 DOCX、零散提交和聊天记录推断实现边界。
- 改动范围：新增迭代交接文档，整理原始需求、已完成内容、13 个需求提交映射、验证证据、P0/P1/P2 后续事项、外部依赖、原项目问题和布局纠偏记录；README 与开发日志增加入口。
- Git 说明：记录专题分支基于 `origin/refactor-webUI@62115a040`，并按用户授权推送专题分支；不直接合并或推送 `dev`，不接触 `upstream`。
- 注意事项：文档明确区分已实现前端布局、待接入契约和未验证真实输出，未将报告、LoRA 或无 ComfyUI 环境描述为已具备执行能力。

## `docs(project): add customer requirements analysis`

- 改造动机：把甲方 0820 模块说明从概括性交接记录继续拆解成可逐条讨论、确认和验收的需求清单，避免后续开发者把参考截图、待讨论内容或技术示例误当成已经冻结的交付标准。
- 改动范围：新增 Markdown 与 PDF 两种格式的甲方需求分析材料；按原文章节和业务语义拆分 129 条需求，逐条标记“明确、参考、待定义”性质并保留来源章节；完整引用 DOCX 中 22 张 PNG 配图并补充截图说明。
- 澄清与风险：整理 42 项 P0/P1/P2 甲方确认问题，覆盖页面信息架构、项目/会话/任务层级、工具顺序、API 契约、报告、LoRA、提示词模板、三维、资产归类、品牌授权和非功能指标；另对浏览器保存地址、资产禁止跨领域、指定 Flux、竞品截图代替规格等 10 项高风险表述给出可执行改写建议。
- 真实性边界：分析材料只提炼甲方原始 DOCX，不把既有代码或此前聊天结论反向写成甲方原文，也不声明报告、模型训练、提示词词库或外部服务已经实现。
- 验证范围：原始 DOCX 重新渲染为 15 页并核对 22 张嵌入图片；最终 PDF 为 A4 横向 41 页，已逐页渲染并检查中文字体、图片比例、表格跨页、编号和文字完整性；Markdown 图片路径、PDF 文本提取和需求编号唯一性检查通过。

## `chore(project): merge customer feedback branches`

- 改造动机：本地 `codex/client-feedback-20260820` 与远程 `codex/customer-feedback-frontend` 从同一基线分别实现了 0820 甲方意见，需要在不覆盖任一分支历史的前提下形成单一可交付分支。
- 整合范围：保留远程分支严格对齐需求图的首页门户、两级侧栏、四模式能力目录、报告/训练待接入布局和多图组件；合入本地分支的结果目录选择 API、前端保存弹窗、数据库与浏览器验收，以及单图按原始比例展示等有效改动。
- 冲突处理：删除合并后无运行时引用的重复 `design-modules.ts`，统一使用按后端可见性和能力契约过滤的 `design-modes.ts`；不采用未获甲方确认的静态提示词词库。
- 文档边界：同步修正设计页仍处于统一平台外壳、全局 AI 助手继续存在、结果可选目录入库等事实，并移除泛化“2D 生 3D”能力表述。
- 验证结果：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 32 项单元测试、真实集成验收、模拟 ComfyUI Worker 端到端、系统 Chrome 全量浏览器验收和 `pnpm build:rail` 全部通过；浏览器脚本同时验证结果目录写入数据库、多图连续查看和全局助手唯一性。

## `feat(@rail/web): redesign real-data home`

- 改造动机：合并后的 0820 首页严格采用居中欢迎区和文字按钮，信息层级单薄；用户要求参考新的 AI 生成视觉稿重新设计，同时明确页面内容必须以系统真实数据为准。
- 视觉改造：建立“轨道客室线稿主视觉、四项平台指标、六项功能卡片、最近工作面板”四层首页；线稿由仓库内 Vue 模板和 CSS/SVG 生成，不新增外部图片依赖。宽屏六列、平板三列、窄屏单列，保持平台红与现有外壳一致。
- 数据边界：可访问项目、项目资产、个人设计会话、已发布应用、最近项目、最近设计和任务动态全部读取现有 `/dashboard` 聚合；没有数据时显示空状态。没有复用参考图中的统计数字、案例图片、平台动态、CPU/内存或在线用户数据。
- 业务边界：新建设计、历史设计、资产与项目入口保持真实业务链路；LoRA 训练和报告服务未接入时继续明确显示“待接入”，不创建假任务。未修改平台 API、数据库迁移、权限范围、外部适配器或 18 份 ComfyUI 工作流 JSON。
- 测试文档：更新系统 Chrome 浏览器验收的首页断言，并同步 README、产品需求、架构、质量门禁和开发日志。`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 32 项单元测试、真实集成、模拟 Worker、生产构建和系统 Chrome 全量验收全部通过；1600×1000 与 390×844 真实浏览器专项复核无标题断行和横向溢出。

## `feat(@rail/web): refine home workflows`

- 改造动机：根据首页逐项评审批注，修正状态浮层冗余、快捷按钮不醒目、最近工作缺少图片、项目导航语义重复、历史设计无法检索，以及训练/报告仍使用弹窗的问题。
- 真实数据：最近项目与最近设计合并为无图片的真实任务列表，另设最近资产成果区；资产沿用 `/dashboard` 权限过滤结果，八种类型统一支持，图片通过预览接口获取短时 URL，文件和 3D 等类型显示集中定义的图标，不使用静态示例图。
- 交互范围：任务与资产分别进入对应中心；历史设计支持项目、任务或设计名称、开始和结束时间组合检索；六个入口使用各自主题色操作按钮。
- 页面边界：训练与报告分别迁移到专属路由，未接入外部服务时控件全部禁用并明确说明，未新增适配器或伪造任务。
- 登录行为：登录成功固定进入首页，即使登录地址包含旧业务页重定向参数也不续跳。
- 助手反馈：发送动作开始后立即清空本次文字草稿，消息以会话气泡为准；提交前失败时在输入框仍为空的前提下恢复草稿，避免误判未发送和意外丢字。
- 数据与部署：没有迁移、环境变量、对象存储结构或部署拓扑变化。
