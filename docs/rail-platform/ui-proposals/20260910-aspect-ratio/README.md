# 比例与联动尺寸：四版效果图

使用内置 image_gen 工具生成，仅供方案确认，未修改产品代码。

## 提示词设计说明

共同要求：中文桌面AI设计输入器局部高保真效果图；白底、灰色边框、深红选中态；保留文生图、生成数量、提示词模板、更多与发送按钮，以及下方四个黑蓝色业务入口。提供自动、9:16、2:3、3:4、1:1、4:3、3:2、16:9八种比例；示例选中16:9，精确宽高2048×1152；锁定比例时两边联动，解锁可独立修改。

- A-ratio-grid.png：一个比例入口；4列2行比例宫格，下方宽度、锁定按钮和高度。同一弹层就地调整，推荐方案。
- B-dual-entry.png：比例与精确尺寸两个入口，两面板并排展示对比。实际交互仅打开当前入口；锁定/解锁为同一个开关，不采用图中重复开关。
- C-canvas-preview.png：左侧画布形状和尺寸标注，右侧比例与宽高表单，点击应用。图中悬浮入口为说明性重复展示，落地仅保留工具栏内一个入口。
- D-inline-edit.png：比例弹层仅选比例，宽、高及锁定按钮直接放在输入栏；右侧联动状态卡为效果说明，不进入实际界面。

## 后续行为建议（待确认）

- 比例统一表示宽:高。锁定16:9，宽度改为2048时高度为1152；改高度反向更新宽度。
- 自动表示采用当前工作流默认尺寸；不承诺工作流具备自动判断画幅能力。
- 实际应用遵守当前工作流宽高范围与步长；无法精确满足比例时提示取整或可用尺寸，不能静默提交无效值。
- 图中背景、字体和装饰为概念展示，开发仅替换比例/宽高区域，不扩展改动到页面其他部分。
