# V2.3 内部编制依据与验证记录

本文件仅供项目内部维护，不进入正式研究报告。正式交付件为 V2.3 DOCX；旧版源稿和交付件继续保留。

## 版本基线

| 字段 | 记录 |
| --- | --- |
| 编制日期与取证日期 | 2026-09-08，Asia/Shanghai |
| 代码分支 | `codex/client-feedback-white-shell-20260901` |
| 完整提交 | `51c4095ec269cde31b543dd6c8886802a9a8f452` |
| 提交时间 | `2026-09-01T21:19:51+08:00` |
| 取证期间再次核对时间 | `2026-09-08T11:41:52+08:00` |
| 取证时工作区 | 已有 README、DEVELOPMENT_LOG 的文档修改及未跟踪合同交付目录；保留这些既有变更 |
| 本轮修改范围 | 报告源稿、14 幅图、文档生成与检查工具、V2.3 交付件及内部登记；未修改业务代码、迁移和部署配置 |
| DOCX SHA-256 | `85fa9a1af2a45584e814614a5b6ca689d165ec88ad4803abca6a7d1d51168215` |

## 原始资料及版本差异

合同依据为《面向中车出口产品平台的客运装备内装模块化分区快速设计系统及关键技术研究-技术开发合同（26版）.doc》，SHA-256 为 `1c0d1e8df8721f6d86f15787dbf881df9542493338af5445bfe3c6f21b540fa3`，与原登记一致。读取正文技术任务、指标、成果和节点，以及附件技术规格书；不将商务条款和原件中的个人信息复制进仓库。

技术响应依据为本机微信文件 2026-09 目录内《投标文件模板-技术响应第四版.docx》，本轮可读文件 SHA-256 为 `06598659cb5a926840a5843fd137b6785a6d439f253d4c7c92353ad73a92be6c`。用户此前提供的桌面路径当前不存在；此前登记哈希为 `f429fda7caee0ea4ab19260d383efb137b5033765c598fad857297130ba6a729`，不能因文件同名就认定二者完全相同。已向用户说明此差异。本版以当前可读同名文件的技术章节为参考，不假定其已经替代最终签认文件；正式发布前项目组应核对签认版本，若不同则重新比对受影响的技术方案。

技术响应的段落 408—694 中含功能响应、总体路线、分区识别、平台开发、测试与保障等内容。备选模型、语义融合、规则引擎、向量检索、消息中间件和集群等只作为方法方向或备选项；没有据此推定当前平台已经实现。合同仍是研究任务和验收节点的依据，用户本轮意见限定当前系统多模态的描述范围。

## 关键事实追踪

下列路径均相对于仓库根目录。此表用于后续版本变化时重新取证，不是正式报告的代码说明。

| 主题 | 主要证据位置 | V2.3 采用的事实或限制 |
| --- | --- | --- |
| 助手图文输入 | `apps/platform-api/utils/domain/assistant/provider.ts:74`、`:100`、`:169`、`:204`、`:282` | 构造文本和 `image_url`，按用户取近期会话附件，限制图片数量与容量；非图片附件正文不进入模型上下文；模型请求不是工作流调用 |
| 助手隔离 | 助手会话、消息和附件接口及上述 provider 查询 | 个人聊天按用户隔离；不能把管理员审计权限理解为聊天正文权限 |
| 人工分区 | `apps/web-antd/src/views/platform/workspace/region-annotation.ts` | 颜色、编号、笔画与框选表达用户意图；序列化标记和快照并保留原始资产关系，不是六类区域自动语义识别 |
| 分区工作流 | `apps/platform-api/utils/domain/workflows/catalog.ts:697`、`:754` | 颜色及编号分区作为两类 EasyMark 编辑契约，接入设计工作流；不能归入助手多模态编排 |
| 原图与标记 | `apps/platform-api/api/v1/jobs/index.post.ts`、`apps/platform-api/migrations/019_*.sql` | 输入原图、输入位置和派生标记关系分别校验，标记快照用于历史显示，不替换实际执行原图 |
| 任务执行 | `apps/platform-api/utils/domain/capabilities/comfyui/worker.ts:289`、`:470` | 数据库队列与租约领取、独立执行、外部结果回收；设计输出先暂存，用户确认登记后复用 |
| 业务模式 | `apps/web-antd/src/views/platform/workspace/`、迁移 `030`、`031` 及工作流目录 | 零部件、CMF、客室效果组织工具与提示词模板，不是三个独立算法系统；提示词模板不等于工程参数模型 |
| 训练与报告 | `apps/platform-api/utils/domain/capabilities/lora/worker.ts`、`capabilities/report/worker.ts`、`capabilities/report/renderer.ts` | 独立任务与产物管理；报告有模板与外部 AI 两种方式；这些是支撑能力，不替代分区识别研究和专业评审 |
| 资料类型与版本 | `apps/platform-api/utils/domain/assets/`、前端 `modules/platform/asset-types.ts` | 八类文件类型；业务用途用标签、来源和任务关系表达；版本关联存在不等于完整前端版本恢复功能 |
| 实际部署 | `deploy/rail-platform/compose.production.yaml` 及能力适配配置 | 前端、平台服务、独立执行进程、PostgreSQL 和 MinIO；外部邮件、模型、工作流与训练按环境接入；未将 Redis、独立消息队列或集群写成当前基础设施 |
| 研究与拟建 | 合同 2.1、2.2、技术规格书、技术响应，结合迁移和领域代码检索 | 自动区域识别、四级模块结构化管理、工程参数、规则校核、向量检索、跨区域自动优化及既有产品平台对接仍需研究或建设，不能由通用资产和任务能力推定完成 |

仓库早期说明中“任务执行器尚未完整实现”等描述与当前 ComfyUI、训练及报告 Worker 代码存在时间差。本轮现状描述依据实际执行链路，并在报告中区分基础实现、目标环境验证和合同验收；未将早期说明直接复制成现状，也未因本次文档编制修改无关架构文件。

## 内容与图形修订

1. 将原多模态大篇幅论述收敛为第六章图文助手短节：正文 274 个汉字、1 幅时序图；术语、总图和结论中保留必要边界说明。合同的多模态识别研究仍在附录追踪，不将助手能力当作整个合同任务已经完成。
2. 正文按研究问题、方法选择、分区研究、快速设计、平台构建、数据与集成、计划及验证组织；补充分组试验、独立样本、失败记录、评价口径与实施前提，没有编造样本数、准确率、效率提升和完成率。
3. 按用户论文式方向重绘 14 幅黑白线图，采用分层、正交、树形、时序和甘特表达；主架构与部署、功能、数据流分图展开。拟建链路使用虚线及文字说明，图中的示例不冒充已经确认的车型结构。
4. 图 1—14、表 1—12 全文连续编号。保留研究技术参考，不包含价格、报价、付款、预算或商务响应内容。原件仅作内部依据，不复制商务页。

## 验证与交付范围

使用 documents 技能的规范渲染流程和 diagram-design 技能的图形布局、自检流程；用户指定的论文式黑白视觉覆盖图形技能默认品牌配色。生成链路为可维护 Markdown 与图形脚本 → HTML → 浏览器导出 SVG/3 倍 PNG → DOCX → 规范逐页渲染。

| 检查 | 结果 |
| --- | --- |
| 篇幅 | 源稿正文、标题、表格和图题共 19,979 个汉字（排除内部文件头和图片路径）；DOCX 可见 XML 共 20,219 个汉字（另含封面和目录），图内文字未计入此数 |
| 页数与逐页检查 | 最终 38 页，逐页观察文字、图表、编号、标题与分页；最后微调的第 31、37、38 页再次检查，其余 35 页与已检查版本逐像素一致 |
| 图形 | 14 幅浏览器导出边界检查无溢出；HTML accessible-SVG 自检全通过；人工复核正交走线、节点留白、标签与箭头关系 |
| 目录 | 16 个章级入口、32 个标题/页码超链接，全部存在对应书签；61 个正文标题保留导航层级及书签；所有标题页码与最终渲染一致 |
| 版式 | 指定 A4、页边距、页眉页脚距离、正文宋体小四及各级标题字号字体、固定 24 磅行距通过 XML 核对；图片段落单倍行距避免裁切，封面标题采用独立封面样式 |
| 表格与图片 | 12 张表格重复表头及不可拆行；14 张图片有替代文本；图表编号连续 |
| 内容检查 | 正式正文、表格、图题及 SVG 图内文字对报价/价格/金额/预算/费用/付款/商务/投标及代码路径、提交等检查零命中；“技术响应”只作为技术依据保留，不属于报价内容 |
| 文件与无障碍 | DOCX 压缩包检查通过；无障碍审计高/中/低问题为 0/0/0 |
| 业务运行验证 | 本轮未重跑系统单元、集成、真实 GPU、浏览器业务验收或生产构建；未接触开发数据库及对象存储，不新增运行验证结论 |

本机渲染缺少微软宋体/楷体/黑体原字体时使用已安装 CJK 字体回退；DOCX 中仍明确设置用户指定的字体名称。目标 Word/WPS 环境如字体度量导致重新分页，应重新生成并核对 `TOC_V2.3.json` 显示页码；目录使用内部超链接，跳转目标是标题书签，不依赖显示页码。目录不是原生自动更新 TOC 域。

可复现命令（从本文档目录执行，Python/Node/浏览器依赖由运行环境提供，不将本机运行时路径写入项目脚本）：

```bash
python tools/build_figures_v2_3.py
node tools/export_figures_v2_3.cjs
python tools/build_report_v2_3.py
python <documents-skill>/render_docx.py <V2.3.docx> --output_dir <临时目录> --emit_pdf
python tools/audit_report_v2_3.py --pdf <渲染PDF>
python <documents-skill>/scripts/a11y_audit.py <V2.3.docx>
python <diagram-design-skill>/scripts/self_check.py assets/v2-3/*.html
```

如浏览器未在 Playwright 默认位置，可通过 `REPORT_CHROME_PATH` 指向已安装 Chromium/Chrome。最终质量统计见 `QA_V2.3.json`。中间 PDF 和页面 PNG 仅作内部 QA，未作为新增交付件存放在 deliverables 中。
