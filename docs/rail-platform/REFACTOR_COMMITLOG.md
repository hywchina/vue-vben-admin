# 核心模块重构提交记录

## chore: capture core refactor baseline

- 提交哈希：`0c352a574`
- 提交范围：从 `dev` 创建 `refactor-core-module`，以空提交标记完整原始仓库状态。
- 优化目的：为后续人工重构提供可比较、可回退的代码基线。
- 解决问题：避免重构改动与输入分支原始内容混淆。
- 变更要点：未修改任何文件、配置、数据或运行行为。
- 架构细节：基线覆盖 Web、平台 API、迁移、脚本、部署和文档。
- 兼容性说明：业务行为完全不变。
- 验证结果：提交前工作区干净；Git 提交钩子无待检查文件。
- 遗留事项：后续提交按领域边界、公共层、测试和文档独立拆分。

## docs(project): record core refactor inventory

- 提交哈希：`db5da86c8`
- 提交范围：核心模块范围、保留/隔离决策、结构问题、风险和回归矩阵。
- 优化目的：将阶段 0 的分析结论固化为后续执行与验收依据。
- 解决问题：明确平台核心代码、Vben 框架能力、模板观察区和待退出样例数据之间的边界。
- 变更要点：新增 `REFACTOR_INVENTORY.md` 和本提交记录；重构计划状态更新为执行中。
- 架构细节：以项目、资产、应用任务和 AI/外部能力为主线规划前后端分层。
- 兼容性说明：仅文档变更，不影响运行行为。
- 验证结果：已核对当前分支、远程、核心目录、测试入口和近期 Git 历史。
- 遗留事项：进入前端领域 API 与类型边界重构。

## refactor(@vben/web-antd): split platform domain boundaries

- 提交哈希：`1a7938676`
- 提交范围：Web 平台领域类型、API 客户端、预签名文件上传和静态样例数据。
- 优化目的：让项目、资产、应用、任务、权限、审计通知和 AI 助手可以沿有限路径独立定位与扩展。
- 解决问题：消除单一平台 API 文件和单一类型文件承担全部领域职责的问题，避免资产与 AI 附件重复实现直传逻辑。
- 变更要点：API 按领域拆分并由原入口统一导出；类型按领域拆分并保留原导出路径；新增预签名上传辅助函数；删除无运行引用的 `data.ts` 静态样例。
- 架构细节：页面和 Store 继续依赖稳定 barrel export，未改变请求路径、请求体、响应类型或上传完成登记顺序。
- 兼容性说明：公共导出、HTTP 契约、错误文案和用户可见行为保持不变；运行时数据仍全部来自平台 API。
- 验证结果：`pnpm typecheck:rail` 通过；`pnpm test:rail` 通过（API 25 项、Web Store 7 项）。
- 遗留事项：继续抽取 Store 纯状态逻辑，并收敛后端共享工具目录。

## refactor(@vben/web-antd): extract platform store helpers

- 提交哈希：`7a02144c2`
- 提交范围：平台 Store 的任务状态判断、任务时长归一化及对应单元测试入口。
- 优化目的：将纯状态规则从 Pinia 协调层分离，便于复用并降低 Store 测试成本。
- 解决问题：任务活动状态和 API 时长展示转换原本内嵌在响应式状态代码中，无法独立验证。
- 变更要点：新增 `store/platform/helpers.ts` 和辅助函数测试；Store 改为调用纯函数；Web 单测入口覆盖全部平台 Store 测试文件。
- 架构细节：Store 继续负责项目上下文、API 协调和缓存，辅助层只接收和返回平台任务值对象。
- 兼容性说明：Store 标识、公开字段、任务筛选条件和数字时长中文展示保持不变。
- 验证结果：Web Store 测试 2 个文件、9 项通过；Web 类型检查通过。首次类型检查暴露测试夹具交叉类型问题，修正夹具后通过。
- 遗留事项：后端共享工具按领域、身份、HTTP 和基础设施分层。

## refactor(@rail/platform-api): organize core domain services

- 提交哈希：`3ebe03b1e`
- 提交范围：资产、项目访问、AI 助手、审计、通知和任务能力适配器边界。
- 优化目的：让后端核心实现按业务领域定位，同时保留现有 API 路由和工具导入兼容性。
- 解决问题：领域实现原本与身份、HTTP、配置、数据库和对象存储工具平铺在同一目录；外部任务能力缺少代码级稳定契约。
- 变更要点：核心实现与测试迁入 `utils/domain/*`；原 `utils` 路径改为兼容导出层；新增 `CapabilityAdapter` 契约与稳定未配置错误常量。
- 架构细节：领域实现通过基础设施工具访问 PostgreSQL/对象存储；路由仍通过原有工具入口调用，不改变 Nitro 文件式 REST 路径。
- 兼容性说明：数据库结构、SQL、事务、对象键、审计字段、响应结构和错误码均保持不变；没有接入或模拟任何外部执行器。
- 验证结果：平台 API 类型检查通过；9 个测试文件、25 项单元测试通过。首次测试发现 Vitest 不解析 Nitro 路径别名，领域内部改用明确相对路径后通过。
- 遗留事项：继续整理身份、HTTP 与基础设施公共层，并补齐目录说明。

## refactor(@rail/platform-api): separate shared service layers

- 提交哈希：`37c5d3e63`
- 提交范围：平台 API 的身份会话、HTTP 请求响应、配置、数据库、对象存储、邮件与健康检查工具。
- 优化目的：建立领域、身份、HTTP 和基础设施之间清晰、可持续的单向依赖结构。
- 解决问题：横切能力与业务领域实现平铺混杂，新增代码难以判断归属和依赖边界。
- 变更要点：实现迁入 `identity/`、`http/`、`infrastructure/`；原路径保留兼容导出；测试随实现移动；新增共享层目录说明。
- 架构细节：API 路由继续依赖稳定入口，内部实现按 `api -> domain/identity/http -> infrastructure` 组织；基础设施不反向依赖领域。
- 兼容性说明：配置名、Cookie、令牌、SQL、S3、SMTP、健康检查、统一响应及错误结构保持不变。
- 验证结果：平台 API 类型检查通过；9 个测试文件、25 项单元测试通过。
- 遗留事项：更新架构、质量、部署/运维和开发日志，并执行完整交付门禁。

## docs(project): align core refactor delivery guides

- 提交哈希：`7b2f7e93b`
- 提交范围：架构、质量门禁、部署、运维、开发记录和重构状态。
- 优化目的：让代码分层、兼容边界、测试入口、发布步骤和回滚方式在交接材料中可复现。
- 解决问题：旧架构仍描述已删除静态样例与平铺后端工具，部署材料尚未说明纯代码重构的回滚边界。
- 变更要点：更新前后端目录说明、能力适配器契约、质量证据、发布回滚步骤和开发时间线。
- 架构细节：记录稳定 barrel export 和后端根目录兼容导出层，明确新增实现的归属规则。
- 兼容性说明：仅文档变更；本轮无迁移、环境变量、端口或部署拓扑变化。
- 验证结果：阶段类型检查和单元测试已通过；完整质量门禁与浏览器冒烟待本提交后执行并回填。
- 遗留事项：执行全量规范、类型、单元、集成、构建和真实浏览器验收。

## style(project): format rail platform documents

- 提交哈希：`8740e0177`
- 提交范围：全仓规范检查报告的 6 份 `docs/rail-platform` Markdown 文档。
- 优化目的：恢复仓库统一 Markdown 格式并解除发布规范门禁。
- 解决问题：完整 `pnpm lint` 首次运行发现计划、需求、部署、质量和重构清单存在格式差异。
- 变更要点：仅使用仓库锁定的 Oxfmt 执行机械排版，不修改文档事实和技术语义。
- 架构细节：无架构或代码变化。
- 兼容性说明：不影响运行、API、数据库、配置或部署行为。
- 验证结果：格式化后 `pnpm lint` 通过，覆盖 1,943 个文件。
- 遗留事项：执行真实集成与浏览器冒烟。

## test(project): record core refactor acceptance

- 提交哈希：`ea6599b21`
- 提交范围：完整质量门禁、真实服务集成、生产 Compose 解析、浏览器冒烟和重构状态。
- 优化目的：形成可复现的重构交付证据并关闭执行计划。
- 解决问题：阶段测试不能单独证明跨 PostgreSQL、MinIO、浏览器和生产构建的完整可交付性。
- 变更要点：将最终命令结果、浏览器观察、部署解析和未重复演练项回填到质量、开发和重构文档。
- 架构细节：验证覆盖 Web barrel、Store 纯函数、Nitro 兼容导出、领域分层、对象存储、权限隔离和外部能力失败边界。
- 兼容性说明：本提交只更新验证证据和文档状态，不改变业务行为。
- 验证结果：`pnpm lint`、`pnpm typecheck:rail`、`pnpm test:rail`、`pnpm test:rail:integration`、`pnpm build:rail` 全部通过；生产 Compose 解析通过；管理员浏览器核心路径通过且控制台错误为 0。
- 遗留事项：正式上线前执行生产数据备份恢复演练；既有 Vben 表单兼容与存储前缀警告后续作为独立框架维护项处理。

## 2026-08-08：移除未使用的上游工作区

### `chore(project): remove unused upstream workspaces`

- 删除未参与轨道平台运行的旧 Mock 服务、Ant Design Vue Next、Element Plus、Naive UI、TDesign Web 变体、Playground 和 Vben 文档站。
- 删除 Changesets、Gitpod、Tea、旧 VS Code 工作区生成器和 Playground 发布脚本。
- 将 pnpm 工作区显式收敛到 `apps/platform-api`、`apps/web-antd`、共享 `packages`、`internal` 与必要脚本。
- 移除开发环境默认启用的 Nitro Mock 插件及其环境变量、类型和配置，确保本项目只连接真实平台 API。
- 保留仍被产品 Web 与构建链使用的 Vben 共享包、内部工具和 MIT 许可证，不进行无依据的框架重写。

验证结果：

- `pnpm install --lockfile-only`：通过，锁文件已按精简后的 40 个工作区刷新。
- `pnpm typecheck:rail`：通过。
- 旧工作区、Playground、VitePress、Nitro Mock 与旧工作区文件名的保留路径扫描：无结果。
- 额外直接编译 `internal/vite-config` 时仍会命中既有 Sass/Vite 测试类型兼容问题；该命令不是项目质量门禁，本次改动未扩大该问题。
