# 二次开发记录

## 2026-08-13：镜头控制与 ComfyUI Qwen Multiangle 对齐

- 以本机 `ComfyUI-qwenmultiangle` 节点真实源码为准，统一水平、垂直和距离预设：水平 `0/45/90/135/180/225/270/315`，垂直 `-30/0/30/60`，距离 `1/4/8`；同步修正单视角能力的数值范围和步长，仍写入原工作流的真实 `horizontal_angle`、`vertical_angle`、`zoom` 输入，未修改原始 API JSON。
- 镜头控制器增加画布拖拽自由环绕、滚轮距离调整、三类预设下拉、精确滑杆/数值输入、默认值恢复和与 ComfyUI 一致的 `<sks>` 镜头语义提示；所有交互双向更新同一组能力参数。
- 设计会话与旧应用工作区均把当前已选图片的预签名预览地址传入镜头控制器，图片显示在中央被摄平面；切换镜头参数不重新上传或丢失素材。
- 增加镜头预设和提示词阈值单元测试，并扩展浏览器验收覆盖中央图片解码显示、自由拖拽角度回写、三类预设修改及刷新后的图片与草稿恢复。

## 2026-08-08：Linux 开发环境镜像源调整

- 开发 Compose 的 PostgreSQL 改用 `m.daocloud.io/docker.io/library/postgres:17.6-alpine`，避免开发机直连 Docker Hub 超时。
- Mailpit 改用 `m.daocloud.io/docker.io/axllent/mailpit:v1.30.0`，同样避免开发机直连 Docker Hub。
- MinIO、镜像版本、端口、环境变量、健康检查和命名卷保持不变；不修改生产 Compose。
- 开发 Compose 配置解析通过，后续仍通过 `pnpm dev:rail` 一键启动。

## 2026-08-08：核心平台模块分层重构

- 从 `dev` 创建 `refactor-core-module`，以 `0c352a574` 标记未修改的原始基线，并建立独立中文 `REFACTOR_COMMITLOG.md`。
- 新增 `REFACTOR_INVENTORY.md`，明确平台核心、Vben 框架保留区、模板隔离观察区、风险和回归矩阵。
- Web 平台 API 和领域类型按项目、资产、应用、任务、权限、审计通知与 AI 助手拆分，原统一导出路径保持兼容。
- 资产与 AI 附件共用预签名直传辅助函数；删除确认无运行引用的 `modules/platform/data.ts` 静态样例。
- 平台 Store 抽取任务活动状态和时长归一化纯函数，单测入口扩展为全部 `platform*.test.ts`。
- 平台 API 实现按 `domain`、`identity`、`http`、`infrastructure` 分层，根目录同名文件保留为兼容导出层，现有 REST 路径、脚本和 Nitro 自动导入不变。
- 新增 `CapabilityAdapter`、执行状态快照和 `ADAPTER_NOT_CONFIGURED` 稳定常量；没有接入外部执行器，也没有伪造任务成功。
- 本轮没有数据库迁移、环境变量、对象键、部署端口或生产拓扑变化；回滚可重新部署 `0c352a574`，不需要回退数据。
- 阶段验证：API 9 个测试文件 25 项通过；Web Store 2 个测试文件 9 项通过；Web/API 类型检查通过。
- 完整门禁通过：`pnpm lint` 检查 1,943 个文件；Web/API 类型检查通过；API 9 个文件 25 项与 Web 2 个文件 9 项测试通过；Nitro 与 Web 生产构建通过，Web 构建转换 8,070 个模块并生成 `dist.zip`。
- 真实集成通过：认证、权限/项目隔离、文本与图片资产、MinIO 对象存储、AI 会话和审计链路均通过，测试数据由脚本按本轮唯一标识清理。
- 真实浏览器通过：管理员登录、2 个项目、当前项目 2 项资产、6 个应用、动态应用工作区、任务/审计/权限页面和 AI 助手待配置状态均正常；控制台错误为 0，仅保留既有 Vben 表单兼容与存储前缀警告。
- 生产 Compose 使用示例环境完成 `config --quiet` 解析；本轮未重复生产数据备份恢复演练，因为没有迁移或数据结构变化，该项仍是正式上线前门禁。

## 2026-08-04：建立仓库级 AGENTS.md 协作规范

- 在仓库根目录新增 `AGENTS.md`，将本会话确认的产品定位、模块职责、数据流、角色权限、资产文件类型、AI 会话隔离、审计边界和外部适配器原则固化为后续开发规则。
- 明确开发环境与生产部署边界、数据库迁移追加规则、破坏性数据操作限制、完整质量门禁和文档同步要求，避免后续把真实平台重新退化为静态 Demo 或胶水代码。
- 固化 `origin/dev` 维护二次开发、`upstream/main` 跟踪官方基线的 Git 协作方式，并要求提交前核对远程、分支、敏感配置和工作区状态。
- 文档只使用仓库相对路径，不依赖当前电脑的绝对目录；其他机器克隆 `dev` 分支后可以获得相同的 Agent 项目上下文。

## 2026-08-04：资产按文件类型重新划分

- 将原来混合“文件形态”和“业务用途”的 9 类资产收敛为 8 类文件：图片、视频、音频、文本、文档、3D 模型、模型文件和压缩包。
- 遮罩与图片材质转为图片，材质包转为压缩包，LoRA 转为模型文件，报告转为文档；原业务含义写入标签，不会因迁移丢失。
- 新增 `009_file_based_asset_types.sql`，迁移历史数据、更新 6 个应用的输入/输出契约，并为数据库 `assets.kind` 增加 8 类值约束。
- 前端将类型标签、图标和允许扩展名收口到同一模块；资产筛选、登记表单、详情、概览、应用中心与应用工作区使用同一定义。
- API 同时校验 MIME 与关键扩展名，解决了前端允许材质 ZIP、后端却只允许图片的旧不一致问题。
- 迁移前备份为 `/tmp/rail-platform-before-asset-type-migration-20260804.dump`。迁移后数据库类型、约束和应用契约实查正确；API 9 个测试文件共 25 个用例、Web 7 个状态用例、Web/API 类型检查、全仓规范检查和生产构建全部通过。
- 真实浏览器确认资产筛选和登记表单只显示 8 类文件，应用中心与 CMF 工作区已显示新的输入/输出类型；旧的材质、遮罩、LoRA、报告不再出现于文件类型下拉框。

## 2026-08-04：跨平台与 Docker 正式部署闭环

- 新增 `DEPLOYMENT.md`，覆盖 Linux、macOS 和 Windows，明确分为“本地 Node.js + Docker 基础设施”的源码模式，以及“核心进程全部容器化”的单机内网正式模式。
- 明确用户、项目、资产、应用、任务、日志和 AI 聊天是 Web/API 内的业务模块，不是需要分别启动的容器；模型推理、ComfyUI 和 LoRA 训练仍为外部 API 服务。
- 新增多阶段生产镜像、Nginx 同域 API 代理、生产 Compose 和可复制的环境模板。正式数据库与 API 不映射宿主机端口，MinIO 管理台仅绑定 127.0.0.1。
- 生产启动按 PostgreSQL 健康、迁移成功、MinIO 健康、API ready、Web 的顺序执行；数据库迁移镜像仅携带必需依赖，由约 2.91 GB 降至约 521 MB。
- 生产配置校验新增 `CHANGE_ME` 占位符拒绝规则，本地密钥文件同时加入 Git 和 Docker 构建忽略列表。
- 文档包含端口/防火墙、启停、升级、日志、备份、精确恢复、离线镜像转移、架构兼容、故障排查和交付验收清单。
- 使用独立的 18080/19000 端口和全新测试数据卷实际构建并启动正式 Compose；8 个迁移、管理员种子、Web、PostgreSQL、MinIO、API ready、管理员登录和备份命令均通过。测试发现并修复容器中 `localhost` 解析导致健康检查连接被拒的兼容问题。
- 最终验证：生产 Compose 解析通过，Web/API/迁移镜像构建通过，全仓 `pnpm lint` 通过，API 类型检查通过，9 个测试文件共 24 个用例通过。验收后只删除本轮临时生产容器和测试卷，原有开发数据卷保留。

## 2026-08-04：产品工程成熟度审计与 P0/P1 整改

- 将 16,009 行平台二次开发成果固化为 Git 基线 `1d8c62246`，提交前规范、格式、样式和全仓类型检查通过。
- 建立需求编号、非功能基线、开发准入/完成定义、P0/P1/P2 路线图和运维手册，明确已实现、部分实现和未闭环内容。
- 生产启动强制检查数据库、JWT、管理员密码、对象存储和 CORS，禁止开发默认凭据和跨域通配符。
- 将宽泛 CORS 改为来源白名单，添加安全响应头；被拒绝请求返回无堆栈统一错误。
- 新增 `/health/live` 和 `/health/ready`，就绪包含 PostgreSQL 与 MinIO/S3，存储失败后允许恢复重试。
- 新增真实集成验收：三账号、项目隔离、文本资产、图片上传、AI 会话隔离、日志范围和自动清理。
- 新增独立 CI，持续执行规范、类型、单元测试、真实服务集成和生产构建。
- 故障注入验证 MinIO 停止时 ready 返回 503 且标出 storage down，依赖恢复后 API 无需重启自动回到 200。
- 浏览器烟雾验收完成普通用户真实登录、主框架六个业务入口、AI 助手入口和零控制台错误检查。

## 2026-08-04：全局 AI 设计助手与用户私有聊天

### 需求与交互定位

- 以 `frontend-demo/rail-cabin-ai-prototype.html` 的右下角助手为参考，但将其改造为登录后的全局平台组件，不属于某个业务路由。
- 视觉使用企业红、石墨、冷灰和白色，新增轨道节点顶线和信号灯式悬浮入口，保留平台自身品牌语言，没有直接复制原型。
- 桌面端为 430 px 宽右下弹窗，移动端自适应为近全屏；支持键盘焦点、Enter 发送、Shift + Enter 换行、Escape 关闭与减少动效偏好。
- 聊天窗口提供历史对话、新对话、项目上下文、建议问题、文本、图片/音视频/文档附件、图片预览、文件下载、清空与外部服务状态。

### 数据层与 API

- 新增迁移 `008_ai_assistant.sql`，建立 `ai_conversations`、`ai_messages`、`ai_attachments` 三张表及用户/会话时间索引。对话可选关联项目，消息保留 completed/failed 状态、稳定错误码和可选上游消息编号。
- 实现 `/api/v1/assistant/status`、`conversations/**`、`attachments/**` 接口，覆盖对话列表、创建、读取、发送、清空，以及附件申请上传、完成校验、删除、预览和下载。
- 附件沿用私有 MinIO/S3 桶与预签名直传，对象键使用 `assistant/<user>/<conversation>/...` 前缀。默认单附件 50 MiB，支持常见图片、音频、视频、文本、PDF、Office 和 ZIP，拒绝可执行文件与 SVG。
- 清空当前对话会在事务中删除消息与附件关系并重置标题，再逐个清理对象文件；对象删除失败会留下审计元数据。

### 外部 AI 适配器

- 通过 `AI_ASSISTANT_API_URL`、`AI_ASSISTANT_API_KEY`、`AI_ASSISTANT_MODEL`、`AI_ASSISTANT_TIMEOUT_MS` 和 `AI_ASSISTANT_MAX_ATTACHMENT_BYTES` 配置，浏览器不获得服务密钥。
- 平台后端只向完整 API 地址发送当前用户、可选项目、对话编号、系统提示、最近 50 条已完成消息和附件短时签名 URL。接受 `{ content, messageId? }` 或 `{ data: { content, messageId? } }` 响应。
- 外部服务未配置、超时、限流、返回非 2xx 或格式无效时，保存 failed 助手消息与错误码，不伪造成功回复，也不回滚已经保存的用户消息。

### 隔离、审计与验收

- 每个对话、消息、上传、预览、下载和清空查询都在 SQL 中强制当前 `user_id`。管理员也只能查看自己的聊天正文；管理员可在操作日志中看到他人的动作类型和结果，不能看到聊天内容。
- 业务审计覆盖对话创建、消息发送、附件上传/删除与清空。经数据库实查，发送日志仅含 `contentLength`、`attachmentCount`、`providerResult`、`errorCode` 等元数据，不含聊天正文。
- 真实 API 联调完成了用户 1 创建对话、PNG 上传、MinIO 完成校验、消息持久化、图片预览与清空。用户 2 直接读取用户 1 的对话返回 404，历史列表也不包含该对话。清空后消息数为 0，原附件预览返回 404。
- 真实浏览器验收了全局入口、弹窗视觉、发送、“服务待接入”失败状态、页面刷新后恢复历史、清空确认与切换用户后的历史隔离。用户 2 弹窗的历史选项只有“新对话”，页面没有新增 JavaScript 错误。
- 迁移 `008_ai_assistant.sql` 已实际应用。`pnpm lint`、`pnpm typecheck:rail`、`pnpm test:rail` 和 `pnpm build:rail` 全部通过；平台 API 7 个测试文件、19 个用例通过，Web 状态 7 个用例通过，Nitro 与 Web 生产包均成功构建。

## 2026-08-04：两角色账号重建与全量操作日志

### 角色与账号收敛

- 平台角色正式收敛为 `admin` 和 `user` 两种；原 `super` 的账号和权限迁移并入 `admin`，数据库约束禁止再创建其他角色代码。
- 每个账号必须且只能拥有一个角色；管理员不能修改自身角色或状态，系统不能降级或停用最后一名启用管理员。
- 迁移 `006_two_role_model.sql` 负责兼容已有数据库，初始迁移和种子脚本也同步为两角色模型。
- 在执行账号删除前创建 PostgreSQL 自定义格式备份；本机验证备份为 `/tmp/rail-platform-before-user-reset-20260804.dump`。其他机器重建前必须生成自己的备份，命令见复现指南。
- 使用带显式确认参数的 `reset-users.ts` 删除原四个账号并创建 `admin`、`user1`、`user2`。项目、资产、版本和任务归属在同一事务中转移给新管理员，没有产生孤儿记录。
- 账号重建脚本及默认账号均由环境变量配置，可在其他机器复现；维护动作写入 `system.users.reset` 审计事件。

### 全量操作日志

- 迁移 `007_comprehensive_audit.sql` 为日志增加操作者用户名、姓名、角色快照，以及 HTTP 方法、路径、状态码、耗时和 User-Agent，并补充操作者、模块和请求路径索引。
- 所有 Nitro API 都经过统一响应层；每个已认证请求无论成功或失败都会写入 `api.request`，业务写接口继续写入更明确的领域动作。
- 退出接口会先通过 HttpOnly 刷新 Cookie 反查会话用户再撤销会话，因此即使请求没有 Bearer 令牌，也会同时留下可归属用户的 `auth.logout` 和 API 请求日志。
- 登录、注册、密码重置、退出和系统维护保留独立事件；Vue 路由切换通过 `/audit-events/client` 写入 `page.view`。
- 操作日志从管理员子路由调整为所有登录用户可访问的 `/audit`。后端强制 `admin` 可读全部、`user` 仅可读自身，普通用户提供其他 `actorId` 也会被忽略。
- 日志页支持模块、操作者、关键字筛选和服务端分页。操作者显示姓名、`@用户名` 和角色快照，因此多个管理员及发生过角色变化的账号可以明确区分。
- 日志不保存密码、令牌、完整请求体、键盘输入或文件正文；“每一步操作”的审计边界为页面访问、API 请求和业务动作，而非原始鼠标与键盘遥测。

### 数据和接口验收

- 数据库最终为 3 个账号、2 个角色；保留 2 个项目、4 个资产和 4 个资产版本，项目/资产/版本孤儿数均为 0。
- 真实接口验证管理员可读取 `admin`、`user1`、`user2` 三名操作者的日志，普通用户返回范围为 `self` 且每条 `actor_id` 都等于自身。
- 临时将 `user2` 提升为第二名管理员，确认其可读取全部日志且留下 `admin` 角色快照；随后恢复为 `user`，最终账号结构仍为一个管理员和两个普通用户。
- 新增两角色和日志可见范围单元测试。平台 API 5 个测试文件、14 个用例全部通过，Web 平台状态 7 个用例全部通过。
- `pnpm lint`、`pnpm typecheck:rail`、`pnpm build:rail` 和 `pnpm install --frozen-lockfile` 全部通过；迁移与种子脚本重复运行无错误。
- 真实浏览器确认管理员页面显示 3 个账号和 2 张角色卡；管理员日志同时显示 `admin`、`user1`、`user2` 及第二管理员历史快照，普通用户日志页没有账号筛选器且只显示 `@user1`。
- 浏览器验收发现日志总数与并发写入的列表曾使用两个数据库快照，短暂显示“当前页 21 条，共 20 条”；改为同一 SQL 使用窗口计数后复验为“当前页 28 条，共 28 条”。

## 2026-08-04：企业邮箱与密码重置闭环

根据国企内网通常使用自有邮件系统的场景，邮箱保留为正式账号字段，并完成可替换企业 SMTP 的真实找回密码链路：

- 数据库迁移 `005_email_password_reset.sql` 为用户增加唯一企业邮箱，并新增一次性密码重置令牌表。
- 注册和个人基本资料接入真实企业邮箱；管理员用户列表可查询和显示邮箱。
- 忘记密码页调用真实申请接口，新建重置密码页完成新密码设置，不再只是无动作的表单。
- 平台 API 通过 Nodemailer 连接 SMTP；开发环境由 Docker Compose 中的 Mailpit 接收邮件，生产环境通过 `SMTP_*` 环境变量替换为企业邮件系统。
- 重置令牌使用密码学安全随机数，数据库只保存 SHA-256 摘要，默认 30 分钟有效且只能使用一次。
- 申请接口使用统一响应避免枚举账号，并增加 IP/账号频率限制；成功重置后撤销该用户全部刷新会话。
- 个人中心恢复“安全设置”入口，但仅保留真实企业邮箱绑定状态；不恢复手机、密保问题、MFA 或虚假备用邮箱。
- 新增密码重置工具单元测试；平台 API 9 个用例、Web 平台状态 6 个用例全部通过，冻结锁依赖安装、全仓格式、类型检查和生产构建通过。
- 真实浏览器完成“邮箱注册 → 申请重置 → Mailpit 收信 → 设置新密码 → 新密码登录”闭环，并确认同一链接二次使用会被拒绝。
- 真实浏览器确认个人中心恢复“安全设置”，该页只显示当前账号的企业邮箱绑定状态。

## 2026-08-04：当前系统架构盘点

根据当前代码、数据库迁移和 Docker 配置重新梳理系统架构，避免继续沿用第一版演示阶段的理解：

- 明确当前运行结构为 Vue/Vben Web、独立 Nitro API、PostgreSQL 和 MinIO 四层，Docker Compose 当前只承载数据库和对象存储。
- 将前端页面、平台 API、数据表、对象存储、认证、RBAC 和项目数据范围逐一映射到对应代码目录。
- 补充总体架构、开发部署、固定页面区域、数据模型、登录刷新、项目切换、资产上传和应用任务等架构图与数据流图。
- 明确 ComfyUI、大模型、LoRA、2D 生 3D、报告生成等尚未接入；当前任务接口只保存真实台账，未配置适配器时返回 `ADAPTER_NOT_CONFIGURED`。
- 记录项目成员管理、任务 Worker、输出回流、应用管理、资产版本前端、生产部署与可观测性等当前缺口。
- 标记 `apps/web-antd/src/modules/platform/data.ts` 为未被运行链路引用的早期静态样例，防止后续误认为业务数据源。

完整说明见 `docs/rail-platform/ARCHITECTURE.md`。

## 2026-08-04：真实图片预览与个人中心生产化

### 图片资产显示真实内容

资产卡片原先只根据类型显示统一的色块和图标，无法直接辨认已上传图片。本轮新增受权限保护的图片预览链路：

- 平台 API 新增 `GET /api/v1/assets/:id/preview`，验证登录身份、资产读取权限、项目数据范围、资产状态和图片 MIME 类型。
- MinIO/S3 仍保持私有桶；接口返回短时有效的内联预签名地址，不公开对象、不把二进制写入数据库。
- 图片、遮罩和图片材质在资产卡片中懒加载真实缩略图；详情抽屉以完整比例展示图片。
- 加载中和加载失败都有明确回退状态，非图片资产继续显示各自的类型图标。
- 预览地址接口与对象读取均完成真实联调，返回状态分别为成功和 HTTP 200。

### 个人资料与权限边界

个人中心原先沿用上游演示数据：普通用户可以编辑角色，“更新基本信息”没有提交处理，“安全设置”和修改密码均为假功能。本轮完成以下调整：

- 删除“安全设置”标签页及手机、邮箱、MFA 等与当前用户名密码登录模式不符的演示内容。
- 登录用户名和平台角色在个人资料页只读；姓名、部门、个人简介通过 `PATCH /api/v1/user/profile` 持久化到 PostgreSQL。
- 新增迁移 `004_user_profile.sql`，保存个人简介和站内消息提醒偏好。
- “更新基本信息”增加真实提交、加载状态、成功提示，并同步刷新顶栏当前用户信息。
- “修改密码”接入真实校验与 scrypt 重新散列；成功后撤销该用户全部刷新会话并要求重新登录。
- “新消息提醒”接入 PostgreSQL 偏好读写，不再是只改变视觉状态的演示开关。

### 管理员角色调整

- 新增管理员专用 `PUT /api/v1/users/:id/roles` 接口，后端强制要求 `platform:role:write` 权限。
- 普通用户调用该接口会返回 `FORBIDDEN`；管理员只能修改其他用户，不能修改自身角色。
- 只有超级管理员可以授予或移除超级管理员角色，且平台始终至少保留一名可用超级管理员。
- “用户与权限”页面新增真实角色调整弹窗；当前用户的角色按钮禁用，角色定义卡片移除无效的省略号按钮。

### 验证结果

- 数据库迁移 `004_user_profile.sql` 已成功应用。
- API 联调：资料更新、通知偏好、管理员角色修改、图片预览均成功；普通用户角色修改被正确拒绝。
- 平台 API 新增资产工具与密码散列单元测试，共 6 个用例通过。
- Web 平台状态测试扩展为 6 个用例，包含管理员角色更新状态同步，全部通过。
- 全仓 `pnpm lint` 通过，包括格式、Oxlint、ESLint 与 Stylelint。
- `pnpm typecheck:rail` 通过，Web 与 API 均无类型错误。
- `pnpm test:rail` 通过：平台 API 6/6，Web 平台状态 6/6。
- `pnpm build:rail` 通过：Nitro API 与 Web 生产包均成功生成，Web 构建产物为 `apps/web-antd/dist.zip`。
- 真实浏览器验证了图片卡片与详情预览、个人资料保存、安全设置移除、修改密码表单、消息偏好和管理员角色弹窗。

## 2026-08-04：资产上传入口可见性优化

浏览器验收发现，登记资产弹窗直接使用操作系统原生文件输入框，“选择文件”按钮面积小、视觉层级弱，用户不容易识别为主要操作。

处理内容：

- 将原生按钮收纳为无障碍文件输入控件，界面改为整块可点击的上传区域。
- 使用平台企业红、上传图标和“点击选择文件”动作强化入口，同时保留清晰的键盘焦点反馈。
- 文件选中后切换为“文件已选择”状态，展示文件名、格式化后的大小以及“重新选择”入口。
- 按资产类型限制文件选择范围；切换资产类型或关闭弹窗时清理旧文件状态，防止误传不匹配文件。
- 完成 Web 类型检查、目标文件格式检查、ESLint、Stylelint 和真实浏览器视觉验收。

## 2026-08-04：登录与注册交互修复

### 注册协议状态修复

浏览器验收发现：注册页的“我同意隐私政策与条款”在视觉上已经勾选，但提交时仍提示必须同意。

根因是表单组件映射将内置 `VbenCheckbox` 绑定到 `checked`，而该组件实际通过 Vue 的默认 `modelValue` 协议传值。点击只改变了复选框自身的视觉状态，表单字段没有收到新值。

处理内容：

- 将内置复选框的表单协议统一为 `modelValue`。
- 新增表单集成回归测试，验证点击前协议校验失败、点击后表单值变为 `true` 且校验通过。
- 表单集成测试共 27 个用例，全部通过。

### 内网登录方式收敛

根据公司内网使用场景，登录页已收敛为用户名密码登录：

- 删除用于演示的角色/账号快捷选择器，用户必须输入自己的用户名和密码。
- 关闭手机号登录与扫码登录入口。
- 关闭微信、QQ、GitHub、Google 和钉钉等第三方登录区域。
- 保留滑块验证、记住账号、忘记密码和自助注册入口；自助注册可在后续改为仅管理员创建账号。

本轮验证结果：全仓 `pnpm lint` 通过，Web 应用类型检查通过，平台状态测试 4/4 通过，表单集成测试 27/27 通过，生产构建通过。验收浏览器当时已有登录态，访问登录与注册路由会被权限守卫重定向到工作台，因此没有强制退出当前用户；未登录页面可在退出登录或无痕窗口中复核。

## 2026-08-03：平台框架第一版

### 目标转译

将口头需求整理为两个边界清晰的系统：

- 平台系统：用户、权限、登录、项目、资产、任务、审计和稳定的 Web 外壳。
- 能力系统：ComfyUI 工作流、大模型、LoRA 训练、2D 生 3D 等第三方接入。

本次只实现第一部分，并为第二部分留下统一应用契约、工作区和资产回流契约。

### 1. 基线审计

- 确认上游为 Vben Admin 5.7.0。
- 记录起点提交 `9f5b1cd9fbc7ee1747491a99317a6239634b68d3`。
- 检查了 monorepo 结构、Ant Design Vue 应用、前端访问模式、Nitro Mock、路由和布局扩展点。
- 使用仓库要求的 Node.js 24 兼容环境和 pnpm 11 安装依赖。
- 开发前执行 `@vben/web-antd` 类型检查，原始基线通过。

### 2. 视觉与布局方向

- 建立“轨道工程控制台”语言：深石墨与冷灰为主，企业红仅用于关键操作、轨迹和状态。
- 平台概览的标志性视觉是“资产流转轨道”，表达不同能力之间共享资产的核心逻辑。
- 保留 Vben 的响应式外壳、标签页、权限和偏好设施，替换与产品无关的演示导航。

### 3. 域模型与状态

新增：

- `apps/web-antd/src/modules/platform/types.ts`
- `apps/web-antd/src/modules/platform/data.ts`
- `apps/web-antd/src/store/platform.ts`
- `apps/web-antd/src/store/platform.test.ts`

实现项目、资产、应用、任务、用户、角色和审计七类核心模型，并实现：

- 切换和创建项目。
- 登记资产与收藏资产。
- 启停用户。
- 模拟执行外部应用。
- 任务完成后生成输出资产，并把任务指向该资产。
- 对当前项目、资产和任务进行本地持久化。
- 对项目列表进行同步持久化，并在旧缓存指向不存在项目时自动回退到默认项目。

### 4. 路由与页面

用 `platform.ts` 替换原始 `dashboard.ts`、`demos.ts` 和 `vben.ts` 演示路由，实现：

- 平台概览。
- 项目空间。
- 资产中心。
- 应用中心。
- 任务中心。
- 用户与权限。
- 审计日志。
- 通用动态应用工作区。

“平台管理”路由仅向 `admin` 和 `super` 权限开放。

### 5. 品牌与框架定制

- 新增本地 `rail-logo.svg`，不依赖外部图片。
- 应用标题改为“轨道客室智能设计平台”。
- 修改 namespace，与上游演示缓存隔离。
- 登录演示账号、Mock 用户姓名、主页和中文文案均已品牌化。
- 顶栏新增项目切换，用户菜单显示当前项目。
- 通知改为项目与任务相关的本地演示内容。
- 移除了产品中的上游文档和 GitHub 跳转入口。

### 6. 兼容修复

在 macOS x64 环境执行生产构建时，`sass-embedded` 1.100.0 附带的 Dart 3.12.0 子进程在 CPU 信息检测阶段崩溃。为避免依赖特定机器的临时处理：

- Vite 全局 SCSS 导入器改为从 `sass` 引入。
- 默认依赖 catalog 将 `sass-embedded` 包名映射到 `sass@1.101.6`，Web 应用继续使用仓库统一的 `catalog:` 声明。
- 修改写入 `package.json` 和 `pnpm-lock.yaml`，新机器执行正常安装即可获得同样结果。
- 使用 pnpm 11.16.0 执行 `install --frozen-lockfile`，完整依赖重装与安装后脚本均通过。

首次尝试纯离线重装时，本机 pnpm store 缺少 `sortablejs` 包体，因此离线安装中止；随后允许访问包仓库后冻结锁安装成功。该过程说明新机器首次安装需要网络，或预先准备完整的 pnpm store，并非锁文件失效。

全仓格式检查曾发现 Vue 模板格式器与 ESLint 对内联元素空白的处理不一致。已在 `oxfmt.config.ts` 中仅对本次新增的平台组件和页面设置 `htmlWhitespaceSensitivity: 'ignore'`，自动格式化、ESLint 与渲染结构现可同时保持一致。全仓 Oxlint 进一步发现的非空断言和嵌套三元表达式也已改为显式兜底与资产格式映射。

### 7. 验证记录

| 检查 | 结果 | 备注 |
| --- | --- | --- |
| 开发前类型基线 | 通过 | 确认上游代码可工作 |
| pnpm 冻结锁安装 | 通过 | pnpm 11.16.0，46 个工作区项目 |
| 全仓库 `pnpm lint` | 通过 | 格式、Oxlint、ESLint、Stylelint |
| 二开后 Vue/TypeScript 类型检查 | 通过 | 无类型错误 |
| Pinia 状态单元测试 | 通过 | 1 个文件、4 个用例 |
| 生产构建 | 通过 | 8,054 个模块，构建约 6 秒 |
| 真实浏览器主流程 | 通过 | 登录态恢复、项目切换/创建/刷新、工作区、任务、资产回流、用户角色与审计 |

真实浏览器验收期间发现“新建项目刷新后丢失，顶栏显示原始 ID”的持久化缺陷。已通过“项目列表纳入持久化 + `afterHydrate` 旧数据自愈”修复，并在同一浏览器内重新创建项目后刷新验证通过。页面运行期无 JavaScript 错误；开发控制台仅有 Vben 上游表单兼容和存储前缀提示。

### 8. 复现原则

- 所有必要修改都在仓库内，不依赖用户主目录的隐藏配置。
- 依赖版本由 `package.json`、`pnpm-workspace.yaml` 和 `pnpm-lock.yaml` 共同锁定。
- 设计决策、目录边界、故障和验证结果均记录在 `docs/rail-platform`。
- 下一阶段接入真实 API 时，应继续追加本日志，而不要删除历史。

## 2026-08-08：仓库产品化精简与身份统一

- 删除 5 套替代 Web、旧 Nitro Mock、Playground、Vben 文档站、Changesets 和上游 GitHub 发布设施。
- 删除实际 Web 与共享包中的 Dashboard/Demos、About、验证码/二维码/第三方登录、样例安全设置、无入口错误页和专属资源。
- 删除无平台引用的 ECharts、Tiptap、VXE Table、Captcha、CountTo、JsonViewer 和替代 UI 样式，保留实际使用的表单、布局、权限、请求、状态、图片裁剪和动效。
- 活动 Web 包改为 `@rail/web` 0.1.0；根包、HTML、PWA、运行时配置变量、默认偏好、版权头、VS Code 和 GitHub 协作文件统一为平台身份。
- 根 README 改为中文项目入口，新增 `REPOSITORY_STRUCTURE.md`，记录完整 `tree` 审计口径和所有保留区域用途。
- 清理本机绝对路径、旧克隆目录名和过期“隔离观察”描述。

验证：`pnpm lint`、`pnpm typecheck:rail`、`pnpm test:rail`、`pnpm build:rail` 已通过；集成和浏览器验收在最终门禁阶段记录。

## 2026-08-08：运行时品牌残留收口与最终验收

- 修复内部 Vite 配置包仅引用历史 `dist` 产物的问题：`build:rail`、`dev:rail` 和 `dev:rail:web` 在执行前统一生成当前源码产物，避免已删除的 Vben 文案继续出现在开发终端。
- 删除内部 Vite 配置中仅服务旧文档站、Mock 和懒加载示例的依赖，锁文件同步收敛。
- 真实集成验收通过，覆盖认证、权限隔离、项目、文本/图片资产、对象存储、AI 会话和审计；本机代理对 `localhost` 的干扰通过 `NO_PROXY/no_proxy` 排除，未修改业务代码规避环境问题。
- 真实浏览器验收通过：管理员进入 `/workspace/overview`，页面只呈现轨道客室智能设计平台、项目、资产、应用、任务和审计入口，浏览器控制台无错误，未发现旧 Vben 品牌文案。

## 2026-08-08：ComfyUI 工作流 API 第二阶段

### 需求

完整实现工作流注册、管理员管理、能力映射、独立持久化 Worker 和文生图端到端；本期只正式发布一个文生图能力，但架构支持后续导入其他工作流。由于本机没有 ComfyUI，使用临时模拟服务测试，运行时不得伪造成功。

### 实现

- 新增 `010_comfyui_workflows.sql`，建立工作流版本、能力绑定、执行租约、输出回执和 Worker 心跳模型。
- 纳入已在其他电脑验证的 Flux2 Klein 文生图 API JSON，并幂等登记首发版本与 `text-to-image` 绑定。
- 新增通用参数/输出映射校验、ComfyUI HTTP 适配器、独立 Worker、取消、轮询重试、崩溃恢复和资产登记。
- 新增管理员工作流管理 API/页面与能力驱动的文生图工作区。
- 开发启动和生产 Compose 均增加独立 Worker。
- 新增详细架构说明与中文独立变更记录。

### 验证计划与环境限制

- 自动化覆盖参数映射、配置、客户端、输出映射和临时模拟 ComfyUI 协议。
- 执行仓库质量门禁、真实 PostgreSQL/MinIO 集成和浏览器手工流程。
- 本机无法验证真实 GPU 推理；真实 ComfyUI、模型和自定义节点验收保留为部署前阻断项。

### 最终验证结果

- `pnpm lint`：通过。
- `pnpm typecheck:rail`：通过。
- `pnpm test:rail`：后端 33 项、前端 9 项通过。
- `pnpm test:rail:integration`：平台基线集成与模拟 ComfyUI Worker 真实 PostgreSQL/MinIO/资产登记均通过。
- `pnpm build:rail`：Nitro 与 Web 生产构建通过。
- 浏览器：管理员管理、Worker 心跳、文生图参数、未配置服务稳定失败和移动端无横向溢出通过。
- 未验证：真实 ComfyUI GPU 推理、模型与自定义节点安装。

## 2026-08-08：ComfyUI 18 项工作流目录接入与工作区重设计

### 需求

- 将 `/Users/huyanwei/projects/ComfyUI/user/default/workflows_api` 中除首发文生图外的所有 17 份 API JSON 按同一标准接入。
- 改造输入资产、文本和 3D 输出链路。
- 使用临时模拟 ComfyUI 验证全目录协议。
- 重设计能力工作区，为图片、多图、遮罩、分区、屏幕捕获、文本和 3D 提供对应交互。
- 编写真实 ComfyUI 启动后的快速联调和适配微调交接文档。

### 实现

- 新增 `011_comfyui_workflow_catalog.sql`，发布 17 项新应用与能力，总数达到 18 项。
- 新增集中能力目录，显式记录业务参数、资产位、ComfyUI 注入目标、输出字段和模型需求。
- Worker 新增 MinIO 输入读取、`/upload/image`、Data URL 注入、PreviewAny 文本资产和 SaveGLB 3D 资产。
- 能力工作区重构为轨道设计控制台，增加项目资产编组、图像导入、屏幕捕获、PNG Alpha 遮罩和 EasyMark 分区笔画。
- 工作流校验和改为完整版本快照，参数/输出/模型微调也会创建新版本。
- 新增真实服务节点预检脚本和 `COMFYUI_LIVE_SERVICE_HANDOFF.md`。

### 验证边界

- 本机 ComfyUI 尚未启动，不将模拟协议通过记为真实 GPU 推理通过。
- 真实服务启动后必须先执行节点预检，再按交接文档的 18 项矩阵验收。

## 2026-08-08：ComfyUI 全工作流目录、通用输入适配与工作区升级

- 将外部 `workflows_api` 目录中的 18 份 API JSON 固化到平台工作流目录，其中 17 项为本次新增，原“文生图”能力继续按同一目录标准管理。
- 新增集中式工作流目录、迁移 `011_comfyui_workflow_catalog.sql`、通用标量/资产/截图/遮罩/区域参数映射和图片/文本/GLB 输出映射。
- Worker 现在可以按有序任务输入从对象存储读取资产、上传到 ComfyUI、注入不可变工作流版本并登记输出资产。
- 工作区改为按能力契约动态渲染的设计控制台，支持项目资产、文件导入、屏幕捕获、遮罩绘制和区域标注。
- 已应用迁移并完成种子初始化，数据库登记 18 项能力。
- 模拟验证结果：`14` 个测试文件、`38` 项测试全部通过；逐项覆盖全部 18 份 API JSON。
- 环境限制：本机没有启动真实 ComfyUI，节点、模型、显存、生成质量和少数自定义节点输出字段仍须按 `COMFYUI_LIVE_SERVICE_HANDOFF.md` 做上线前预检与微调。

## 2026-08-08：AI 设计助手接入 GeekAI

- 将助手外部请求从平台自定义包装体切换为 GeekAI / OpenAI Chat Completions 兼容协议。
- 默认模型改为 `deepseek-v4-flash-0731`，服务地址和 Key 仍只从 API 环境变量读取。
- 会话、消息和附件继续按 `user_id` 强制隔离，且提供商历史查询增加二次用户约束。
- 上游请求不再发送真实姓名和用户名；审计只保留字符数、附件数、结果和稳定错误码。
- 图片和视频附件按官方多模态结构传递短时签名地址；不支持直读的文档明确告知模型正文未传递。
- 本次不使用用户在对话中暴露的 Key，需轮换后再按 `AI_ASSISTANT_GEEKAI_INTEGRATION.md` 做真实服务验收。

### 2026-08-08 GeekAI 本机真实部署与验收

- AI 助手模拟测试通过：`14` 个测试文件、`41` 项测试。
- 本机 API 已读取 GeekAI 配置，页面正确显示提供商和 `deepseek-v4-flash-0731` 模型。
- 已通过平台页面完成真实文本问答，GeekAI 返回有效中文回复。
- 刷新页面后会话与双方消息仍存在，证明请求、响应和 PostgreSQL 持久化闭环通过。
- 未将真实 Key 写入任何受版本控制文件；当前 Key 已在对话中暴露，后续必须轮换。

## 2026-08-08：ComfyUI 真实服务第一批适配

- 本机 HTTPS ComfyUI 0.27.0 已接入，使用 CA 证书完成严格 TLS 校验，不关闭证书验证。
- 新增跨平台环境启动器，先加载 `apps/platform-api/.env` 再创建 Node.js 子进程，解决 `NODE_EXTRA_CA_CERTS` 在进程启动后加载不生效的问题；API、Worker、迁移、种子和预检统一使用该入口。
- 本机 `.env` 已从 Git 跟踪中移除并加入忽略规则，配置模板与部署文档新增 HTTPS CA 说明。
- 真实服务预检从“仅检查节点类型”扩展为同时检查静态枚举值，并将只存在于前端控件的附加字段作为兼容性提示输出；18 项能力全部通过阻断项预检。
- 第一批真实推理通过：文生图、单图设计编辑、文本生成、多视图生三维，输出分别登记为项目图片、文本和 GLB 资产。
- 多视图生三维在极低测试参数下产生空网格，平台正确返回 `COMFYUI_OUTPUT_MISSING`；使用工作流默认 `20 steps / 3072` 后成功登记约 24.4 MiB GLB。同步移除 `SaveGLB` 已废弃的 `image` 输入。
- 第二批 8 项真实推理通过：LoRA、多图融合、图片理解、放大修复、单视角镜头控制、多视角镜头生成、Base 双图编辑和 KV 双图编辑；多视角镜头一次登记 5 张图片。
- 第三批屏幕捕获、单图/参考图遮罩、智能扩图直接通过；两项 EasyMark 分区首次被真实服务拒绝，原因是当前 `sum_stack_flux2_Klein` 要求显式 `context`。
- 修复两项分区工作流：移除 `Anything Everywhere` 隐式广播并直接连接 `sum_load_adv` 上下文；编号分区的 `SaveImage` 改为保存采样器生成图而非缩放原图；移除旧前端预览节点和过期尺寸字段。
- 最终结果：18/18 项能力均完成真实 GPU 推理和项目资产回写。验证覆盖单/多资产上传、Data URL、Alpha 遮罩、EasyMark 笔画、批量图片、内联文本、GLB、长队列轮询和 Worker 租约。
- 完整门禁通过：`pnpm lint`、`pnpm typecheck:rail`、42 项 Platform API 与 9 项 Web 单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 集成验收和 `pnpm build:rail`。

## 2026-08-08：应用中心筛选、可见性管理与卡片修复

- 修复应用卡片短名称水印与状态徽标重叠：水印移到卡片头部下方并限制宽度，状态与隐藏徽标保持独立层级。
- 应用中心增加名称/描述/服务方搜索、应用状态筛选，并保留类别筛选；管理员额外获得用户可见性筛选。
- 新增 `012_application_visibility.sql`：`applications.visible` 默认启用，新增仅管理员拥有的 `platform:application:write` 权限。
- 新增管理员应用可见性 API 和审计事件 `application.visibility.update`。普通用户的应用列表不返回隐藏应用，并且直接请求隐藏能力详情或创建任务同样返回 404；管理员仍能查看、筛选和进入隐藏应用。
- 前端管理员可在每张卡片持久化切换“用户可见”，隐藏卡片使用独立视觉状态；筛选无结果时显示明确空状态。
- 验证：API/Web 类型检查通过；Platform API 42 项、Web 10 项单元测试通过；真实 PostgreSQL/MinIO 集成验收覆盖管理员隐藏/恢复、普通用户列表隔离、写权限拒绝、能力详情和任务创建防绕过。
- 浏览器：1000×1000 管理员页面共 24 张卡片，状态徽标与短名称水印边界交叠为 0；名称搜索、状态筛选、可见性开关及恢复通过，控制台无错误。

## 2026-08-08：工作流可视化交互、结果暂存与跨能力流转

- 保持 `apps/platform-api/workflows/comfyui/` 内 18 份原始 API JSON 不变，只调整平台参数渲染、输入组件和结果生命周期。
- 工作区默认展示基础业务参数并收纳高级参数，补充参数总数、说明、数值范围、默认值和高级下拉项。
- 图片输入明确拆分为“从项目资产选择”和“本地导入”；新增资产缩略图选择器和全屏缩放预览。
- 遮罩与分区重绘升级为全屏 Canvas 编辑器，支持画笔、橡皮、颜色、粗细、撤销、重做、清除和缩放；实时捕捉支持屏幕/摄像头预览与可视化选区裁剪。
- 新增 `013_workflow_output_staging.sql`。Worker 输出默认 `saved_at = NULL`，仅在任务结果区可见；用户点击保存后才进入资产中心。
- 任务接口返回项目范围内的全部暂存输出；结果可下载、放大或手动加入资产。未加入资产时后端不允许当作其他工作流输入。
- 资产中心新增后端强制项目写权限的软删除接口、对象清理和 `asset.delete` 审计；历史任务外键与血缘记录继续保留。
- 自动化验收覆盖暂存结果不进入资产列表、任务接口可读取、确认保存后进入列表、删除后不可访问，以及 Worker 输出保持暂存状态。

## 2026-08-10：Linux 本地开发环境管理脚本

- 根目录新增 `rail-platform.sh`，统一提供 `check_env`、`start`、`status`、`stop`、`restart`、`logs` 和 `help`。
- 新增 `.nvmrc` 固定 Node.js `24.16.0`；脚本自动加载 NVM，并校验项目允许的 Node 版本和锁定的 pnpm `11.16.0`。
- `start` 在后台复用 `pnpm dev:rail`，保留原有 Compose、迁移、种子、API、Worker 和 Web 启动链路；PID 和日志写入 Git 忽略的 `.rail-platform-runtime/`。
- 每次 `start/restart` 生成带年月日时分秒的独立日志 `dev-YYYYMMDD-HHMMSS.log`，文件头记录完整启动时间和时区；稳定入口 `dev.log` 始终指向本次日志，历史日志不再被覆盖。
- `status` 同时检查受管进程、Compose 服务、Web/API HTTP 健康以及 MinIO/Mailpit 端口。
- `stop` 使用进程组停止 Node 开发服务并执行不带 `-v` 的 Compose down，保留 PostgreSQL 和 MinIO 数据；`--keep-infra` 可只停止 Node 进程。

## 2026-08-09：工作区状态、完整参数、资产预览与精确流转

- 执行链路横线默认保持静态，仅当当前应用存在 `queued`、`running` 或 `cancelling` 任务时显示运行动效，减少错误的“正在工作”感知。
- 动态应用页签使用应用/能力名称；重新进入应用时不恢复旧的已完成结果，但仍恢复后台正在运行的任务。
- 新增参数同步器与 `parameter-schemas.generated.json`，根据真实 ComfyUI `/object_info` 为 18 份 API JSON 补齐每个非连线输入；完整性测试强制未映射数为 0。
- `ScreenShare` 实现与参考 ComfyUI webUI 一致的屏幕/摄像头、拖拽选区、裁剪实时预览、`refresh_rate`、画面差异检测和串行实时任务；停止时关闭媒体轨道并取消本页活跃任务。
- 资产预览 API 按项目权限返回内联文本或图片/视频/音频/PDF 短时地址；资产中心支持图片放大、文本阅读与媒体播放。
- 新增 `014_workflow_asset_transfers.sql`。流转记录按用户、项目、目标工作流和输入位持久化；多输入工作流中用户必须明确选择“基础空间”、“样式参考”或“材质参考”等语义输入，刷新后仍回填到同一位置，任务创建后标记为已消费。
- 后端同时强制“只有已登记项目资产才能流转”；暂存结果会返回 `WORKFLOW_OUTPUT_NOT_SAVED`，前端只在用户明确确认“加入资产并流转”后连续完成两步。
- 最终门禁通过：`pnpm lint`、API/Web 类型检查、Platform API 48 项测试、Web 12 项状态测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、真实 Chromium 页面验收和 `pnpm build:rail`。浏览器验收同时使用真实 ComfyUI 配置创建并停止一次 ScreenShare 实时任务；临时账号、项目、对象和流转记录均完成清理。

## 2026-08-09：工作区草稿、专用控制器与 Worker 协议同步

- 高级参数首次进入时默认折叠；基础参数、专用控制器和高级参数仍共同使用同一份公开参数 Schema 与任务提交契约。
- 新增 `015_workflow_workspace_drafts.sql` 和 `/workflow-drafts` 接口。当前用户在指定项目和应用中的参数、图片输入位及 EasyMark 数据持久化到 PostgreSQL；再次点击应用标签、切换回来或刷新页面后恢复，已完成任务结果仍不自动恢复到工作区。
- 草稿接口校验用户项目范围、应用可见性、公开参数键、精确输入位、资产登记状态和类型兼容性；已删除、跨项目、暂存或重复占位的资产不会恢复。
- Qwen 单视角和五镜头工作流增加轨道式镜头可视化控制器，水平角、俯仰角和镜头距离仍写入各自真实 ComfyUI 节点。
- EasyMark 分区编辑与参考 webUI 协议对齐，支持自由画笔、方框、色块、橡皮、六种颜色、透明度和 1–6 编号，序列化结果直接写入 `brush_data`。
- ScreenShare 输入改为工作区内直接展示“共享屏幕、摄像头、Set Area、Live On”，保留选区裁剪、变化检测、串行提交和停止清理。
- 修复开发环境 API 热更新但 Worker 不重载的问题：`dev:rail` 现在使用 `tsx watch` 启动 Worker；工作流参数协议变化会自动重启 Worker。即使出现版本不一致，也只返回稳定的中文错误，不再把 Zod 联合类型明细显示给用户。
- 验证覆盖参数 Schema、草稿纯函数、真实 PostgreSQL/MinIO 草稿读写与隔离、模拟 ComfyUI Worker，以及 Chromium 中高级参数折叠、直接实时捕获、角度草稿恢复、图片输入恢复和 EasyMark 编号序列化。
- 最终门禁通过：`pnpm lint`、API/Web 类型检查、Platform API 55 项测试、Web 12 项状态测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、真实 Chromium 页面验收和 `pnpm build:rail`。浏览器验收产出了实时捕获、镜头控制、分区打标和草稿恢复四组截图，运行期没有 JavaScript 页面错误。

## 2026-08-10：工作流结果自适应与统一 ComfyUI 遮罩编辑器

- 保持 `apps/platform-api/workflows/comfyui/` 内 18 份原始 API JSON 完全不变，仅调整所有能力共用的前端工作区和浏览器验收。
- 图片结果区移除固定深色画布和最小高度，按图片实际宽高在可用区域内自适应完整显示；右下角不再显示“点击放大”，统一使用 ComfyUI 官方 `comfy--mask` 图标进入全屏遮罩编辑器。
- 新增共享遮罩编辑器，按 ComfyUI webUI 结构提供顶部撤销/重做、旋转、翻转、反转、清除、保存/取消，左侧遮罩/绘画/橡皮/填充/吸管工具，中央缩放画布，以及右侧笔刷和图层设置。
- 所有 `mask` 类型工作流输入与图片结果复用同一组件；输入遮罩保存后作为新的输入资产回填，结果遮罩保存后直接登记为当前用户、当前项目的图片资产，并保留“遮罩”用途标签。
- 高级参数展开后使用最高 `520px`、同时受视口高度约束的内部滚动区，不再继续拉长控制栏和中央结果画布。
- 浏览器验收新增：单图局部重绘遮罩入口共用性、文生图高级参数内部滚动、结果图片容器无额外填充、遮罩绘制保存和数据库资产登记。使用系统 Chrome 的真实页面、PostgreSQL 和 MinIO 验收通过，测试临时用户、项目、对象和资产均已清理。
- 验证通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 55 项与 Web 12 项单元/状态测试、`RAIL_BROWSER_EXECUTABLE=/usr/bin/google-chrome pnpm test:rail:browser` 和 `pnpm build:rail`。

### ComfyUI 官方遮罩功能逐项对齐

- 以本机 ComfyUI 0.27.0 前端包 Source Map 中的 `maskeditor` 实际源码为事实来源，替换第一版简化笔画模型；工具顺序、官方 SVG、顶部操作、左右面板尺寸和深色主题与当前 ComfyUI 遮罩编辑器保持一致。
- 画布改为基础图像、绘画、遮罩三个真实像素层，历史同时保存三层并支持最多 20 步撤销/重做；旋转、镜像、反转和清除均操作实际像素，变换后仍可继续编辑和保存。
- 补齐遮罩画笔、绘画画笔、橡皮、油漆桶和颜色选取五种工具。笔刷支持圆/方形、半径、透明度、硬度、间距、右键临时擦除、Shift 直线和 Alt+右键拖动调整大小/硬度。
- 油漆桶按遮罩 Alpha 连通域提供阈值和填充不透明度；颜色选取提供 Simple/HSL/LAB、连续区域/整图、实时预览、选取透明度、遮罩边界和遮罩阈值，算法参数与官方实现一致。
- 画布支持以指针为中心的滚轮缩放、空格+左键或中键平移、触控单指平移和双指缩放；左下角显示实际缩放比例与图片尺寸并可一键恢复适应窗口。
- 图层面板支持显隐、遮罩透明度、黑/白/负片混合，以及橡皮工具在遮罩层和绘画层之间切换；保存按官方 `paintedMaskedImage` 语义合成 RGB 绘画层并将遮罩 Alpha 反向写入最终 PNG，再登记为当前项目资产。
- 新增遮罩像素算法单测和浏览器验收：覆盖连通填充、颜色范围、反转/旋转/镜像、五工具设置面板、三图层切换、绘制与资产保存。原始 18 份 ComfyUI API JSON 未修改。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 55 项与 Web 15 项测试、`RAIL_BROWSER_EXECUTABLE=/usr/bin/google-chrome pnpm test:rail:browser` 和 `pnpm build:rail`；浏览器验收覆盖官方式指针画布、工具切换、撤销和最终资产登记。

## 2026-08-10：应用单实例、多轮工作区与图片差异对比

- 修复同一应用因 `transferId` 等查询参数产生重复标签的问题：应用工作区标签键固定使用应用路径，并在进入时清理会话中遗留的同路径旧标签；不同应用仍保持各自独立标签。
- 后端创建任务时按当前用户、项目和应用获取 PostgreSQL 事务级 advisory lock，并检查 `queued`、`running`、`cancelling` 状态；并发提交稳定返回 `APPLICATION_JOB_ACTIVE`，前端按钮隐藏不再是唯一保护。
- 任务查询补充 `createdBy`、`ownedByCurrentUser`、`parameters`、有序 `inputs` 和带位置的 `outputs`。工作区只将当前用户在当前项目、当前应用的任务组成会话，按时间展示全部轮次，切换标签、任务完成和刷新后均不清空。
- 每一轮以“输入气泡 + 可展开完整输入 + 执行结果”的形式展示；完整输入包含业务字段名称、提交时的参数值、输入资产顺序、名称和图片预览。历史结果继续支持多结果切换、下载、遮罩编辑、加入资产、精确流转，以及将该轮参数和资产复制回当前草稿后再次运行。
- 从已发布工作流版本的 `api_json` 中只读识别 `ImageCompare` 与 `Image Comparer (rgthree)` 节点；对应应用使用第一张业务图片输入和当前图片输出展示左右拖动对比，并可切回单独结果视图。18 份原始 ComfyUI API JSON 未修改。
- 修复遮罩编辑器在历史轮次中按需首次挂载时未初始化的问题：监听增加立即初始化，任意轮次第一次打开遮罩都能正确读取画布。
- 自动化证据：新增图片对比节点识别单测和工作区轮次筛选单测；真实 PostgreSQL/MinIO 集成验收覆盖并发拒绝、参数与有序输入快照；Chrome 验收覆盖两个历史轮次、完成后恢复、标签去重、输入展开、对比滑块、遮罩、入资产与流转。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 57 项与 Web 16 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`；验收临时用户、项目、任务、资产与对象均按本轮唯一标识清理。

## 2026-08-11：同应用多会话实例、实例级流转与字号层级

- 修正上一轮对产品层级的误解：应用是功能模板，应用实例是用户打开的一次持久化会话，一个应用允许同一用户在同一项目中创建多个实例；每个实例对应独立标签、草稿和多轮任务历史。
- 新增迁移 `016_workflow_workspace_instances.sql` 和 `/workflow-instances` 接口。旧任务、草稿和流转记录按用户、项目、应用归入兼容的“历史会话”，升级不删除历史数据。
- `jobs.workspace_instance_id` 成为任务会话边界；事务级 advisory lock 和活动任务查询均按实例执行。同一实例的第二个活动任务返回 `WORKSPACE_INSTANCE_JOB_ACTIVE`，同一应用的另一个实例可并行运行。
- 资产流转改为“目标应用 → 已有会话/新建会话 → 具体输入位”。待流转唯一约束和查询都按目标实例执行，页面不再用含糊的“已有待流转资产”表示整个应用被占用；仅当所选实例的具体输入位已有待接收资产时显示“该会话输入位已占用”。
- 应用中心和概览的应用入口每次创建新会话；任务中心可直接返回任务所属实例；路由以 `instanceId` 区分同一应用的多个标签，并只清理同一实例的旧查询参数标签。
- 工作区建立 12px 辅助信息、13px 字段标签、14px 正文、17px 面板标题的统一字号层级，参数、资产选择、专用输入组件和多轮任务卡片不再混用 8–11px 微小文字。
- 测试覆盖实例内互斥、同应用跨实例并行、草稿隔离、目标实例流转、同应用两个独立标签、历史轮次隔离、已有目标实例接收和浏览器计算字号；原始 18 份 ComfyUI API JSON 未修改。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 57 项与 Web 16 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`。浏览器验收覆盖同应用多实例标签、实例历史保留、目标实例流转、统一字号计算值及既有遮罩、镜头、分区和图像对比能力。

## 2026-08-11：遮罩工作流图像对比原图血缘修正

- 修复遮罩保存后任务只记录带 Alpha 的遮罩 PNG，导致对比组件错误将遮罩输入当作“原始输入”的问题。遮罩资产现在在 `asset_versions.metadata.derivedFromAssetId` 中登记遮罩编辑前的项目图片；重复编辑遮罩时继续保留最初原图，不形成遮罩链误判。
- 任务输入快照返回遮罩资产的原图血缘。带图片对比节点的遮罩工作流左侧固定读取本轮遮罩前原图，右侧读取当前选中的本轮生成结果，并显示“遮罩前原图 / 遮罩生成结果”；普通图片编辑工作流仍使用本轮输入图和生成结果。
- 对修复前已经完成且没有原图血缘的历史遮罩任务，不再拿遮罩 PNG 冒充原图，而是隐藏错误对比并提示“历史任务未记录遮罩前原图，无法准确对比”。原始 18 份 ComfyUI API JSON 未修改。
- 验证通过：`pnpm typecheck:rail`、Platform API 57 项与 Web 16 项测试、真实 PostgreSQL/MinIO 集成验收，以及系统 Chrome 浏览器验收。浏览器直接断言左图预签名 URL 属于原始底图且不属于遮罩资产，右图属于本轮生成结果。

## 2026-08-11：智能扩图红色扩展区适配修正生效

- 根据用户截图核查最近智能扩图任务：异常结果使用工作流 V3，V3 的持久化参数 Schema 未包含原始 API 工作流节点 367 所要求的“删除红色部分”协议；节点 368 `ImagePadForOutpaint` 产生的红色扩展标记因此被模型当作结果内容保留。最终输出映射仍为节点 `366.images`，不是误取预览或遮罩节点。
- 保持原始 `outpaint-v1.json` 不变，通过后端私有参数 `valuePrefix` 为用户设计描述自动添加“删除红色扩展标记区域、参考原图自然延展、不得保留纯色边框或遮罩痕迹”。该固定协议不会暴露为可编辑表单字段，任务台账仍只保存用户输入。
- 重新执行幂等工作流目录同步后，本地数据库生成并激活智能扩图 V4；只读核验确认节点 367 的实际物化提示词为固定协议与用户描述的拼接。旧 V3 任务及其结果不改写，用户点击“复用本轮再运行”会按当前活动 V4 创建新任务。
- 回归覆盖目录物化测试，断言节点 367 同时包含固定去红色协议和用户扩图描述；原始 18 份 ComfyUI API JSON 未修改。

## 2026-08-11：项目级统一设计会话与多应用时间线

- 将普通用户的主要入口从“逐个应用创建独立工作区实例”调整为项目内“开始设计”：左侧管理历史会话，中间按时间显示多轮、多应用结果，底部统一选择全部可见应用并配置本轮参数。未显式选择应用时默认由“文生文”执行，后续仍可替换为其他文本 API 适配器。
- 新增迁移 `017_project_design_conversations.sql` 和 `/design-conversations` 接口。设计会话按用户和项目隔离，支持创建、重命名、列表和软删除；归档只从历史栏隐藏，不删除项目资产、任务台账和审计记录。
- 旧应用工作区实例按原 UUID 幂等迁移为设计会话，旧任务和草稿完成关联回填。`workflow_workspace_instances`、`/workspace/:appKey` 和应用中心继续保留，仅供管理员调试单一能力，避免破坏已有任务历史。
- 任务执行边界改为设计会话：同一会话通过 PostgreSQL 事务级 advisory lock 和活动任务检查只允许一个生成任务；同一用户的不同会话可以并行。任务查询返回设计会话归属，时间线能够在同一会话中混合展示不同应用的历史轮次。
- 新增按“设计会话 + 应用”持久化的参数草稿。底部先展示应用名称和少量高频参数，其余公开参数进入抽屉；图片、资产、遮罩、镜头、分区、实时捕获等既有专用输入继续复用原组件和真实工作流参数契约。
- “流转到工作流”改为本会话内的“继续设计”：选择目标应用和兼容输入位后直接切换当前应用并回填资产，不再选择目标应用的第几次会话。暂存结果仍必须由用户明确加入项目资产后才能继续设计，保持资产作为能力交换协议的业务规则。
- 应用中心更名为“应用调试中心”并限制管理员访问；项目列表、平台概览和应用快捷入口统一进入 `/design`。开始设计页面隐藏右下角全局 AI 助手，其他页面暂时保留。
- 原始 18 份 ComfyUI API JSON 未修改。数据库迁移后只读核验：40 个旧工作区会话完成迁移，78 个历史任务全部具备设计会话范围，26 份旧草稿完成回填。
- 完整门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 59 项与 Web 17 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`。浏览器验收覆盖左侧会话、三轮混合应用历史、18 项能力选择、旧管理员调试工作区以及开始设计页不重复显示全局 AI 助手。

## 2026-08-11：开始设计沉浸式工作台与两态统一输入器

- 根据最终交互截图重构 `/design`：页面不再嵌套后台深色导航、顶部页签和第二层侧栏，只保留项目选择、历史会话、对话时间线和固定底部输入器。旧应用实例迁移记录通过接口标记为 `legacy`，不再挤占普通用户历史栏，仍可在任务中心追溯。
- 统一输入器严格区分两种状态：未选应用时显示“添加、常用应用、更多、发送”，业务执行仍默认使用 `text-chat`；选定应用后隐藏其他应用，显示可取消的应用标签、最多四项主要参数、输入素材、高级参数和发送。完整公开参数及图片、遮罩、区域、镜头和实时捕获等专用输入继续进入同一参数抽屉。
- “继续设计”保持在当前会话内选择目标应用和兼容输入位，结果经用户确认登记为项目资产后直接回填；历史轮次仍保留下载、加入资产、遮罩、图像对比和复用运行能力。
- 浏览器验收新增 1600×1000 全屏边界、普通/旧版历史分离、未选应用默认文生文但不显示已选标签、常用应用与“更多”、选定后隐藏其他应用、可取消应用及未选/已选双态截图断言。原始 18 份 ComfyUI API JSON 未修改。

## 2026-08-11：统一输入器快捷参数、素材消息与生成停止

- 将底部输入器正文与应用入口字号放大，边框、应用标签、交互焦点和发送按钮统一使用平台红色系，不再使用外部 AI 产品的蓝色视觉。
- 新增通用快捷参数弹层：下拉选项、比例、数值、随机种子、开关和短文本可在底部原位修改；只有“更多”打开 18 项能力各自的完整参数面板。
- “+”与“素材”只打开上传/项目资产选择窗口。已选图片在发送前显示缩略图、输入位和名称；发送后按用户消息形式显示独立大图与文本气泡，完整输入详情仍可展开。非图片素材保留类型和名称卡片。
- 任务运行时，发送按钮切换为灰底方形停止标记；点击调用现有取消 API，完成后恢复发送状态。后端的同会话单活动任务约束保持不变。
- 扩展真实 Chrome 验收，对红色边框/按钮计算样式、最小字号、快捷弹层、完整面板、发送前图片、发送后图片、停止交互和 18 项应用数量进行断言，并产出对应截图。本轮未修改数据库、API 契约或 18 份原始 ComfyUI API JSON。
- 最终验证通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 59 项与 Web 17 项单元/状态测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`。浏览器验收点击了真实停止接口并验证输入器恢复为可发送状态。

## 2026-08-11：历史输入悬浮操作、Markdown 结果与轻量工具栏

- 移除每轮用户输入下方常驻的“查看本轮完整输入”长条。复制输入和查看参数改为在输入消息悬浮或键盘聚焦时出现的图标；完整资产与参数进入独立、可滚动弹窗，移动端保持入口常显。
- ComfyUI Worker 将新生成的文本结果统一归一化为 `.md` 文件名和 `text/markdown` MIME，包含 ComfyUI 返回的已有文本文件与内联文本节点；原始内容不重写，18 份原始 API JSON 不修改。
- 新增安全的本地 Markdown 阅读组件，以 Vue 文本节点渲染标题、段落、列表、引用、代码块和可折叠思考过程，不使用未净化 HTML。文本结果增加复制图标，复制的是完整 Markdown 原文。
- 下载/查看、加入资产、继续设计和复用运行统一改为有可访问名称的图标按钮，鼠标悬浮显示具体功能；共享任务卡片同时覆盖普通用户“开始设计”和管理员单能力调试页面，原有下载、资产、流转、遮罩、对比与复用事件保持不变。
- 单元测试覆盖内联文本和外部 `.txt` 输出的 Markdown 元数据归一化；真实 Chrome 验收覆盖输入操作默认隐藏、悬浮出现、参数弹窗、Markdown 标题与列表、复制原文、五项图标操作和 Tooltip，并生成输入悬浮、Markdown 结果两张专项截图。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 60 项与 Web 17 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`；Git 路径审计确认 18 份原始 ComfyUI API JSON 变更为 0。

## 2026-08-11：设计会话输入闭环、自动命名与轻量运行反馈

- 修复正在运行会话的编辑、删除图标换行问题：活动状态与操作区固定同行显示，长标题只在标题区域截断。成功创建任务后立即清空本轮文本、图片和输入位素材，保留当前应用及其他参数；创建失败时保留输入，便于修正后重试。
- 任务运行卡移除大面积技术进度框、`ComfyUI 正在执行工作流` 和 Prompt ID，只显示三点“生成中”轻量反馈；底部发送按钮同步切换为停止方块。文本输出取消额外左缩进，时间线与底部输入器共用同一内容宽度。
- 历史轮次的图片输入增加遮罩图标与点击编辑入口，复用完整 ComfyUI 风格遮罩编辑器。保存编辑结果时登记为当前项目资产并保留原图血缘，暂存输出尚未登记前不伪造派生资产关系。
- 统一输入器可从当前项目资产选择 `.md` / `text/markdown` 文本，读取短时授权下载地址后只提取可读 Markdown 文本，过滤图片、HTML 图片标签和链接目标；图片仍以缩略图和文本气泡形式在发送前后直接显示。
- 参数较多时不再截断为四项：快捷参数区横向滚动，“更多”固定在输入器右侧且始终可见。会话列表操作保持同行，输入器与历史内容使用统一宽度和视觉层级。
- 新增迁移 `018_design_conversation_titles.sql`。仍为默认名称且从未手工改名的会话，在首次有效任务创建事务中按业务文本自动生成最多 20 个字符的标题；手工改名通过 `title_manually_edited` 标记保护，后续运行不得覆盖。没有有效文本时保留“新设计会话”。
- 单元测试覆盖标题提取、截断及 Markdown 文本清理；真实集成验收覆盖自动命名和手工名称保护；系统 Chrome 验收逐项覆盖会话操作同行、输入清空、三点生成状态、输入图片遮罩、左右间距、Markdown 资产输入、输入器等宽、参数可见性与自动命名，并生成专项截图。
- 原始 18 份 ComfyUI API JSON 未修改。最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 19 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器九项交互验收和 `pnpm build:rail`。生成中专项截图可直接看到三点状态、停止按钮及对齐后的统一输入器。

## 2026-08-11：三维结果可视化、资产改名排序与会话末轮定位

- 新增共享 Three.js 三维查看器，设计会话结果和资产详情均可直接解析 GLB/GLTF、OBJ、FBX、STL、PLY；提供与 ComfyUI 查看语义一致的场景、模型、摄影机、灯光、控制、导出和全屏工具，并支持轨道旋转、缩放、平移、网格、线框、自动旋转和原文件下载。加载失败时显示明确错误，不再用静态“3D 模型”占位伪装为可视化成功。
- 资产详情增加名称编辑，后端 `PATCH /assets/:id` 校验当前用户写权限和项目范围，并记录 `asset.rename` 审计事件。资产列表新增创建时间、名称和文件类型的升降序选择，排序在服务端使用白名单字段执行，不依赖前端当前页重排。
- 资产预览接口允许经过项目权限校验的 `model3d` 对象生成短时读取地址；设计任务结果按资产 MIME 与文件扩展名选择加载器，同一共享组件也用于资产详情，避免两套查看行为不一致。
- 进入或切换设计会话时自动定位到最新一轮，并在异步图片、Markdown 或三维查看器完成布局后进行一次位置修正；不同会话各自保留持久化历史。任务运行卡仅保留动态三点，移除“生成中”文字，停止按钮和任务状态台账保持不变。
- 真实 PostgreSQL/MinIO 集成验收覆盖改名持久化、跨用户拒绝、六种服务端排序组合和三维文件预览；系统 Chrome 验收上传并解析真实 GLB，断言 WebGL 画布、模型网格/顶点、六项工具、资产详情改名、排序请求、会话末轮定位和纯三点运行反馈，并逐图核对三维查看器已占满结果区域。
- 本轮无数据库迁移，18 份原始 ComfyUI API JSON 变更为 0。最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 20 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`。

## 2026-08-12：全局图标离线渲染修复

- 定位到结果操作按钮、导航、会话和其他页面的空白图标并非 CSS 遮挡：`@iconify/vue` 收到字符串图标名后尝试从 Iconify 公网 API 动态下载，服务器或用户网络无法访问时只留下按钮和 Tooltip 空壳。
- 将完整 Lucide 图标集作为 Web 运行依赖，在应用挂载前注册到 Iconify 本地存储；导航、会话侧栏、输入器、应用参数、资产中心、结果工具栏和三维查看器不再依赖浏览器访问外部图标服务。
- 将少量 MDI、Fluent、Element Plus、Ant Design 在线前缀替换为语义一致的 Lucide 图标；修正 `scan-image`、`box-off`、`badge-sparkles` 三个 Lucide 集合中不存在的名称，并为数据库中可能保留的旧名称注册本地兼容别名。
- Chrome 验收主动阻断 `api.iconify.design`、`api.simplesvg.com` 和 `api.unisvg.com`，断言新建会话、搜索、历史会话、输入器以及结果区五个操作按钮均包含可见 SVG 路径，且整轮验收没有任何图标公网请求。
- 本轮无数据库迁移，18 份原始 ComfyUI API JSON 变更为 0。门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 20 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、离线图标 Chrome 浏览器验收和 `pnpm build:rail`。

## 2026-08-12：分区编号、输入标记快照与输出对比闭环

- 修复 Ant Design 弹窗 Teleport 后无法继承工作区主题变量，导致当前选中的 1–6 分区编号白字落在透明背景上、视觉上消失的问题；选中编号现在使用稳定的平台红色回退，六个编号始终保留，活动项只改变高亮状态。
- 新增迁移 `019_job_input_annotations.sql`，为 `job_inputs` 增加可选 `annotation_asset_id`。`asset_id` 继续保存并提供给 ComfyUI Worker 的标记前原图，标记图只作为同项目图片资产快照关联；创建任务时校验输入位置、图片类型、项目范围、保存状态和 `derivedFromAssetId` 血缘，避免错配其他原图。
- 用户提交带分区参数的任务前，浏览器按原始尺寸合成原图与 EasyMark 笔画并登记标记图；普通用户开始设计与管理员调试工作区复用同一逻辑。历史轮次并列显示“标记前原图”和“分区标记图”，标记图支持弹层放大，完整输入弹窗也保留两份输入说明。
- 分区工作流输出固定启用“标记前原图 / 分区生成结果”左右滑动对比，即使能力元数据仍在异步加载，也可通过任务输入的标记快照恢复正确语义；原有遮罩血缘对比继续使用遮罩编辑前原图。18 份原始 ComfyUI API JSON 未修改。
- 验证覆盖 EasyMark 序列化单测、真实 PostgreSQL/MinIO 中原图与标记图关联的创建及刷新恢复、浏览器六个编号可见性与颜色对比、输入双图、标记图放大、分区输出前后图来源、既有遮罩对比和 18 项工作流页面回归。迁移在已有数据库上成功升级。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 22 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器全量验收和 `pnpm build:rail`。

## 2026-08-12：历史轮次修改重发、资产文件管理与单层项目空间

- 历史轮次输入悬浮区增加“修改并重新发送”入口。用户只编辑该轮业务文本，平台从任务台账恢复原应用、全部参数、有序输入资产及遮罩/分区数据，并创建新的任务轮次；原任务参数和结果保持不可变。重发前重新检查所有历史资产仍属于当前项目且可用，失效资产明确阻断。
- 新增 `020_asset_folders.sql`：资产文件夹、父子关系及 `assets.folder_id` 全部持久化到 PostgreSQL。资产中心支持面包屑、多级文件夹、重命名、网格/列表和多选；批量移动只修改目录归属，批量复制为每项创建独立资产版本和对象键，数据库失败时补偿删除新对象；批量删除与非空文件夹递归删除均采用软删除并保留任务血缘和审计。
- 新增 `021_project_user_pins.sql`：项目置顶是当前用户个人偏好。原“平台概览”与“项目空间”合并到 `/projects`，旧地址只重定向；页面纵向展示全部可访问项目及资产、任务、成员统计，支持搜索、创建、负责人/管理员改名、置顶以及进入资产或开始设计。项目空间页隐藏重复的当前项目下拉框，其他项目页面继续保留。
- 权限与范围验证覆盖文件夹、目标目录、批量资产和项目负责人；对象资产复制使用 S3 服务端复制，复制件删除不影响源对象。审计新增文件夹创建/改名/删除、资产批量操作、项目改名和项目置顶事件，不记录文件正文或任务输入正文。
- 自动化覆盖 Pinia 文件夹与批量状态、项目改名/置顶，真实 PostgreSQL/MinIO 中多级目录、独立对象复制、移动、递归软删除、跨用户拒绝，以及系统 Chrome 中双项目展示、搜索/改名/置顶、历史遮罩任务修改重发、原轮不可变、两级目录、列表视图、复制和移动。18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：项目成员、统一业务 ID 与成员归属管理

- 新增 `022_public_ids_project_members_and_job_archival.sql`。内部外键继续使用 UUID；用户、资产和任务分别获得唯一 `USR-*`、`AST-*`、`TSK-*` 业务 ID，项目沿用 `CR-*`。迁移在已有数据上回填、建立唯一索引并为新记录配置序列默认值。用户 ID 同步显示在个人中心、右上角用户信息和管理员用户列表。
- 项目卡片支持同时修改名称和说明；项目资产、累计任务、运行任务和项目成员四项统计均可点击。前三项先切换项目上下文再进入对应筛选页，成员项展示角色、用户 ID、部门和个人资产/任务贡献。只有 `projects.owner_id` 对应的项目创建者可通过启用用户的唯一 ID 邀请编辑或只读成员，管理员身份不会绕过该邀请规则。
- 资产继续以 `owner_id` 绑定具体项目成员，并在网格/列表/详情显示资产 ID 与成员 ID；查询增加成员筛选和创建人排序，既有搜索、类型/时间/名称排序、文件夹、网格/列表与批量移动/复制/软删除保持不变。来源任务展示 `TSK-*`，不向用户暴露内部任务 UUID。
- 任务继续以 `created_by` 绑定具体成员；任务中心增加业务 ID、成员/状态/关键词筛选、创建时间/名称/创建人/状态排序、多选和批量删除。删除写入 `archived_at/archived_by`，只从任务中心隐藏，任务输入输出、项目资产和审计记录保持可追溯；运行中或正在取消的任务必须先取消，不能归档。
- 产品侧栏、路由和页面代码均移除“应用调试中心”，`/applications` 不再是可访问页面；应用目录、适配器配置和历史任务数据作为底层执行能力继续保留。
- 验证通过 `pnpm lint`、`pnpm typecheck:rail`、Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收和系统 Chrome 全量浏览器验收。浏览器额外核对项目名称/说明编辑、成员面板、统计导航、资产成员筛选、任务成员筛选/排序、业务 ID 及调试中心菜单消失。18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：项目软删除与列表可读性收尾

- 新增 `023_project_archival_actor.sql` 和 `DELETE /api/v1/projects/:id`。普通用户仅能删除自己创建的项目，管理员可以删除任意项目；存在排队、运行或取消中任务时明确拒绝。删除采用软归档并记录操作者，同时清除项目置顶和失效的当前项目偏好，资产、任务、设计会话、成员关系和审计记录均不物理删除。
- 项目卡片把删除按钮放在编辑按钮之后，移除底部与统计入口重复的“查看资产”。长项目名称和说明允许安全换行，不覆盖右上角操作区。
- 资产中心与任务中心的批量操作栏均增加明确“取消选择”。失败任务列表显示可读摘要并限制在列宽内，完整原始错误改由可滚动详情弹窗承载。
- 管理员用户列表逐项标明姓名、用户 ID、用户名和邮箱；角色卡片的“系统角色”、数据范围及类似长文本增加收缩、截断和换行约束，避免越过卡片边界。18 份原始 ComfyUI API JSON 未修改。
- 验证通过 `pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。浏览器额外验证项目软删除确认/隐藏、两处取消选择、失败日志行宽与详情弹窗、用户字段名称和角色卡片边界，控制台脚本错误为 0。

## 2026-08-12：项目编辑弹窗与管理员邀请权限修正

- 项目说明字符计数改为弹窗正文中的独立布局元素，不再由文本域的浮动计数占用底部按钮区域；浏览器验收直接比较计数和 footer 的几何边界，阻断再次遮挡。
- 项目成员邀请权限由“仅创建者”调整为“项目创建者或平台管理员”。成员列表接口和邀请写接口使用同一角色判断；管理员无需先成为项目成员，也可在他人创建的未归档项目中邀请启用用户。普通编辑成员和只读成员仍不能邀请，自我邀请仍被拒绝。
- 真实集成验收新增第四个临时普通账号，覆盖非项目创建者管理员读取 `canInvite`、成功邀请、被邀请账号获得项目访问权，以及普通成员邀请被拒绝的完整边界。
- “开始设计”侧栏取消显示旧应用记录数量及“已移至任务中心”提示；旧任务、迁移关联和任务中心回溯数据保持不变。
- 最终验证通过 `pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 四账号集成验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。

## 2026-08-12：项目列表排序与卡片视觉简化

- 项目列表新增与资产中心一致的排序选择，支持更新时间、创建时间和名称正反序。`GET /projects` 使用 Zod 白名单校验排序字段并在 PostgreSQL 执行排序，置顶项目始终优先；项目响应补充真实 `createdAt`，不依赖浏览器对当前结果临时重排。
- 项目卡片由大面积暗色背景改为浅色信息卡，移除“输入资产—开始设计—成果入库”三段装饰流程。卡片底部只保留一个“开始设计”主按钮，四个统计入口以及置顶、编辑、删除功能保持不变。
- 集成验收覆盖项目名称正反序及创建/更新时间查询；浏览器验收覆盖排序请求参数与实际卡片顺序、浅色背景、三段流程消失和单一“开始设计”入口。
- 最终验证通过 `pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。

## 2026-08-12：图片放大查看抓手拖拽

- 共享 `ImageLightbox` 增加抓手提示和 Pointer Events 平移交互。图片放大超过适配比例后，视口使用 `grab/grabbing` 光标，按住鼠标或触控笔即可拖动查看细节；平移范围按缩放后图像与视口尺寸约束，不会把图片完全拖出可视区域。
- 缩小到适配比例、点击“原始比例”、切换图片或重新打开弹窗都会同时复位缩放和平移。视口隐藏滚动条，资产中心、工作流输入标记图和任务结果等使用共享查看器的位置同步获得该能力。
- 浏览器验收断言抓手入口、放大后的光标、隐藏滚动条及实际拖拽后的 transform 位移。
- 最终验证通过 `pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：长文本输入器与对比模式操作收敛

- “开始设计”输入器改为豆包式有限高度：短文本在 2–8 行内自适应，超过约 220px 后只在文本域内部滚动，不再撑满整个会话页面；底部素材、应用快捷入口和发送按钮始终固定在输入器底部并保持可见。
- 输入正文统一提升到 17px/500 字重，应用与功能入口提升到 15px/600 字重；发送按钮继续沿用平台红色和原有外形，白色上箭头增大到 22px 并提高线条粗细，保证层级清楚但不破坏整体节奏。
- 图片处于左右滑动“对比”模式时完全隐藏遮罩编辑按钮，避免对合成对比视图执行含义不明确的编辑操作；切回单张“结果”模式后恢复既有遮罩入口，输入图片和普通结果的遮罩功能不受影响。
- 系统 Chrome 验收使用 28 行长文本核对输入框最大高度、内部滚动、底部工具栏可见性、字号/字重和发送箭头，并分别断言对比模式无遮罩按钮、结果模式恢复一个遮罩按钮；专项截图已逐项人工核对。

## 2026-08-12：项目成员移除、头像裁剪与项目列表视觉收敛

- 新增项目成员移除接口。项目创建者和平台管理员可以移除非创建者成员；创建者的“移除”仍在同一对齐列显示但置灰，列表不再混用图标和不同字号。移除只删除项目访问关系及个人置顶/当前项目偏好，成员已经创建的资产、任务、设计会话和审计记录完整保留。
- 新增 `024_user_avatars.sql` 和真实头像上传 API。PNG、JPEG、WebP 先在个人中心的沉浸式 1:1 裁剪器中完成拖动、缩放、90 度旋转和复位，再以 512×512 PNG 上传；API 仍独立校验大小、文件签名和方形尺寸，写入 MinIO 私有对象后更新用户记录，并在数据库失败或替换成功时执行相应对象清理。上传成功后个人页、顶栏和用户菜单同步刷新。
- 项目卡片建立主标题、正文和辅助信息三级文字色，项目编号改为灰色；统计数字放大加粗、辅助说明降灰，运行任务大于零时使用平台红强调。卡片整体支持悬浮与键盘焦点，主体可直接进入项目；置顶、编辑、删除操作增加悬浮面与 Tooltip，统计分隔线淡化，日期统一为 `YYYY-MM-DD HH:mm`，搜索占位精简为“搜索项目”。
- 项目空间统一 12–14px 圆角和 16–24px 间距栅格，补齐无项目/无搜索结果空状态与创建按钮禁用态。浏览器专项截图已与需求图逐项比对，浅色层级、操作区、统计区和头像裁剪区均未出现遮挡或越界。
- 自动化新增头像签名/尺寸单测、真实 MinIO 方形约束与成员移除数据保留集成验收，以及项目视觉层级、日期、hover、Tooltip、空状态、禁用态、成员对齐移除和头像裁剪的系统 Chrome 验收。18 份原始 ComfyUI API JSON 未修改。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。

## 2026-08-12：设计会话头像同步修复

- 修复“开始设计”会话侧栏底部仍使用姓名首字而未读取用户头像的问题。该位置现在与个人中心和顶栏共用响应式 `userStore.userInfo.avatar`，上传成功后无需重新登录或刷新即可同步；未设置头像时仍保留姓名首字兜底。
- 会话头像使用固定圆形裁切和 `object-fit: cover`，不会因原图比例破坏侧栏布局。系统 Chrome 头像上传流程继续进入设计会话，断言侧栏使用本轮刚上传的同一头像地址。

## 2026-08-12：资产收藏目录与悬浮星标闭环

- 新增 `025_asset_favorite_virtual_folder.sql`。收藏 API 在同一数据库事务中幂等创建或复用项目根级“收藏”系统文件夹并登记个人收藏；收藏内容通过 `asset_favorites` 关联查询形成当前用户的软链接视图，不复制对象、不修改 `assets.folder_id`，不同用户的收藏互相隔离。
- 取消收藏只删除当前用户的软链接，资产原文件夹、版本、对象和任务血缘全部不变。收藏系统目录不允许上传、文本登记、批量移动/复制、创建子目录、重命名或删除，避免它被误当成普通资产目录。
- 未收藏星标默认隐藏，仅在资产卡片悬浮或键盘聚焦时出现；已收藏星标以平台红选中态常驻。收藏与多选控件统一为 30×30 角标容器和 16px 图标，保持位置、类型和交互反馈一致。
- Pinia 单测覆盖收藏后的目录与原列表刷新；真实 PostgreSQL 集成验收覆盖首次创建、重复收藏幂等、个人软链接查询、原目录不变、取消收藏清除链接和跨用户拒绝；系统 Chrome 验收覆盖默认隐藏、悬浮显示、与选择控件等尺寸、原卡片保留、收藏目录引用及选中态常驻。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；同时复验设计会话侧栏头像已同步本轮上传头像。

## 2026-08-12：资产多选角标视觉一致性修复

- 资产多选与收藏统一使用同一 30×30 白色圆角容器；多选内部改为 16px 黑色描边圆圈并严格水平、垂直居中，选中后只改变圆圈填充和勾选状态，不再呈现偏移的方形控件。
- 移除图片预览左上角与多选位置重叠的半透明格式角标。文件类型仍在卡片正文中明确显示，未悬浮且未选择时左上角现在完全为空，不再残留灰色矩形；网格和列表视图共用同一规则。
- 系统 Chrome 验收增加默认透明度、旧格式角标不存在、收藏/多选等尺寸、圆形边框色与几何中心断言，覆盖未悬浮、悬浮和已选中状态。

## 2026-08-12：设计会话返回入口文案修正

- 设计会话侧栏用户区的返回按钮仍指向现有 `/projects` 项目空间，但旧 Tooltip 和无障碍名称错误显示“返回平台概览”。现统一更正为“返回项目空间”，与单层项目空间的信息架构和实际跳转目标保持一致。
- 全仓检索清理该旧提示，系统 Chrome 验收增加按钮无障碍名称和 `title` 属性断言，避免后续再次出现目标与提示不一致。

## 2026-08-12：设计工作台首页与核心闭环可视化

- 新增登录默认入口 `/home`“设计工作台”，项目空间继续作为项目创建、搜索、排序和维护页面。首页与项目空间均隐藏重复的顶栏项目下拉框，首页自身以紧凑选择器明确当前项目。
- 新增 `/api/v1/dashboard` 真实聚合接口。接口按登录用户的项目范围汇总可访问项目、运行/异常任务、未读通知、当前项目资产/设计会话/成果、可见 AI 应用、七日任务趋势、状态分布、资产构成及最近会话、任务、项目和成果；管理员可见范围与普通成员隔离继续由服务端校验。
- 首页用“项目资产 → 开始设计 → AI 应用 → 任务执行 → 设计成果 → 继续设计”的关系图解释平台核心能力，辅以堆叠趋势图、状态环图、资产横条图和紧凑最近工作列表，避免大段产品说明。所有节点、图例和列表均携带目标项目上下文进入资产、设计或任务模块。
- 无项目用户展示明确创建项目空状态；首页图片预览继续通过项目范围预览 API 获取短时地址，不暴露对象存储私有键。新增真实集成验收覆盖有项目聚合数据和无项目空状态，系统 Chrome 验收覆盖默认路由、闭环节点、三类图表及重复项目选择器消失。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；首页专项截图已人工核对闭环、图表、字号、留白和可点击状态。

## 2026-08-12：设计工作台动态闭环与内容边界优化

- 移除首页标题下方说明、项目选择/快捷按钮和四项顶部汇总区，保留“设计工作台”清晰主标题，进入页面后直接展示平台核心业务闭环，避免与项目空间和下方图表重复。
- “平台设计闭环”由横向串行卡片改为平台红、粉、灰色体系的原生 SVG/CSS 环形动态图。项目资产、开始设计、AI 应用、任务执行和设计成果作为固定可点击节点，中心表达持续设计与验证复盘，环形流向层缓慢顺时针旋转；悬浮暂停，并遵循系统减少动态效果偏好。
- 任务趋势、任务状态、继续设计、资产构成、任务动态、最近项目和最近设计成果统一增加卡片内滚动边界及细滚动条；窄屏时环形关系图和趋势图只在自身区域横向滚动，不再撑破卡片或页面。
- 浏览器验收增加顶部冗余区不存在、五个闭环节点顺序、旋转动画和七类内容区 `overflow` 约束检查；产品需求、架构和质量门禁同步更新。18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：设计闭环方案 A 轻量圆环优化

- 根据视觉复核将厚重分段转盘收敛为固定双细环：移除整圈渐变色块、旋转、虚线连接和额外回流说明，五个业务节点按“项目资产 → 开始设计 → AI 应用 → 任务执行 → 设计成果”直接贴合圆环分布，中心仅保留“持续设计”和“输入 · 生成 · 验证 · 入库”。
- 圆环、节点、图标和文字全部保持静止，仅五个方向箭头按业务顺序依次高亮并产生轻微光晕；系统要求减少动态效果时停止箭头动画并保持清晰静态箭头。节点仍携带当前项目上下文跳转，窄屏仍只在闭环卡片内部横向滚动。
- 浏览器验收由“整圈必须旋转”改为双重约束：外圈不得存在动画，五个箭头必须具有不同延迟的流动动画，避免后续视觉回退。18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：首页闭环数据能量流与视觉放大

- 删除“平台设计闭环”卡片右上角的当前项目名称与编号，避免重复信息占用首页核心视觉区域；当前项目仍由真实聚合 API 决定，五个节点跳转继续携带该项目上下文。
- 将圆环由 340px 放大到 410px，节点图标由 54px 放大到 64px，标题与数量分别提升到 18px、13.5px；同步增大中心信息、节点标签间距和闭环卡片高度，让五项核心能力在首页首屏更加醒目舒展。
- 完全移除五个三角方向箭头，改为固定双层轨道：外层点阵、内层点阵与局部高亮能量束按不同速度连续流动，圆环和节点本体保持静止；减少动态偏好下停止所有轨道动画并保留清晰静态关系。
- 浏览器验收新增右上角文字缺失、圆环尺寸、字号、标签距离、无三角箭头、四层数据能量流均启用及外圈固定断言。真实系统 Chrome 全流程验收通过，截图为 `/tmp/rail-workflow-browser-acceptance-dashboard.png`；目标 ESLint、Stylelint 通过，18 份原始 ComfyUI API JSON 未修改。
- 视觉复核发现局部高亮能量束的长尾运动形态类似蠕动条，已完整移除渐变长束与白色拖尾，替换为三个等距、固定直径、无尾迹的小型数据脉冲沿轨道匀速移动；浏览器验收增加长条能量束不得存在的回归断言。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；首页专项截图已人工核对节点位置、文字层级、固定外圈和箭头流向。

## 2026-08-12：首页闭环层级与任务列表一致性修复

- “项目资产”调整为文字和数量位于圆环外、图标贴合圆环，与其余四个节点保持一致；五个节点标题统一提升到 16px/700，数量提升到 12px/500，并同步放大节点图标，强化首页核心入口层级。
- 定位“首页显示运行任务、任务中心为空”为数据刷新时机问题：首页聚合接口读取实时数据库，任务中心此前可能继续显示 Pinia 初始化时的旧任务列表。任务中心现于每次进入页面时单独重新拉取当前项目任务，不额外刷新资产或目录，确保 `queued`、`running`、`cancelling` 三种活动状态与首页统计一致。
- 本地只读排查确认 `CR-2026-0183` 的 `TSK-00000177` 是早期系统 Chrome 验收残留的真实 `running` 测试记录，并非首页伪造数字；本次代码修复不擅自删除用户环境数据。浏览器验收新增五节点字号和“项目资产”外置布局断言。
- 最终验证通过：目标 ESLint/Stylelint、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；首页与任务中心专项截图已人工确认。

## 2026-08-12：首页跨项目口径与资产详情深链

- 统一设计工作台数据口径：项目资产、资产类型、已入库设计成果按当前账号可访问的全部未归档项目汇总；任务趋势、状态和运行数按当前账号在这些可访问项目中创建的任务汇总。被移出或归档项目的数据不再残留在首页统计中，管理员仍按其平台范围查看全部未归档项目。
- 最近项目改为稳定的四段同行布局，分别显示项目名称、项目资产及最多三类类型摘要、项目任务及编号、运行状态或更新时间；长名称与类型摘要均在所在列截断，不再把编号、状态挤到不同行。
- 最近设计成果不再进入资产中心根列表。新增受身份和项目范围保护的 `GET /api/v1/assets/:id`，首页先切换到成果所属项目，再使用 `?assetId=` 打开具体资产详情；详情 URL 可刷新、可回退，目标资产即使位于子文件夹也能读取，越权访问仍返回 404。
- 集成验收补充跨项目资产汇总、项目级类型拆分、资产详情深链和跨用户拒绝；18 份原始 ComfyUI API JSON 未修改。
- 全项目回归进一步发现资产详情抽屉的文本/三维预览会与列表刷新和深链路由产生异步竞态。文本阅读和三维模型预览现分别由按资产 ID 驱动的独立组件加载；切换资产时旧请求不能覆盖新详情，文本正文不再停留在加载占位，三维查看器关闭时停止动画、清空场景并显式释放 WebGL 上下文。
- 系统 Chrome 全量浏览器验收覆盖首页跨项目统计、项目级资产类型摘要、最近成果精确详情、文本正文、图片抓手拖拽、3D 模型与其余设计链路并全部通过；专项截图为 `/tmp/rail-workflow-browser-acceptance-dashboard.png`。最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元/状态测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。

## 2026-08-13：应用资产目录选择与遗留活动任务治理

- 将应用输入、完整参数抽屉、旧工作区和 Markdown 输入共用的资产选择器改为按当前项目独立读取。弹窗可逐级进入持久化文件夹和个人收藏目录，通过面包屑返回，并在当前目录按名称、业务编号或标签搜索；兼容类型筛选及创建时间、名称、类型正反序均复用资产 API 的白名单排序。
- 任务中心不再让活动任务只显示一个必然失败的删除入口。`queued`、`running` 任务显示“取消任务”：存在 `job_executions` 的真实 ComfyUI 执行进入 `cancelling` 并等待 Worker 确认；没有执行记录的孤立台账任务立即转为 `cancelled`，随后才显示软删除。取消审计补充取消模式和原状态，不删除任务、资产或审计台账。
- 任务列表 API 同步返回是否存在持久化外部执行记录。活动任务没有 `job_executions` 时，页面明确提示“无外部执行，可取消后删除”，避免把测试台账或异常孤立记录误认为真实 ComfyUI 作业；从“进行中”筛选取消成功后自动回到全部任务，以便用户立刻完成软删除。
- 定位 `TSK-00000177 / 浏览器活跃任务 / 32%` 为系统 Chrome 自动验收中断后遗留的测试台账，数据库确认其没有 Worker 执行记录和外部任务编号，不是真实 ComfyUI 运行。浏览器脚本为测试任务增加显式标记，并在每次启动前清理超过 30 分钟的验收项目及无引用验收账号，避免异常退出再次污染开发数据。
- 浏览器验收将 Markdown 输入资产放入真实子目录，覆盖进入文件夹、面包屑、类型筛选、排序、子目录选择回填和孤立活动任务取消闭环；验收启动时已清理截图中的 `TSK-00000177` 测试项目，复查数据库确认该任务不存在。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；18 份原始 ComfyUI API JSON 保持只读。

## 2026-08-13：镜头预设控件水平对齐

- 修复多视角镜头完整参数面板中“水平、垂直、距离”三组预设因标签和选项宽度不同而上下错位的问题。共享镜头控制组件改为固定两行网格：标签与下拉框统一处于首行，当前数值统一处于次行，恢复默认按钮独立占位；镜头 1 至镜头 5 及单角度能力共用该修复。
- 角度预设、拖拽环绕、滚轮距离、提示词映射和 ComfyUI 参数协议均未改变，18 份原始 API JSON 未修改。浏览器验收新增三组下拉框与数值行纵坐标误差不超过 1px 的断言。
- 验证通过：目标 ESLint/Stylelint、Web 28 项单元测试、`@rail/web` 类型检查；在本地真实设计会话的完整参数抽屉中读取计算布局，三组下拉框 `y=999.734375`、三组数值 `y=1026.734375`，确认保持同一水平线。

## 2026-08-23：甲方模块意见前端第一版适配

- 在 `codex/customer-feedback-frontend` 分支先建立原始需求基线：原样保存甲方 DOCX、22 张 PNG 和 1 个 WDP 资源，源文件与仓库副本 SHA-256 均为 `3ae9c58db091988165fb157a2c654400c7d3a6e742802abf1f0f7ee5e45a5dac`。独立 `COMMITLOG.md` 按本地提交逐条记录中文动机、范围和边界，未推送远程。
- 新增可扩展设计模式目录，将客室零部件、CMF、客室效果和报告映射到平台应用键；页面只消费 API 返回的可见应用与已发布能力契约。同类能力或版本通过目录追加，报告与 LoRA 训练缺少契约时明确待接入，不创建任务、不伪造资产。
- 设计页增加四模式切换、场景提示与动态占位；URL 应用入口和结果继续设计可自动匹配模式。现有项目隔离、会话草稿、任务互斥、参数 Schema、失败状态、资产登记、审计和 ComfyUI Worker 边界均未旁路，18 份原始工作流 JSON 修改为 0。
- 共享结果卡片增加 2/3/4+ 图片网格与 `+N` 聚合；共享图片查看器增加上一张、下一张和键盘方向键，保留缩放、拖拽、遮罩对比、3D、下载、资产保存、重新运行与继续设计。
- 首页增加六个快捷入口；新设计复用真实项目创建接口，历史弹窗使用真实最近会话，资产入口保持当前项目上下文，训练/报告入口按真实能力状态展示。平台标题与侧栏名称统一为“客运装备内装模块化分区快速设计平台”，只覆盖品牌身份字段，不清除用户主题和布局缓存；甲方未提供独立授权 PNG 标识，继续使用仓库现有标识。
- 自动化验证通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 31 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收和 `pnpm build:rail`。真实浏览器复验首页六入口、新建/历史弹窗、品牌缓存迁移、CMF/零部件模式、专属 3D/多角度能力和待接入不跳转；控制台无新增错误，仅保留既有表单迁移与存储前缀警告。当前本地没有可执行 ComfyUI 服务及含多图片成功输出的会话，因此本轮未现场生成多图，网格/前后切换只完成类型、规范和生产构建验证，待具备真实输出样本后补做视觉复验。

## 2026-08-23：0820 首页布局收敛

- 将登录默认入口、侧边菜单和页签从“设计工作台”统一命名为“首页”，移除首页中的闭环图、趋势图、任务状态、最近项目和最近成果，使 `/home` 只承担平台门户职责。
- 主内容按 0820 示意图改为居中欢迎区和 3×2 六个入口；布局对齐文档，配色统一使用平台红、浅红、雾灰和灰色边界，不使用示意图中的绿、黄标注色。
- 新设计、历史设计、资产中心和项目工作区继续走真实业务链路；模型训练与报告生成未接入时保持可点击说明，不伪造外部服务成功。
- 浏览器验收脚本同步从旧看板断言转为首页命名、入口顺序、3×2 尺寸、主题配色、旧看板不存在、新设计/历史弹窗和待接入入口回归。目标 ESLint、Stylelint、`@rail/web` 类型检查及真实浏览器复核通过。

## 2026-08-23：0820 设计主界面布局收敛

- 左侧历史会话区按文档补充独立收起/展开控制，收起后结果区占满视口；品牌全称允许换行完整显示。页面仍沿用平台红、浅红、雾灰和灰色边界，没有照搬需求图中的标注色。
- 客室零部件、CMF 和客室效果三种视觉模式统一将“文本、文生图、理解、重绘、扩图、多图、放大”作为前七个快捷工具，LoRA、单图编辑、捕获、参考重绘、多角度和 3D 等既有高级能力继续由“更多”承载；只调整展示目录，不改动能力 Schema、适配器或 18 份原始 ComfyUI 工作流 JSON。
- 报告模式按 0820 文档增加报告类型、Word/PPT 交付格式、标题说明及三项图片素材布局。当前没有已发布报告能力契约，因此控件保持禁用并明确提示不能提交，不创建任务或静态结果。
- 首页模型训练入口按文档参考补充左右分栏：参数设置包括 Repeat、Epoch、触发词和预览提示词，图片区展示打标/裁剪上传位，底部保留裁剪方式、尺寸、打标模型和开始训练。当前 LoRA 服务与数据协议未定义，全部控件禁用，不创建训练任务。
- 历史结果的“继续设计”面向用户改称“深化设计”，实际资产确认、项目范围与目标应用输入逻辑保持不变。模式目录单测、目标 ESLint、Stylelint、Web 类型检查通过；真实浏览器确认工具顺序、任务栏折叠和报告待接入布局。
