# 二次开发记录

## 2026-09-10：当日综合审查与远程交付

- 按最终行为梳理平台外壳、设计输入器、模板与比例、多图预览、资产中心、工作台、助手、三维导出及夜览主题，完整说明见 reviews/20260910-development-review.md。
- 修复工作台未保存结果误用已入库详情接口、跨类型预览请求竞态和三维格式失败处理；模板应用先校验最终文本再变更参数。更新过期浏览器选择器并清理全仓库规范问题。
- 完整门禁及八类专项浏览器验收执行结果记录在综合审查文档；外部助手不可用按失败路径验收，未执行真实 GPU/LoRA 任务。新增迁移032–034随代码交付，数据库与对象文件不进入Git。
- 集成入口改为通过既有 run-with-env 加载当前环境，修复凭据切换后的 Worker/清理测试失败；完整重跑通过，精确清理本轮遗留4个测试对象。
- 同日启动/MinIO配置变更已结束并纳入审查，默认开发配置与本机凭据覆盖分离；真实环境文件保持忽略。

## 2026-09-10：HTTPS 子域名的私有对象访问修复

- 保留现有网页入口，新增回环绑定的对象存储 FRP 隧道。Caddy 按桶路径转发、保留原始 Host/路径/查询参数，不公开 MinIO 管理台，不改变公司官网路由。对象响应设置沙箱、nosniff 和无来源泄露策略；网关仅接受 GET/HEAD/PUT/OPTIONS。
- API 内部存储连接与浏览器公开地址分离；公开预签名 URL 改为站点同源 HTTPS，同步明确的 CORS 来源。轮换开发默认 MinIO 凭据并同步 API/Worker；凭据仅保存在权限受限、Git 忽略的本机文件。
- 开发 Compose 新增 `minio.defaults.env` 和可选 `.env.minio.local` 覆盖，使后续容器重建保留自定义凭据。管理脚本新增 `start/restart --services-only` 与配套 pnpm 命令，仅重启业务进程，不执行迁移/种子或重建数据库。
- 保留原 MinIO 命名卷和已有对象；未执行数据库迁移。变更前备份本机/云端相关配置；原网页和公司官网均返回 200。
- 新增 `relay-storage-smoke-test.ts`：真实公网 API 登录、签名 PUT、预览、下载、Range、CORS、匿名访问/错误签名拒绝及跨项目拒绝全部通过。用户指定的已有 PNG 经公网读取，与内部读取的 3,786,303 字节内容完全一致。两个临时测试账号、项目和对象已按本轮 ID 清理，未删除原业务数据。
- 静态验证：平台 API `typecheck`、专项脚本 ESLint、Shell `bash -n`、Compose 配置解析及 Caddy 配置校验通过；配置/CORS 两个测试文件共 11 项单测通过。FRP 后台服务 active/enabled，Web、API、Worker 和 MinIO 已恢复运行；本机凭据文件权限为 600。
- 此轮为配置与部署修复，未更改业务 UI/权限代码，未运行完整生成任务、GPU、负载或全仓构建验收。浏览器个人会话中的具体页面由用户刷新确认；API 签发与真实文件字节链路已验证。部署与回退说明见 DEPLOYMENT 第 4.8 节。

## 2026-09-10：模板与外部比例双向联动

- 模板图像尺寸分类改为共用比例预设宫格；选择立即更新外部真实宽高，预览和再次应用均读取当前参数，避免旧模板尺寸覆盖最后一次外部修改。
- 应用比例模板时新增独立比例描述行；实际宽高变化时仅更新该行，其余用户文字保留，重复应用不叠加比例行。没有模板比例行时外部尺寸修改不添加文本。已有会话草稿接口持久化宽高和文本，无迁移。
- 修改设计页、模板选择组件、尺寸工具与上下文类型，新增比例文本同步单测并扩展 aspect-ratio-acceptance.ts。API 108 / Web 65 项单测、类型检查、生产构建、目标 ESLint/Stylelint 通过；真实浏览器覆盖即时同步、比例文字更新、内部回显、交叉修改后再次应用、刷新恢复和窄屏，控制台无错误。未改后端接口，本轮未重复后端集成与实际 GPU 生图。

## 2026-09-10：比例参数简化样式

- 去掉比例弹层底部的操作说明和尺寸范围两行常驻文字，外部比例入口改为普通深色文字与透明底色；异常尺寸的按需反馈仍保留。
- 目标 ESLint、Stylelint 及比例浏览器验收通过，桌面截图确认说明移除和默认配色；宽高联动、模板同步、刷新持久化及窄屏检查通过，控制台无错误。本次仅样式与说明文字调整，未重复全量门禁，无数据库变更。

## 2026-09-10：A 版比例尺寸与提示词模板联动

- 设计输入器合并宽高快捷参数为比例入口，提供八格比例选择、宽高输入、双向比例锁定与解锁，自动恢复工作流默认尺寸；使用工作流范围及步长寻找最近的兼容尺寸，并明确提示调整或失败。保留管理员快捷参数展示配置。
- 模板选项新增可选 aspectRatio，预览接口返回独立 size；应用时同步真实工作流参数和持久化会话草稿。兼容旧宽高文本，仅比例按当前宽度联动；尺寸不再拼入提示词，尺寸单独应用保留已有文本，非法尺寸或矛盾配置阻止应用。
- 核心文件：design-image-size.vue、image-dimensions.ts、设计页、模板选择与维护组件，以及服务端模板 schema/compose。无需新增迁移，比例保存于既有模板 JSONB，宽高继续走既有会话草稿 API。
- 验证：pnpm test:rail（API 108、Web 64 项）、pnpm typecheck:rail、pnpm test:rail:integration、pnpm build:rail，以及目标 ESLint/Stylelint/Oxlint 和 diff 检查。浏览器脚本 aspect-ratio-acceptance.ts 验证横竖方比例、宽高双向联动、解锁、模板尺寸同步、文本保留、刷新恢复与 390px 面板；控制台无错误，临时数据已清理。证据见 ui-proposals/20260910-aspect-ratio/verification/。
- 未新增真实 GPU 生图验证；集成 Worker 验收验证既有提交/轮询/输出存储链路。本次未重跑全仓库 lint，仅对相关文件执行规范检查。

## 2026-09-10：AI 回复统一左对齐

- 设计会话的多图图片组、单图和对比图取消水平自动居中，统一贴齐回复内容区左边界；文字回复设置左对齐，运行中反馈也靠左。混合比例主图在稳定展示区域内靠左完整显示，用户输入气泡及大图预览布局保持原有行为。
- 浏览器实测单图、对比图及五组多图的左边界均与回复区域一致，控制台无错误；目标 ESLint、Stylelint、typecheck:rail 与 git diff --check 通过。本轮为局部样式调整，无 API/数据库改动，未重复完整测试和构建。

## 2026-09-10：主图与紧凑缩略图条

- 新增 ImageResultGallery，设计会话多图使用一个主图与固定 64px 正方形缩略图条；实际有多少张就显示多少张，取消 +N 占位。点击缩略图更新主图与计数，点击主图进入既有大图预览及图片操作。
- 图片组按首张自然比例和可用视口限定宽高，同组切换混合比例时保持几何尺寸稳定；主图 contain 完整显示、缩略图 cover 居中裁切。时间与计数分别对齐图片组左右边缘，窄屏缩略图局部横向滚动。
- 浏览器实测：2048×1072 横图展示约 825×432，1024×1024 方图展示 432×432；390px 窄屏分别为 345×181、345×345，无页面横向溢出，八图缩略图可横向滚动。切换第 5 张后主图、选中状态、计数与大图预览一致，控制台无错误。
- 新增 7 项组件测试覆盖 2/3/5/8 张的选择和放大事件，以及方图、横图、竖图和混合比例的稳定尺寸逻辑；竖图由组件测试覆盖，未新增真实 GPU 竖图任务。Web 共 50 项测试通过；类型检查、目标 ESLint/Stylelint、生产构建与 diff 检查完成。同步长流程浏览器验收断言，本轮未执行完整后端/GPU 长流程。无 API 或数据库迁移。

## 2026-09-10：图片预览与更多缩略图按原图比例适配

- 按最新反馈替代上一版固定 96vw×96vh 窗口：读取图片自然宽高，结合视口约束等比计算预览尺寸，窗口随图片收拢；保留工具栏、切图、缩放及右下角操作。96% 作为视口上限，不再强制窗口占满固定面积。
- 会话多图网格取消固定 16:9 总高度，各图片按钮随实际显示图片尺寸收拢；主图和次图分别限制最大高度，+N 遮罩覆盖实际缩略图而非整列空白。
- 浏览器验证：1024×1024 图片的 +6 缩略图为 180×180，预览图为 541×541、窗口约 581×681（1280×720 视口），无两侧大块留白，控制台无错误。typecheck:rail、目标 ESLint/Stylelint、Web 43 项测试通过；无 API 或数据库改动，未重复执行完整集成测试与生产构建。

## 2026-09-10：多图操作移入大图预览

- 底部四个业务入口恢复原始黑蓝暗色渐变，历史会话选中侧线及图标恢复主题红色；输入器按钮继续使用红色。
- 设计会话多图结果下方只保留完成时间。放大预览通过 actions 插槽在右下角展示图标操作，切图复用 activeOutput，下载、保存、编辑等对应当前图片；打开编辑类操作时关闭预览，单图会话操作保持不变。
- 图片预览取消 1180px 上限，窗口宽高均为可视区域的 96%，图片等比完整适配。桌面实测窗口面积占比 92.16%，右下角 10 个操作图标；切到第二张后打开遮罩编辑，核对图像源与第二张相同，取消编辑未保存或提交任务。
- 已验证：独立新页面预览和切图控制台无错误；typecheck:rail、Web 43 项单元测试、目标 ESLint/Stylelint、Web 生产构建与 git diff --check 通过。未重新执行完整 GPU/后端集成验收，本轮无 API 或数据库迁移。

## 2026-09-10：恢复设计输入器主题红色

- 按反馈恢复功能选择、更多、发送按钮及参数选中/悬浮状态的主题红色；底部四个业务入口保留图片背景，使用酒红色渐变与白色文字统一主色。
- 灰色输入框与无边框图片保持此前效果。本轮仅修改 design/index.vue、design-quick-field.vue 的颜色样式，无 API 或数据库变更。
- 已验证：真实浏览器截图与计算样式确认主题红色生效、输入框仍为灰边，控制台无错误；目标 ESLint、Stylelint 和 git diff --check 通过。纯颜色调整未重复执行集成测试或生产构建。

## 2026-09-10：会话图片去边框与方形上传缩略图

- 设计会话的生成图片、历史输入图片与输入框上传缩略图移除可见边框；生成图片选中或悬浮时不再出现外框，保留键盘焦点提示和图片操作。
- 输入框缩略图统一为 78×78px，使用 object-fit: cover 居中等比裁切；多张图片沿用同一渲染组件，原图文件和工作流输入不裁剪、不变形。
- 已验证：真实浏览器中生成图及历史输入图片边框均为 0px；2048×1072 原图的输入缩略图显示为 78×78px、居中 cover，控制台无错误。测试选择的素材已从草稿移除，未提交生成任务。目标 ESLint、Stylelint、typecheck:rail 与 git diff --check 通过。
- 本轮仅调整两处 Vue 组件样式，无 API 或数据库迁移；未重复运行完整集成验收和构建。

## 2026-09-10：灰色界面与紧凑参数浮层

- 快捷参数浮层按类型收窄：数字/开关 220px，其余 280px，小屏按视口限制；选项网格按可用宽度自动排列。
- 设计结果操作栏后显示真实任务 completedAt，短格式为月/日 时:分，悬浮显示完整本地时间；没有完成时间的任务不伪造时间，顶部轮次卡片信息继续隐藏。
- 任务栏折叠控件改为 24×34px 白底灰边悬浮按钮，增加清晰的悬浮/键盘焦点样式，保留展开和收起行为。
- 平台强调色变量切换为中性灰，保留旧变量别名兼容页面；设计输入框、快捷参数、图片选中框及相关装饰改为灰色，减少红色边框与阴影。品牌 Logo、少量主操作和独立告警色仍保留。
- 已验证：真实页面数字参数浮层宽 220px，结果显示完成时间，图片边框为灰色，任务栏可正常收放；typecheck:rail、Web 43 项单元测试、目标 ESLint/Stylelint 与 Web 生产构建通过。同步浏览器脚本灰色主题断言，未重新执行完整长流程；无 API 或数据库改动，未提交或推送代码。

## 2026-09-10：生成数量上限统一为 8

- 平台在公开参数 Schema 和实际任务参数映射时统一应用 batch_size/batchSize 数量策略：整数 1–8，数量默认值也限制在范围内。旧文生图 max=4 和其他节点 max=4096 均按新策略生效，已发布版本及历史参数不改写，无数据库迁移。没有数量字段的工作流不虚构额外批次参数；图片尺寸等其他数字参数不受影响。
- 参数帮助显示单次最多 8 张；HYPIR 修复批次另行说明它不等于最终出图数量，三维批次用个数描述。参数帮助由现有更多抽屉和快捷参数浮层展示。
- 工作流管理是管理员全局配置，顶部隐藏当前项目选择器，后端工作流管理继续沿用全局管理员权限。
- 验证：API 96 项、Web 43 项单元测试、typecheck:rail、目标 ESLint、平台集成与 Worker 协议验收、生产构建；浏览器检查工作流管理无项目选择器，数量字段显示范围提示。未重新执行真实 GPU 八图推理；未提交或推送代码。

## 2026-09-10：连续会话布局与图标操作栏

- 设计会话内容与输入器统一横向留白、最大宽度和滚动条占位，移除多图区域独立 920px 上限；消除手机多图最小高度导致的横向溢出。
- WorkflowRunCard 新增 conversationLayout，仅设计会话隐藏轮次、时间与状态标题，移除每轮圆角外框、背景和阴影；运行反馈与错误正文保留，其他工作区继续使用原卡片。图片操作改为左对齐纯图标，提供 Tooltip、按钮名称和禁用原因，既有动作不变。
- 浏览器实测：1370px 视口中会话、多图区域和输入框均宽 989px，左边界 354px；390px 视口中三者均宽 345px，左边界 18px。轮次标题不存在，十个操作按钮无可见文字且保留可访问名称。
- 验证：目标 ESLint、Stylelint、typecheck:rail、Web 43 项单元测试及 Web 生产构建通过。同步长流程浏览器验收断言，本轮使用真实页面交互检查，未重新执行完整长流程脚本。纯 UI 调整，无数据库迁移、API 改动或重新提交生成任务；未提交或推送代码。

## 2026-09-10：默认文生图、统一功能选择与管理员参数展示

- 先梳理设计页选择逻辑、模式目录、能力 Schema、草稿 API 和工作流管理。此前默认执行文生文、工具栏按模式过滤、外显参数固定由 advanced 决定。
- 改为默认选中文生图，功能下拉一项对应一个真实 ComfyUI 能力；三个视觉模式共享功能目录，右侧参数和“更多”绑定当前工作流，保留四个底部入口与独立报告页。模式使用独立首次提示词预设、模板及结果操作。
- 管理员可在工作流管理中勾选外显字段并排序，新配置持久化、校验版本和字段白名单并写审计；不改写已发布工作流和历史任务。
- 新增迁移 032，将展示配置独立存储，将设计草稿扩展为会话＋工作流＋视觉模式；旧草稿保留为 cabin。API 保留未传 designMode 时的兼容默认。
- 已验证：迁移应用成功；API 94 项、Web 43 项单元测试；类型检查；目标 ESLint/Stylelint；真实平台集成和 ComfyUI Worker 协议端到端验收；API/Web 生产构建。新增浏览器检查覆盖默认功能、完整菜单、模式预设、参数替换及管理员设置；管理员实际勾选种子、上移、保存，重新打开与设计页回显顺序一致，随后恢复测试前的外显字段。1370px、768px、390px 布局检查通过，手机默认收起任务栏并支持覆盖展开，功能选择、“更多”和发送保持可见。浏览器长流程验收脚本同步新交互断言，本轮使用交互式浏览器检查，未重新运行整套长流程脚本。
- 本轮未重复执行真实 GPU 出图，外部 ComfyUI 模型、节点和服务仍需目标环境提供。部署需先应用 032；回退注意旧草稿双列主键不兼容，恢复方案见架构文档。未提交或推送代码。

## 2026-09-09：本地开发服务子域名联通测试

- 恢复既有 FRP 客户端，将云端回环端口转发到本地 Web 5666，启用用户级后台服务和开机启动；云端 Caddy 原有主站与子域名配置未改写，仅平滑重载以重新申请子域名 HTTPS 证书，证书已签发。
- Web 开发服务器仅在 `server.allowedHosts` 增加指定测试域名，不开放任意域名；原有 `/api` 到本机 API 的代理、账号认证、数据库、对象存储和业务逻辑均保持不变。没有迁移、种子初始化、数据清理或依赖变更。
- 已验证：公网登录页、入口模块、开发客户端、Logo 和 `/api/v1/health` 返回 200；未登录访问用户 API 返回 401；未列入白名单的 Host 返回 403；HTTP 跳转 HTTPS，TLS 校验成功，独立公司官网仍返回 200。目标 Vite 配置 Oxlint 与 `git diff --check` 通过。
- 本轮是开发环境映射测试，未运行全量门禁、浏览器账号登录、上传下载、对象存储外网可达性或真实业务操作验收；不把入口联通视为生产部署完成。长期公开运行应使用生产构建，并独立检查对象存储地址、强密码与访问控制。未提交或推送代码。

## 2026-09-08：研究报告 V2.3 事实校准与论文式图形重绘

- 按最新意见发布 `RAIL-CONTRACT-RP-001` V2.3 建设指导评审稿，保留 V1.0 至 V2.2 历史文件。多模态收敛为 AI 助手文本与图片问答，在功能章节内以两段、274 个汉字简述，不再设大篇幅章节，也不与 ComfyUI 设计任务编排混写。
- 重新核对合同（26版）技术内容、可读取的技术响应第四版及助手、标注、工作流、任务、资产、训练、报告和部署实现。区分合同要求、现有实现与拟研究内容；明确人工标注不等于自动识别、个人图文问答不等于设计执行、任务结果与可复用项目资产存在登记边界；参数与规则引擎、跨区域优化、向量检索和既有产品平台对接不写成已完成能力。
- 用户原桌面技术响应路径不可读，本轮使用微信文件目录内同名第四版，其 SHA-256 与旧登记不同；合同原件哈希未变。内部 `source/EVIDENCE_V2.3.md` 记录两个技术响应哈希、使用范围及正式发布前核对签署版本的事项，不将其视为同一文件静默替换。
- 重绘 14 幅论文式黑白灰技术图，分别采用架构分组、部署拓扑、层级树、正交流程、时序图、关系矩阵和甘特图；减少装饰，明确系统边界、人工确认与拟建能力，检查所有连接及图文布局。源稿研究内容为 19,979 个汉字（含标题、图表题和表格，不含文件头和图片内文字），报告共 38 页、14 幅图、12 张表；不含报价、商务或代码说明，不填造实验数据。
- 最终 38 页均完成视觉复核，14 幅图的自检和边界检查通过；16 个章级目录入口、32 个内部链接及 61 个标题书签通过检查，页码映射与最终 PDF 标题位置一致。页面与样式、图表连续编号、图片替代文本、重复表头、正式内容词项和 DOCX 完整性检查通过，无障碍审计高/中/低风险为 0/0/0。目录为真实内部链接而非自动目录域；目标 Word 字体环境差异可能导致重新分页，内部记录已说明再生成方法。
- 代码基线仍为分支 `codex/client-feedback-white-shell-20260901`、完整提交 `51c4095ec269cde31b543dd6c8886802a9a8f452`，于 2026-09-08 重新检查。仅修改合同报告源稿、图形、生成与审计工具、交付件及登记说明，未修改业务代码、迁移、配置或开发数据；本轮未重跑业务单元测试、真实集成、外部推理、浏览器业务验收和生产构建，不据此声称运行系统已通过合同验收。

## 2026-09-03：研究报告 V2.2 清除投标语境残留

- 对 V2.1 重新进行逐段语义审查，确认此前虽已删除价格、金额、费用和预算等直接内容，但仍存在“技术标书内容转化”“投标响应扩展”“技术响应文件”和“资源配置”等投标语境残留；发布 `RAIL-CONTRACT-RP-001` V2.2，并将 V2.1 转为历史版本。
- 表 1 改为“平台建设输入与研究任务的展开关系”，列名统一为“建设输入—本报告研究重点—对后续建设的指导作用”；图 1 改为“平台建设输入向研究任务的分解与闭环”，将原技术响应输入改为平台建设需求。文档控制页、报告定位、建设依据、评价指标、模型选型、运行保障、验收原则及甲方关注事项表同步改为独立研究报告表述。
- 将容易产生商务测算联想的“人工修订工作量”“资源适配性”等改为“人工干预强度”“运行环境适配性”，正式 DOCX 不再出现直接价格词、投标语境词和相关模糊表述。
- V2.2 正式可见文字为 19,758 个非空字符，共 40 页；15 幅连续编号技术图、14 张连续编号数据表、66 个目录入口、132 个内部链接及目录真实页码保持一致。全文 XML、可见正文和最终 PDF 词项扫描均为零命中，40 页逐页渲染无空白页、裁切、重叠或表格越界，无障碍审计高/中/低风险为 0/0/0，压缩包检查通过。
- 代码基线仍为分支 `codex/client-feedback-white-shell-20260901`、提交 `51c4095ec269cde31b543dd6c8886802a9a8f452`；本轮只修改合同文档源稿、图 1 素材、生成工具、交付件和内部登记说明，未修改应用代码、数据库迁移、部署配置或开发数据。

## 2026-09-03：研究报告 V2.1 图形体系重绘与篇幅控制

- 根据第二轮评审意见发布 `RAIL-CONTRACT-RP-001` V2.1 建设指导评审稿，保留 V2.0 历史版本。正式报告继续以平台构建关键技术、总体架构、功能模块、技术路线、进度计划和验证方法为主，不增加运行代码说明。
- 重绘全部 15 幅技术图：总体技术架构由原四层概览扩展为业务访问、业务应用、领域服务、智能能力、平台支撑、数据与基础设施六个技术层面，并增加标准治理、安全运行两条横向主线；服务拓扑补充网络分区、接入防护、计算调度、数据保护和产品平台边界。
- 其他图分别改用分层图、泳道图、矩阵图或正交流程图，增加对象、能力、规则、数据、任务、阶段门和验证证据等细节。原跨区域关系网络改为“顶部/侧墙/地板/端墙/门区/座椅 × 空间边界/装配接口/功能联动/风格协调”的依赖矩阵，不再使用大量交叉连线。
- 清理正式报告中的报价、价格、金额、预算、费用、付款、商务等内容；对制造适配、人工修订工作量和资源适配性采用工程研究表述。最终 Word 正式可见文字为 19,764 个非空字符，符合约两万字的篇幅目标。
- V2.1 成品共 40 页，包含 15 幅连续编号技术图、14 张连续编号数据表、66 个目录入口和 132 个内部链接；目录页码按最终渲染结果重新计算，66 项全部与正文实际页码一致。
- 最终版逐页渲染复核无空白页、图文裁切、对象重叠、表格越界或异常交叉连线；页面参数、标题层级、图像嵌入、图片替代文本、表头标记、图表编号、目录书签与超链接、无障碍、敏感词和压缩包完整性检查通过。代码基线仍为分支 `codex/client-feedback-white-shell-20260901`、提交 `51c4095ec269cde31b543dd6c8886802a9a8f452`，仅在内部源稿和登记记录中保存。
- 本轮仅修改合同文档源稿、图形素材、生成工具、交付件和登记说明，未修改应用代码、数据库迁移、部署配置或开发数据。

## 2026-09-03：研究报告 V2.0 重构为平台建设指导文件

- 依据甲方最新意见，将 `RAIL-CONTRACT-RP-001` 从偏重现状与代码事实说明的 V1.x 重构为平台构建前技术指导文件。正式稿重点展开内装模块分区识别、多模态应用、模块化/参数化/规则化快速设计、跨区域协同优化、平台总体架构、功能模块、数据与知识底座、系统集成、实施技术路线、搭建进度、验证方法和风险控制。
- 参考《投标文件模板-技术响应第四版》中的四层架构、功能响应、项目组织、质量、安全、服务和验收思路，将技术标书的总体响应扩充为可供需求、数据、算法、设计、开发、测试和实施共同使用的落地指导版本；技术开发合同（26版）及附件技术规格书继续作为要求冲突时的最终边界。
- 正式 DOCX 删除 Git、代码目录、路由、迁移、框架、测试命令等实现说明，只保留面向评审的研究内容、技术方法和建设方案。代码基线仍在 V2.0 源稿文件头、文档说明和登记表内部记录：分支 `codex/client-feedback-white-shell-20260901`，提交 `51c4095ec269cde31b543dd6c8886802a9a8f452`，提交时间 `2026-09-01T21:19:51+08:00`，便于后续系统迭代时同步修订文档。
- 新增并统一管理 15 幅技术图，覆盖建设指导框架、业务闭环、研究主线、四层架构、部署边界、四级模块体系、分区识别、人机复核、多模态编排、快速设计闭环、跨区域优化、功能地图、数据底座、技术路线和验证体系；14 张数据表按全文顺序统一编号并逐一在正文中引用。
- 目录改为 66 个带书签的可点击标题入口，标题文字与页码共 132 个内部链接；目录显示页码按照最终渲染结果固化。编号列表统一使用紧凑悬挂缩进和中文全角句点，修复序号与正文之间间隔过大的问题。
- 延续指定版式：A4 纵向，上/下页边距 2.5 cm、左 2.6 cm、右 2.4 cm、装订线 0 cm、页眉 1.5 cm、页脚 1.75 cm；正文宋体小四、固定 24 磅行距、首行缩进 2 字符，一级标题黑体四号、二级标题楷体四号、三级及四级标题宋体四号。图片段落仅为保证绘图完整显示采用单倍行距，不改变正文、标题、图题和表题的固定 24 磅要求。
- 最终 DOCX 共 33 页并逐页渲染复核，15 幅图片均完整显示，正文页面在目录页码更新前后逐页像素一致。页面参数、标题层级、17 个 DOCX 表格的表头标记、15 幅图片、图表编号连续性、66 个书签、132 个超链接、目录 PDF 注释、正式正文实现词扫描和压缩包完整性均通过；无障碍审计高/中/低风险为 0/0/0。
- 本轮仅修改合同文档源稿、图表、生成工具、交付件和登记说明，未修改应用代码、数据库迁移、部署配置或开发数据，也未重新执行运行系统的集成、浏览器、外部推理或生产构建测试。

## 2026-09-03：研究报告 V1.2 按指定文章格式排版

- 在不改变 V1.1 研究正文、表格数据、研究结论、系统完成度判断和代码事实的前提下，发布 `RAIL-CONTRACT-RP-001` V1.2 格式评审稿，并保留 V1.0、V1.1 历史版本。
- 页面统一为 A4 纵向，上/下页边距 2.5 cm、左 2.6 cm、右 2.4 cm、装订线 0 cm、页眉 1.5 cm、页脚 1.75 cm；正文使用宋体小四、固定 24 磅行距和首行缩进 2 字符。
- 一级标题使用黑体四号，二级标题使用楷体四号，三级及四级标题使用宋体四号；标题编号按“一、→（一）→1．→1.1→1.1.1→（1）→①”设置，当前正文实际使用的一级、二级标题均已转换。
- 本版继续对应分支 `codex/client-feedback-white-shell-20260901`、提交 `51c4095ec269cde31b543dd6c8886802a9a8f452`（提交时间 `2026-09-01T21:19:51+08:00`），未重新取证或修改应用代码、迁移、部署配置和开发数据。
- 最终 DOCX 共 60 页并完成逐页渲染复核；页面参数、22 张表格几何、24 个一级标题、90 个二级标题、6 张行内图、图片替代文本、表头无障碍和压缩包完整性检查通过，无障碍审计高/中/低风险为 0/0/0。样式提示经复核为本版统一格式、封面、表头或编号列表的预期直接格式。

## 2026-09-03：研究报告按技术开发合同（26版）完善

- 以最新版《面向中车出口产品平台的客运装备内装模块化分区快速设计系统及关键技术研究—技术开发合同（26版）》及附件技术规格书为依据，校验源文件 SHA-256 为 `1c0d1e8df8721f6d86f15787dbf881df9542493338af5445bfe3c6f21b540fa3`；合同原件含联系人、账号和财务信息，未复制进仓库，只保留脱敏技术要求矩阵、文件名与校验哈希。
- 发布 `RAIL-CONTRACT-RP-001` V1.1 评审稿并保留 V1.0。V1.1 对齐第一阶段研究报告、2026-09-30 专家评审、第二/第三阶段成果、功能 100% 落地、测试用例通过率不低于 99%、无严重及以上缺陷、三级测试、第三方检测和 7 日整改等合同要求。
- 在系统事实章节之外，增加“四级模块对象—多模态分区识别—参数化方案生成—显式约束过滤—跨区域协同优化—方案评价入库—产品平台接口”的研究内容体系；补充合同与规范分析、系统工程、领域建模、多模态融合、人机协同、快速原型、对照/消融、代码取证和场景验证九类研究方法。
- 新增研究过程与证据链、评价指标和计算口径、E1—E6 六组实验设计、数据采集/抽样/标注方案，以及效度、信度和研究限制；同时扩充规则引擎、区域依赖图、多目标优化、方案检索评价、既有产品平台接口、风险、里程碑和合同要求追踪矩阵。
- 本版继续对应分支 `codex/client-feedback-white-shell-20260901`、提交 `51c4095ec269cde31b543dd6c8886802a9a8f452`（提交时间 `2026-09-01T21:19:51+08:00`），取证时间为 `2026-09-03T14:40:10+08:00`。取证时仅 V1.0 文档、索引和开发日志存在未提交变更，应用代码、迁移和部署配置均未改变。
- 本轮重新执行 `pnpm typecheck:rail` 和 `pnpm test:rail`：类型检查通过，Platform API 26 个文件 87 项、Web 10 个文件 41 项单元测试全部通过，共计 128/128。最终 V1.1 DOCX 共 56 页，逐页渲染复核以及表格几何、标题层级、图片替代文本和表头无障碍检查通过；样式审计提示均为封面、表头或编号列表的预期直接格式。
- 本轮仅完善文档，没有修改应用代码、数据库迁移、部署配置或开发数据；未重跑真实 PostgreSQL/MinIO 集成、浏览器全量验收、生产构建、真实 GPU 推理和中车既有产品平台联调，报告中均按未验证或后续建设项表述。

## 2026-09-02：合同研究报告与版本化交付文档库

- 新建 `docs/rail-platform/contract-deliverables/`，建立按“文档独立目录 + 源稿 + 受控素材 + 交付件 + 生成工具”归档的合同文档库，并增加全库登记表；后续每一版必须记录 Git 分支、完整提交哈希、提交时间、取证时间、工作区状态、验证范围和版本差异。
- 编制 `RAIL-CONTRACT-RP-001`《基于轨道交通客运装备的内装模块分区识别与快速设计系统构建研究报告》V1.0 评审稿，覆盖建设背景、需求与研究方法、31 版数据迁移和 41 张主要业务表、103 个 `/api/v1` 路由、18 项 ComfyUI 工作流、四类设计模式、分区标注与血缘、任务编排、资产协同、权限审计、部署验证、风险和后续研究计划。
- 本版描述代码基线为分支 `codex/client-feedback-white-shell-20260901`、提交 `51c4095ec269cde31b543dd6c8886802a9a8f452`（提交时间 `2026-09-01T21:19:51+08:00`），取证时间 `2026-09-02T16:11:11+08:00`；取证前工作区干净，文档新增内容不反向计入该代码事实基线。
- 明确事实边界：当前分区识别是人工在环的颜色/编号标注、遮罩编辑、原图与标记图双快照、参数物化和任务血缘；仓库尚无独立全自动客室检测、语义分割或实例分割模型，不把规划能力写成已交付。
- 生成可编辑 DOCX、Markdown 源稿、4 张项目内图表和可复现构建脚本。最终 DOCX 共 30 页，已逐页渲染检查，无空白页、裁切、表格越界或标题孤立；表格几何审计通过，无障碍审计高/中/低风险为 0/0/0。
- 本轮重新执行 `pnpm typecheck:rail` 和 `pnpm test:rail`：Platform API 与 Web 类型检查通过，API 26 个文件 87 项、Web 10 个文件 41 项单元测试通过。因本轮仅编制文档，没有修改运行代码或开发数据，未重跑真实服务集成、浏览器全量验收、生产构建或真实 GPU 推理；这些项目仍须在合同验收环境复核。

## 2026-09-01：平台白色一体化外壳

- 按甲方标注取消登录后页面的独立黑色侧栏，固定使用“顶部品牌栏 + 全宽标签栏 + 嵌入式竖向导航”布局；侧栏从 320 px 收窄为约 184 px，以深色文字和品牌红选中态承载原六项平台导航。
- 将红色中车 Logo 与“客运装备内装模块化分区快速设计平台”完整标题迁移到白色顶栏左侧；搜索、设置、通知和用户工具继续右置，当前页面名称由标签栏表达，不在顶栏重复显示面包屑。
- 固定发布外壳会覆盖旧浏览器缓存中的黑色半暗侧栏、宽侧栏和其他布局偏好，同时保留用户的明暗主题选择、桌面侧栏折叠与移动端抽屉能力。
- 根据首轮视觉复核继续消除浅色侧栏的分区感：标签栏改为横跨全宽，侧栏从标签栏下方开始，移除右边框，并让侧栏、菜单和内容区共享同一画布底色。
- 浏览器验收新增顶部品牌归属、全宽标签栏、共享背景、无分隔边框、侧栏几何位置、宽度和六项导航顺序检查。
- 本轮仅修改 Web 固定外壳、验收脚本和交接文档；没有 API、数据库迁移、对象存储、权限、环境变量或外部适配器变更。
- 验证通过：目标文件 Oxlint/Stylelint、Web 与 API 类型检查、API 26 个文件 87 项测试、Web 10 个文件 41 项测试、真实平台/ComfyUI Worker 集成验收及 Nitro/Web 生产构建。真实浏览器确认桌面端 184 px/60 px 展开折叠、390 px 白色导航抽屉、全宽标签栏、共享画布底色和零像素分隔边框；独立登录标签仍捕获到一条既有 Vue Router `Object` 控制台错误，本轮未改动路由逻辑。
- 全仓 `pnpm lint` 仍被本轮开始前已经存在的 6 项 Oxlint 错误阻断，位置为报告 AI 适配器/渲染器和 Web 复制工具；未把无关修复混入本分支。全量浏览器脚本多次通过本轮新增外壳断言，随后分别在既有模型训练内容和报告页返回后的设计输入器位置断言处出现不稳定失败，失败均发生在外壳验收之后。

## 2026-08-25：首页交互反馈与专属能力页面

- 移除首页主视觉右下角的当前项目状态浮层，并为六项快捷入口增加主题色按钮背景，提高主操作识别度。
- 首页最近工作合并为无图片的“最近任务”列表与“最近资产”成果区；最近资产直接使用 `/dashboard` 已有权限过滤结果，覆盖八种文件类型，图片通过受控预览接口显示，文件与 3D 等资产显示集中定义的类型图标。
- 设计历史按当前用户可访问项目聚合，支持项目、任务或设计名称、开始与结束时间筛选。
- 新增 `/model-training` 与 `/report-generation` 专属页面；服务和能力契约未接入时所有执行控件禁用，不创建假任务或假资产。
- 登录成功、登录过期后重新认证和已登录用户访问登录页时均使用平台固定常量进入 `/home`；不再读取旧 `redirect` 查询、接口返回的 `homePath` 或尚未完成初始化的框架默认偏好，避免经兼容路由误入 `/projects`。
- AI 助手发送时立即清空已提交文字，避免等待模型响应期间造成“尚未发送”的误解；提交前失败且用户没有继续输入时恢复原草稿，附件仍沿用既有上传与失败重试流程。
- 本轮没有数据库迁移、环境变量或外部适配器变更。

## 2026-08-13：镜头控制与 ComfyUI Qwen Multiangle 对齐

- 以本机 `ComfyUI-qwenmultiangle` 节点真实源码为准，统一水平、垂直和距离预设：水平 `0/45/90/135/180/225/270/315`，垂直 `-30/0/30/60`，距离 `1/4/8`；同步修正单视角能力的数值范围和步长，仍写入原工作流的真实 `horizontal_angle`、`vertical_angle`、`zoom` 输入，未修改原始 API JSON。
- 镜头控制器增加画布拖拽自由环绕、滚轮距离调整、三类预设下拉、精确滑杆/数值输入、默认值恢复和与 ComfyUI 一致的 `<sks>` 镜头语义提示；所有交互双向更新同一组能力参数。
- 设计会话与旧应用工作区均把当前已选图片的预签名预览地址传入镜头控制器，图片显示在中央被摄平面；切换镜头参数不重新上传或丢失素材。
- 增加镜头预设和提示词阈值单元测试，并扩展浏览器验收覆盖中央图片解码显示、自由拖拽角度回写、三类预设修改及刷新后的图片与草稿恢复。

## 2026-08-08：Linux 开发环境镜像源调整

- 开发 Compose 的 PostgreSQL 改用 `m.daocloud.io/docker.io/library/postgres:17.6-alpine`，避免开发机直连 Docker Hub 超时。
- Mailpit 改用 `m.daocloud.io/docker.io/axllent/mailpit:v1.30.0`，同样避免开发机直连 Docker Hub。
- MinIO、镜像版本、端口、环境变量、健康检查和命名卷保持不变；不修改生产 Compose。
- 开发 Compose 配置解析通过，后续仍通过 `pnpm dev:rail` 一键启动。

## 2026-08-08：核心平台模块分层重构

- 从 `dev` 创建 `refactor-core-module`，以 `0c352a574` 标记未修改的原始基线，并建立独立中文 `REFACTOR_COMMITLOG.md`。
- 新增 `REFACTOR_INVENTORY.md`，明确平台核心、Vben 框架保留区、模板隔离观察区、风险和回归矩阵。
- Web 平台 API 和领域类型按项目、资产、应用、任务、权限、审计通知与 AI 助手拆分，原统一导出路径保持兼容。
- 资产与 AI 附件共用预签名直传辅助函数；删除确认无运行引用的 `modules/platform/data.ts` 静态样例。
- 平台 Store 抽取任务活动状态和时长归一化纯函数，单测入口扩展为全部 `platform*.test.ts`。
- 平台 API 实现按 `domain`、`identity`、`http`、`infrastructure` 分层，根目录同名文件保留为兼容导出层，现有 REST 路径、脚本和 Nitro 自动导入不变。
- 新增 `CapabilityAdapter`、执行状态快照和 `ADAPTER_NOT_CONFIGURED` 稳定常量；没有接入外部执行器，也没有伪造任务成功。
- 本轮没有数据库迁移、环境变量、对象键、部署端口或生产拓扑变化；回滚可重新部署 `0c352a574`，不需要回退数据。
- 阶段验证：API 9 个测试文件 25 项通过；Web Store 2 个测试文件 9 项通过；Web/API 类型检查通过。
- 完整门禁通过：`pnpm lint` 检查 1,943 个文件；Web/API 类型检查通过；API 9 个文件 25 项与 Web 2 个文件 9 项测试通过；Nitro 与 Web 生产构建通过，Web 构建转换 8,070 个模块并生成 `dist.zip`。
- 真实集成通过：认证、权限/项目隔离、文本与图片资产、MinIO 对象存储、AI 会话和审计链路均通过，测试数据由脚本按本轮唯一标识清理。
- 真实浏览器通过：管理员登录、2 个项目、当前项目 2 项资产、6 个应用、动态应用工作区、任务/审计/权限页面和 AI 助手待配置状态均正常；控制台错误为 0，仅保留既有 Vben 表单兼容与存储前缀警告。
- 生产 Compose 使用示例环境完成 `config --quiet` 解析；本轮未重复生产数据备份恢复演练，因为没有迁移或数据结构变化，该项仍是正式上线前门禁。

## 2026-08-04：建立仓库级 AGENTS.md 协作规范

- 在仓库根目录新增 `AGENTS.md`，将本会话确认的产品定位、模块职责、数据流、角色权限、资产文件类型、AI 会话隔离、审计边界和外部适配器原则固化为后续开发规则。
- 明确开发环境与生产部署边界、数据库迁移追加规则、破坏性数据操作限制、完整质量门禁和文档同步要求，避免后续把真实平台重新退化为静态 Demo 或胶水代码。
- 固化 `origin/dev` 维护二次开发、`upstream/main` 跟踪官方基线的 Git 协作方式，并要求提交前核对远程、分支、敏感配置和工作区状态。
- 文档只使用仓库相对路径，不依赖当前电脑的绝对目录；其他机器克隆 `dev` 分支后可以获得相同的 Agent 项目上下文。

## 2026-08-04：资产按文件类型重新划分

- 将原来混合“文件形态”和“业务用途”的 9 类资产收敛为 8 类文件：图片、视频、音频、文本、文档、3D 模型、模型文件和压缩包。
- 遮罩与图片材质转为图片，材质包转为压缩包，LoRA 转为模型文件，报告转为文档；原业务含义写入标签，不会因迁移丢失。
- 新增 `009_file_based_asset_types.sql`，迁移历史数据、更新 6 个应用的输入/输出契约，并为数据库 `assets.kind` 增加 8 类值约束。
- 前端将类型标签、图标和允许扩展名收口到同一模块；资产筛选、登记表单、详情、概览、应用中心与应用工作区使用同一定义。
- API 同时校验 MIME 与关键扩展名，解决了前端允许材质 ZIP、后端却只允许图片的旧不一致问题。
- 迁移前备份为 `/tmp/rail-platform-before-asset-type-migration-20260804.dump`。迁移后数据库类型、约束和应用契约实查正确；API 9 个测试文件共 25 个用例、Web 7 个状态用例、Web/API 类型检查、全仓规范检查和生产构建全部通过。
- 真实浏览器确认资产筛选和登记表单只显示 8 类文件，应用中心与 CMF 工作区已显示新的输入/输出类型；旧的材质、遮罩、LoRA、报告不再出现于文件类型下拉框。

## 2026-08-04：跨平台与 Docker 正式部署闭环

- 新增 `DEPLOYMENT.md`，覆盖 Linux、macOS 和 Windows，明确分为“本地 Node.js + Docker 基础设施”的源码模式，以及“核心进程全部容器化”的单机内网正式模式。
- 明确用户、项目、资产、应用、任务、日志和 AI 聊天是 Web/API 内的业务模块，不是需要分别启动的容器；模型推理、ComfyUI 和 LoRA 训练仍为外部 API 服务。
- 新增多阶段生产镜像、Nginx 同域 API 代理、生产 Compose 和可复制的环境模板。正式数据库与 API 不映射宿主机端口，MinIO 管理台仅绑定 127.0.0.1。
- 生产启动按 PostgreSQL 健康、迁移成功、MinIO 健康、API ready、Web 的顺序执行；数据库迁移镜像仅携带必需依赖，由约 2.91 GB 降至约 521 MB。
- 生产配置校验新增 `CHANGE_ME` 占位符拒绝规则，本地密钥文件同时加入 Git 和 Docker 构建忽略列表。
- 文档包含端口/防火墙、启停、升级、日志、备份、精确恢复、离线镜像转移、架构兼容、故障排查和交付验收清单。
- 使用独立的 18080/19000 端口和全新测试数据卷实际构建并启动正式 Compose；8 个迁移、管理员种子、Web、PostgreSQL、MinIO、API ready、管理员登录和备份命令均通过。测试发现并修复容器中 `localhost` 解析导致健康检查连接被拒的兼容问题。
- 最终验证：生产 Compose 解析通过，Web/API/迁移镜像构建通过，全仓 `pnpm lint` 通过，API 类型检查通过，9 个测试文件共 24 个用例通过。验收后只删除本轮临时生产容器和测试卷，原有开发数据卷保留。

## 2026-08-04：产品工程成熟度审计与 P0/P1 整改

- 将 16,009 行平台二次开发成果固化为 Git 基线 `1d8c62246`，提交前规范、格式、样式和全仓类型检查通过。
- 建立需求编号、非功能基线、开发准入/完成定义、P0/P1/P2 路线图和运维手册，明确已实现、部分实现和未闭环内容。
- 生产启动强制检查数据库、JWT、管理员密码、对象存储和 CORS，禁止开发默认凭据和跨域通配符。
- 将宽泛 CORS 改为来源白名单，添加安全响应头；被拒绝请求返回无堆栈统一错误。
- 新增 `/health/live` 和 `/health/ready`，就绪包含 PostgreSQL 与 MinIO/S3，存储失败后允许恢复重试。
- 新增真实集成验收：三账号、项目隔离、文本资产、图片上传、AI 会话隔离、日志范围和自动清理。
- 新增独立 CI，持续执行规范、类型、单元测试、真实服务集成和生产构建。
- 故障注入验证 MinIO 停止时 ready 返回 503 且标出 storage down，依赖恢复后 API 无需重启自动回到 200。
- 浏览器烟雾验收完成普通用户真实登录、主框架六个业务入口、AI 助手入口和零控制台错误检查。

## 2026-08-04：全局 AI 设计助手与用户私有聊天

### 需求与交互定位

- 以 `frontend-demo/rail-cabin-ai-prototype.html` 的右下角助手为参考，但将其改造为登录后的全局平台组件，不属于某个业务路由。
- 视觉使用企业红、石墨、冷灰和白色，新增轨道节点顶线和信号灯式悬浮入口，保留平台自身品牌语言，没有直接复制原型。
- 桌面端为 430 px 宽右下弹窗，移动端自适应为近全屏；支持键盘焦点、Enter 发送、Shift + Enter 换行、Escape 关闭与减少动效偏好。
- 聊天窗口提供历史对话、新对话、项目上下文、建议问题、文本、图片/音视频/文档附件、图片预览、文件下载、清空与外部服务状态。

### 数据层与 API

- 新增迁移 `008_ai_assistant.sql`，建立 `ai_conversations`、`ai_messages`、`ai_attachments` 三张表及用户/会话时间索引。对话可选关联项目，消息保留 completed/failed 状态、稳定错误码和可选上游消息编号。
- 实现 `/api/v1/assistant/status`、`conversations/**`、`attachments/**` 接口，覆盖对话列表、创建、读取、发送、清空，以及附件申请上传、完成校验、删除、预览和下载。
- 附件沿用私有 MinIO/S3 桶与预签名直传，对象键使用 `assistant/<user>/<conversation>/...` 前缀。默认单附件 50 MiB，支持常见图片、音频、视频、文本、PDF、Office 和 ZIP，拒绝可执行文件与 SVG。
- 清空当前对话会在事务中删除消息与附件关系并重置标题，再逐个清理对象文件；对象删除失败会留下审计元数据。

### 外部 AI 适配器

- 通过 `AI_ASSISTANT_API_URL`、`AI_ASSISTANT_API_KEY`、`AI_ASSISTANT_MODEL`、`AI_ASSISTANT_TIMEOUT_MS` 和 `AI_ASSISTANT_MAX_ATTACHMENT_BYTES` 配置，浏览器不获得服务密钥。
- 平台后端只向完整 API 地址发送当前用户、可选项目、对话编号、系统提示、最近 50 条已完成消息和附件短时签名 URL。接受 `{ content, messageId? }` 或 `{ data: { content, messageId? } }` 响应。
- 外部服务未配置、超时、限流、返回非 2xx 或格式无效时，保存 failed 助手消息与错误码，不伪造成功回复，也不回滚已经保存的用户消息。

### 隔离、审计与验收

- 每个对话、消息、上传、预览、下载和清空查询都在 SQL 中强制当前 `user_id`。管理员也只能查看自己的聊天正文；管理员可在操作日志中看到他人的动作类型和结果，不能看到聊天内容。
- 业务审计覆盖对话创建、消息发送、附件上传/删除与清空。经数据库实查，发送日志仅含 `contentLength`、`attachmentCount`、`providerResult`、`errorCode` 等元数据，不含聊天正文。
- 真实 API 联调完成了用户 1 创建对话、PNG 上传、MinIO 完成校验、消息持久化、图片预览与清空。用户 2 直接读取用户 1 的对话返回 404，历史列表也不包含该对话。清空后消息数为 0，原附件预览返回 404。
- 真实浏览器验收了全局入口、弹窗视觉、发送、“服务待接入”失败状态、页面刷新后恢复历史、清空确认与切换用户后的历史隔离。用户 2 弹窗的历史选项只有“新对话”，页面没有新增 JavaScript 错误。
- 迁移 `008_ai_assistant.sql` 已实际应用。`pnpm lint`、`pnpm typecheck:rail`、`pnpm test:rail` 和 `pnpm build:rail` 全部通过；平台 API 7 个测试文件、19 个用例通过，Web 状态 7 个用例通过，Nitro 与 Web 生产包均成功构建。

## 2026-08-04：两角色账号重建与全量操作日志

### 角色与账号收敛

- 平台角色正式收敛为 `admin` 和 `user` 两种；原 `super` 的账号和权限迁移并入 `admin`，数据库约束禁止再创建其他角色代码。
- 每个账号必须且只能拥有一个角色；管理员不能修改自身角色或状态，系统不能降级或停用最后一名启用管理员。
- 迁移 `006_two_role_model.sql` 负责兼容已有数据库，初始迁移和种子脚本也同步为两角色模型。
- 在执行账号删除前创建 PostgreSQL 自定义格式备份；本机验证备份为 `/tmp/rail-platform-before-user-reset-20260804.dump`。其他机器重建前必须生成自己的备份，命令见复现指南。
- 使用带显式确认参数的 `reset-users.ts` 删除原四个账号并创建 `admin`、`user1`、`user2`。项目、资产、版本和任务归属在同一事务中转移给新管理员，没有产生孤儿记录。
- 账号重建脚本及默认账号均由环境变量配置，可在其他机器复现；维护动作写入 `system.users.reset` 审计事件。

### 全量操作日志

- 迁移 `007_comprehensive_audit.sql` 为日志增加操作者用户名、姓名、角色快照，以及 HTTP 方法、路径、状态码、耗时和 User-Agent，并补充操作者、模块和请求路径索引。
- 所有 Nitro API 都经过统一响应层；每个已认证请求无论成功或失败都会写入 `api.request`，业务写接口继续写入更明确的领域动作。
- 退出接口会先通过 HttpOnly 刷新 Cookie 反查会话用户再撤销会话，因此即使请求没有 Bearer 令牌，也会同时留下可归属用户的 `auth.logout` 和 API 请求日志。
- 登录、注册、密码重置、退出和系统维护保留独立事件；Vue 路由切换通过 `/audit-events/client` 写入 `page.view`。
- 操作日志从管理员子路由调整为所有登录用户可访问的 `/audit`。后端强制 `admin` 可读全部、`user` 仅可读自身，普通用户提供其他 `actorId` 也会被忽略。
- 日志页支持模块、操作者、关键字筛选和服务端分页。操作者显示姓名、`@用户名` 和角色快照，因此多个管理员及发生过角色变化的账号可以明确区分。
- 日志不保存密码、令牌、完整请求体、键盘输入或文件正文；“每一步操作”的审计边界为页面访问、API 请求和业务动作，而非原始鼠标与键盘遥测。

### 数据和接口验收

- 数据库最终为 3 个账号、2 个角色；保留 2 个项目、4 个资产和 4 个资产版本，项目/资产/版本孤儿数均为 0。
- 真实接口验证管理员可读取 `admin`、`user1`、`user2` 三名操作者的日志，普通用户返回范围为 `self` 且每条 `actor_id` 都等于自身。
- 临时将 `user2` 提升为第二名管理员，确认其可读取全部日志且留下 `admin` 角色快照；随后恢复为 `user`，最终账号结构仍为一个管理员和两个普通用户。
- 新增两角色和日志可见范围单元测试。平台 API 5 个测试文件、14 个用例全部通过，Web 平台状态 7 个用例全部通过。
- `pnpm lint`、`pnpm typecheck:rail`、`pnpm build:rail` 和 `pnpm install --frozen-lockfile` 全部通过；迁移与种子脚本重复运行无错误。
- 真实浏览器确认管理员页面显示 3 个账号和 2 张角色卡；管理员日志同时显示 `admin`、`user1`、`user2` 及第二管理员历史快照，普通用户日志页没有账号筛选器且只显示 `@user1`。
- 浏览器验收发现日志总数与并发写入的列表曾使用两个数据库快照，短暂显示“当前页 21 条，共 20 条”；改为同一 SQL 使用窗口计数后复验为“当前页 28 条，共 28 条”。

## 2026-08-04：企业邮箱与密码重置闭环

根据国企内网通常使用自有邮件系统的场景，邮箱保留为正式账号字段，并完成可替换企业 SMTP 的真实找回密码链路：

- 数据库迁移 `005_email_password_reset.sql` 为用户增加唯一企业邮箱，并新增一次性密码重置令牌表。
- 注册和个人基本资料接入真实企业邮箱；管理员用户列表可查询和显示邮箱。
- 忘记密码页调用真实申请接口，新建重置密码页完成新密码设置，不再只是无动作的表单。
- 平台 API 通过 Nodemailer 连接 SMTP；开发环境由 Docker Compose 中的 Mailpit 接收邮件，生产环境通过 `SMTP_*` 环境变量替换为企业邮件系统。
- 重置令牌使用密码学安全随机数，数据库只保存 SHA-256 摘要，默认 30 分钟有效且只能使用一次。
- 申请接口使用统一响应避免枚举账号，并增加 IP/账号频率限制；成功重置后撤销该用户全部刷新会话。
- 个人中心恢复“安全设置”入口，但仅保留真实企业邮箱绑定状态；不恢复手机、密保问题、MFA 或虚假备用邮箱。
- 新增密码重置工具单元测试；平台 API 9 个用例、Web 平台状态 6 个用例全部通过，冻结锁依赖安装、全仓格式、类型检查和生产构建通过。
- 真实浏览器完成“邮箱注册 → 申请重置 → Mailpit 收信 → 设置新密码 → 新密码登录”闭环，并确认同一链接二次使用会被拒绝。
- 真实浏览器确认个人中心恢复“安全设置”，该页只显示当前账号的企业邮箱绑定状态。

## 2026-08-04：当前系统架构盘点

根据当前代码、数据库迁移和 Docker 配置重新梳理系统架构，避免继续沿用第一版演示阶段的理解：

- 明确当前运行结构为 Vue/Vben Web、独立 Nitro API、PostgreSQL 和 MinIO 四层，Docker Compose 当前只承载数据库和对象存储。
- 将前端页面、平台 API、数据表、对象存储、认证、RBAC 和项目数据范围逐一映射到对应代码目录。
- 补充总体架构、开发部署、固定页面区域、数据模型、登录刷新、项目切换、资产上传和应用任务等架构图与数据流图。
- 明确 ComfyUI、大模型、LoRA、报告生成等尚未接入；当前任务接口只保存真实台账，未配置适配器时返回 `ADAPTER_NOT_CONFIGURED`。
- 记录项目成员管理、任务 Worker、输出回流、应用管理、资产版本前端、生产部署与可观测性等当前缺口。
- 标记 `apps/web-antd/src/modules/platform/data.ts` 为未被运行链路引用的早期静态样例，防止后续误认为业务数据源。

完整说明见 `docs/rail-platform/ARCHITECTURE.md`。

## 2026-08-04：真实图片预览与个人中心生产化

### 图片资产显示真实内容

资产卡片原先只根据类型显示统一的色块和图标，无法直接辨认已上传图片。本轮新增受权限保护的图片预览链路：

- 平台 API 新增 `GET /api/v1/assets/:id/preview`，验证登录身份、资产读取权限、项目数据范围、资产状态和图片 MIME 类型。
- MinIO/S3 仍保持私有桶；接口返回短时有效的内联预签名地址，不公开对象、不把二进制写入数据库。
- 图片、遮罩和图片材质在资产卡片中懒加载真实缩略图；详情抽屉以完整比例展示图片。
- 加载中和加载失败都有明确回退状态，非图片资产继续显示各自的类型图标。
- 预览地址接口与对象读取均完成真实联调，返回状态分别为成功和 HTTP 200。

### 个人资料与权限边界

个人中心原先沿用上游演示数据：普通用户可以编辑角色，“更新基本信息”没有提交处理，“安全设置”和修改密码均为假功能。本轮完成以下调整：

- 删除“安全设置”标签页及手机、邮箱、MFA 等与当前用户名密码登录模式不符的演示内容。
- 登录用户名和平台角色在个人资料页只读；姓名、部门、个人简介通过 `PATCH /api/v1/user/profile` 持久化到 PostgreSQL。
- 新增迁移 `004_user_profile.sql`，保存个人简介和站内消息提醒偏好。
- “更新基本信息”增加真实提交、加载状态、成功提示，并同步刷新顶栏当前用户信息。
- “修改密码”接入真实校验与 scrypt 重新散列；成功后撤销该用户全部刷新会话并要求重新登录。
- “新消息提醒”接入 PostgreSQL 偏好读写，不再是只改变视觉状态的演示开关。

### 管理员角色调整

- 新增管理员专用 `PUT /api/v1/users/:id/roles` 接口，后端强制要求 `platform:role:write` 权限。
- 普通用户调用该接口会返回 `FORBIDDEN`；管理员只能修改其他用户，不能修改自身角色。
- 只有超级管理员可以授予或移除超级管理员角色，且平台始终至少保留一名可用超级管理员。
- “用户与权限”页面新增真实角色调整弹窗；当前用户的角色按钮禁用，角色定义卡片移除无效的省略号按钮。

### 验证结果

- 数据库迁移 `004_user_profile.sql` 已成功应用。
- API 联调：资料更新、通知偏好、管理员角色修改、图片预览均成功；普通用户角色修改被正确拒绝。
- 平台 API 新增资产工具与密码散列单元测试，共 6 个用例通过。
- Web 平台状态测试扩展为 6 个用例，包含管理员角色更新状态同步，全部通过。
- 全仓 `pnpm lint` 通过，包括格式、Oxlint、ESLint 与 Stylelint。
- `pnpm typecheck:rail` 通过，Web 与 API 均无类型错误。
- `pnpm test:rail` 通过：平台 API 6/6，Web 平台状态 6/6。
- `pnpm build:rail` 通过：Nitro API 与 Web 生产包均成功生成，Web 构建产物为 `apps/web-antd/dist.zip`。
- 真实浏览器验证了图片卡片与详情预览、个人资料保存、安全设置移除、修改密码表单、消息偏好和管理员角色弹窗。

## 2026-08-04：资产上传入口可见性优化

浏览器验收发现，登记资产弹窗直接使用操作系统原生文件输入框，“选择文件”按钮面积小、视觉层级弱，用户不容易识别为主要操作。

处理内容：

- 将原生按钮收纳为无障碍文件输入控件，界面改为整块可点击的上传区域。
- 使用平台企业红、上传图标和“点击选择文件”动作强化入口，同时保留清晰的键盘焦点反馈。
- 文件选中后切换为“文件已选择”状态，展示文件名、格式化后的大小以及“重新选择”入口。
- 按资产类型限制文件选择范围；切换资产类型或关闭弹窗时清理旧文件状态，防止误传不匹配文件。
- 完成 Web 类型检查、目标文件格式检查、ESLint、Stylelint 和真实浏览器视觉验收。

## 2026-08-04：登录与注册交互修复

### 注册协议状态修复

浏览器验收发现：注册页的“我同意隐私政策与条款”在视觉上已经勾选，但提交时仍提示必须同意。

根因是表单组件映射将内置 `VbenCheckbox` 绑定到 `checked`，而该组件实际通过 Vue 的默认 `modelValue` 协议传值。点击只改变了复选框自身的视觉状态，表单字段没有收到新值。

处理内容：

- 将内置复选框的表单协议统一为 `modelValue`。
- 新增表单集成回归测试，验证点击前协议校验失败、点击后表单值变为 `true` 且校验通过。
- 表单集成测试共 27 个用例，全部通过。

### 内网登录方式收敛

根据公司内网使用场景，登录页已收敛为用户名密码登录：

- 删除用于演示的角色/账号快捷选择器，用户必须输入自己的用户名和密码。
- 关闭手机号登录与扫码登录入口。
- 关闭微信、QQ、GitHub、Google 和钉钉等第三方登录区域。
- 保留滑块验证、记住账号、忘记密码和自助注册入口；自助注册可在后续改为仅管理员创建账号。

本轮验证结果：全仓 `pnpm lint` 通过，Web 应用类型检查通过，平台状态测试 4/4 通过，表单集成测试 27/27 通过，生产构建通过。验收浏览器当时已有登录态，访问登录与注册路由会被权限守卫重定向到工作台，因此没有强制退出当前用户；未登录页面可在退出登录或无痕窗口中复核。

## 2026-08-03：平台框架第一版

### 目标转译

将口头需求整理为两个边界清晰的系统：

- 平台系统：用户、权限、登录、项目、资产、任务、审计和稳定的 Web 外壳。
- 能力系统：ComfyUI 工作流、大模型、LoRA 训练等第三方接入。

本次只实现第一部分，并为第二部分留下统一应用契约、工作区和资产回流契约。

### 1. 基线审计

- 确认上游为 Vben Admin 5.7.0。
- 记录起点提交 `9f5b1cd9fbc7ee1747491a99317a6239634b68d3`。
- 检查了 monorepo 结构、Ant Design Vue 应用、前端访问模式、Nitro Mock、路由和布局扩展点。
- 使用仓库要求的 Node.js 24 兼容环境和 pnpm 11 安装依赖。
- 开发前执行 `@vben/web-antd` 类型检查，原始基线通过。

### 2. 视觉与布局方向

- 建立“轨道工程控制台”语言：深石墨与冷灰为主，企业红仅用于关键操作、轨迹和状态。
- 平台概览的标志性视觉是“资产流转轨道”，表达不同能力之间共享资产的核心逻辑。
- 保留 Vben 的响应式外壳、标签页、权限和偏好设施，替换与产品无关的演示导航。

### 3. 域模型与状态

新增：

- `apps/web-antd/src/modules/platform/types.ts`
- `apps/web-antd/src/modules/platform/data.ts`
- `apps/web-antd/src/store/platform.ts`
- `apps/web-antd/src/store/platform.test.ts`

实现项目、资产、应用、任务、用户、角色和审计七类核心模型，并实现：

- 切换和创建项目。
- 登记资产与收藏资产。
- 启停用户。
- 模拟执行外部应用。
- 任务完成后生成输出资产，并把任务指向该资产。
- 对当前项目、资产和任务进行本地持久化。
- 对项目列表进行同步持久化，并在旧缓存指向不存在项目时自动回退到默认项目。

### 4. 路由与页面

用 `platform.ts` 替换原始 `dashboard.ts`、`demos.ts` 和 `vben.ts` 演示路由，实现：

- 平台概览。
- 项目空间。
- 资产中心。
- 应用中心。
- 任务中心。
- 用户与权限。
- 审计日志。
- 通用动态应用工作区。

“平台管理”路由仅向 `admin` 和 `super` 权限开放。

### 5. 品牌与框架定制

- 新增本地 `rail-logo.svg`，不依赖外部图片。
- 应用标题改为“轨道客室智能设计平台”。
- 修改 namespace，与上游演示缓存隔离。
- 登录演示账号、Mock 用户姓名、主页和中文文案均已品牌化。
- 顶栏新增项目切换，用户菜单显示当前项目。
- 通知改为项目与任务相关的本地演示内容。
- 移除了产品中的上游文档和 GitHub 跳转入口。

### 6. 兼容修复

在 macOS x64 环境执行生产构建时，`sass-embedded` 1.100.0 附带的 Dart 3.12.0 子进程在 CPU 信息检测阶段崩溃。为避免依赖特定机器的临时处理：

- Vite 全局 SCSS 导入器改为从 `sass` 引入。
- 默认依赖 catalog 将 `sass-embedded` 包名映射到 `sass@1.101.6`，Web 应用继续使用仓库统一的 `catalog:` 声明。
- 修改写入 `package.json` 和 `pnpm-lock.yaml`，新机器执行正常安装即可获得同样结果。
- 使用 pnpm 11.16.0 执行 `install --frozen-lockfile`，完整依赖重装与安装后脚本均通过。

首次尝试纯离线重装时，本机 pnpm store 缺少 `sortablejs` 包体，因此离线安装中止；随后允许访问包仓库后冻结锁安装成功。该过程说明新机器首次安装需要网络，或预先准备完整的 pnpm store，并非锁文件失效。

全仓格式检查曾发现 Vue 模板格式器与 ESLint 对内联元素空白的处理不一致。已在 `oxfmt.config.ts` 中仅对本次新增的平台组件和页面设置 `htmlWhitespaceSensitivity: 'ignore'`，自动格式化、ESLint 与渲染结构现可同时保持一致。全仓 Oxlint 进一步发现的非空断言和嵌套三元表达式也已改为显式兜底与资产格式映射。

### 7. 验证记录

| 检查 | 结果 | 备注 |
| --- | --- | --- |
| 开发前类型基线 | 通过 | 确认上游代码可工作 |
| pnpm 冻结锁安装 | 通过 | pnpm 11.16.0，46 个工作区项目 |
| 全仓库 `pnpm lint` | 通过 | 格式、Oxlint、ESLint、Stylelint |
| 二开后 Vue/TypeScript 类型检查 | 通过 | 无类型错误 |
| Pinia 状态单元测试 | 通过 | 1 个文件、4 个用例 |
| 生产构建 | 通过 | 8,054 个模块，构建约 6 秒 |
| 真实浏览器主流程 | 通过 | 登录态恢复、项目切换/创建/刷新、工作区、任务、资产回流、用户角色与审计 |

真实浏览器验收期间发现“新建项目刷新后丢失，顶栏显示原始 ID”的持久化缺陷。已通过“项目列表纳入持久化 + `afterHydrate` 旧数据自愈”修复，并在同一浏览器内重新创建项目后刷新验证通过。页面运行期无 JavaScript 错误；开发控制台仅有 Vben 上游表单兼容和存储前缀提示。

### 8. 复现原则

- 所有必要修改都在仓库内，不依赖用户主目录的隐藏配置。
- 依赖版本由 `package.json`、`pnpm-workspace.yaml` 和 `pnpm-lock.yaml` 共同锁定。
- 设计决策、目录边界、故障和验证结果均记录在 `docs/rail-platform`。
- 下一阶段接入真实 API 时，应继续追加本日志，而不要删除历史。

## 2026-08-08：仓库产品化精简与身份统一

- 删除 5 套替代 Web、旧 Nitro Mock、Playground、Vben 文档站、Changesets 和上游 GitHub 发布设施。
- 删除实际 Web 与共享包中的 Dashboard/Demos、About、验证码/二维码/第三方登录、样例安全设置、无入口错误页和专属资源。
- 删除无平台引用的 ECharts、Tiptap、VXE Table、Captcha、CountTo、JsonViewer 和替代 UI 样式，保留实际使用的表单、布局、权限、请求、状态、图片裁剪和动效。
- 活动 Web 包改为 `@rail/web` 0.1.0；根包、HTML、PWA、运行时配置变量、默认偏好、版权头、VS Code 和 GitHub 协作文件统一为平台身份。
- 根 README 改为中文项目入口，新增 `REPOSITORY_STRUCTURE.md`，记录完整 `tree` 审计口径和所有保留区域用途。
- 清理本机绝对路径、旧克隆目录名和过期“隔离观察”描述。

验证：`pnpm lint`、`pnpm typecheck:rail`、`pnpm test:rail`、`pnpm build:rail` 已通过；集成和浏览器验收在最终门禁阶段记录。

## 2026-08-08：运行时品牌残留收口与最终验收

- 修复内部 Vite 配置包仅引用历史 `dist` 产物的问题：`build:rail`、`dev:rail` 和 `dev:rail:web` 在执行前统一生成当前源码产物，避免已删除的 Vben 文案继续出现在开发终端。
- 删除内部 Vite 配置中仅服务旧文档站、Mock 和懒加载示例的依赖，锁文件同步收敛。
- 真实集成验收通过，覆盖认证、权限隔离、项目、文本/图片资产、对象存储、AI 会话和审计；本机代理对 `localhost` 的干扰通过 `NO_PROXY/no_proxy` 排除，未修改业务代码规避环境问题。
- 真实浏览器验收通过：管理员进入 `/workspace/overview`，页面只呈现轨道客室智能设计平台、项目、资产、应用、任务和审计入口，浏览器控制台无错误，未发现旧 Vben 品牌文案。

## 2026-08-08：ComfyUI 工作流 API 第二阶段

### 需求

完整实现工作流注册、管理员管理、能力映射、独立持久化 Worker 和文生图端到端；本期只正式发布一个文生图能力，但架构支持后续导入其他工作流。由于本机没有 ComfyUI，使用临时模拟服务测试，运行时不得伪造成功。

### 实现

- 新增 `010_comfyui_workflows.sql`，建立工作流版本、能力绑定、执行租约、输出回执和 Worker 心跳模型。
- 纳入已在其他电脑验证的 Flux2 Klein 文生图 API JSON，并幂等登记首发版本与 `text-to-image` 绑定。
- 新增通用参数/输出映射校验、ComfyUI HTTP 适配器、独立 Worker、取消、轮询重试、崩溃恢复和资产登记。
- 新增管理员工作流管理 API/页面与能力驱动的文生图工作区。
- 开发启动和生产 Compose 均增加独立 Worker。
- 新增详细架构说明与中文独立变更记录。

### 验证计划与环境限制

- 自动化覆盖参数映射、配置、客户端、输出映射和临时模拟 ComfyUI 协议。
- 执行仓库质量门禁、真实 PostgreSQL/MinIO 集成和浏览器手工流程。
- 本机无法验证真实 GPU 推理；真实 ComfyUI、模型和自定义节点验收保留为部署前阻断项。

### 最终验证结果

- `pnpm lint`：通过。
- `pnpm typecheck:rail`：通过。
- `pnpm test:rail`：后端 33 项、前端 9 项通过。
- `pnpm test:rail:integration`：平台基线集成与模拟 ComfyUI Worker 真实 PostgreSQL/MinIO/资产登记均通过。
- `pnpm build:rail`：Nitro 与 Web 生产构建通过。
- 浏览器：管理员管理、Worker 心跳、文生图参数、未配置服务稳定失败和移动端无横向溢出通过。
- 未验证：真实 ComfyUI GPU 推理、模型与自定义节点安装。

## 2026-08-08：ComfyUI 18 项工作流目录接入与工作区重设计

### 需求

- 将 `/Users/huyanwei/projects/ComfyUI/user/default/workflows_api` 中除首发文生图外的所有 17 份 API JSON 按同一标准接入。
- 改造输入资产、文本和 3D 输出链路。
- 使用临时模拟 ComfyUI 验证全目录协议。
- 重设计能力工作区，为图片、多图、遮罩、分区、屏幕捕获、文本和 3D 提供对应交互。
- 编写真实 ComfyUI 启动后的快速联调和适配微调交接文档。

### 实现

- 新增 `011_comfyui_workflow_catalog.sql`，发布 17 项新应用与能力，总数达到 18 项。
- 新增集中能力目录，显式记录业务参数、资产位、ComfyUI 注入目标、输出字段和模型需求。
- Worker 新增 MinIO 输入读取、`/upload/image`、Data URL 注入、PreviewAny 文本资产和 SaveGLB 3D 资产。
- 能力工作区重构为轨道设计控制台，增加项目资产编组、图像导入、屏幕捕获、PNG Alpha 遮罩和 EasyMark 分区笔画。
- 工作流校验和改为完整版本快照，参数/输出/模型微调也会创建新版本。
- 新增真实服务节点预检脚本和 `COMFYUI_LIVE_SERVICE_HANDOFF.md`。

### 验证边界

- 本机 ComfyUI 尚未启动，不将模拟协议通过记为真实 GPU 推理通过。
- 真实服务启动后必须先执行节点预检，再按交接文档的 18 项矩阵验收。

## 2026-08-08：ComfyUI 全工作流目录、通用输入适配与工作区升级

- 将外部 `workflows_api` 目录中的 18 份 API JSON 固化到平台工作流目录，其中 17 项为本次新增，原“文生图”能力继续按同一目录标准管理。
- 新增集中式工作流目录、迁移 `011_comfyui_workflow_catalog.sql`、通用标量/资产/截图/遮罩/区域参数映射和图片/文本/GLB 输出映射。
- Worker 现在可以按有序任务输入从对象存储读取资产、上传到 ComfyUI、注入不可变工作流版本并登记输出资产。
- 工作区改为按能力契约动态渲染的设计控制台，支持项目资产、文件导入、屏幕捕获、遮罩绘制和区域标注。
- 已应用迁移并完成种子初始化，数据库登记 18 项能力。
- 模拟验证结果：`14` 个测试文件、`38` 项测试全部通过；逐项覆盖全部 18 份 API JSON。
- 环境限制：本机没有启动真实 ComfyUI，节点、模型、显存、生成质量和少数自定义节点输出字段仍须按 `COMFYUI_LIVE_SERVICE_HANDOFF.md` 做上线前预检与微调。

## 2026-08-08：AI 设计助手接入 GeekAI

- 将助手外部请求从平台自定义包装体切换为 GeekAI / OpenAI Chat Completions 兼容协议。
- 默认模型改为 `deepseek-v4-flash-0731`，服务地址和 Key 仍只从 API 环境变量读取。
- 会话、消息和附件继续按 `user_id` 强制隔离，且提供商历史查询增加二次用户约束。
- 上游请求不再发送真实姓名和用户名；审计只保留字符数、附件数、结果和稳定错误码。
- 图片和视频附件按官方多模态结构传递短时签名地址；不支持直读的文档明确告知模型正文未传递。
- 本次不使用用户在对话中暴露的 Key，需轮换后再按 `AI_ASSISTANT_GEEKAI_INTEGRATION.md` 做真实服务验收。

### 2026-08-08 GeekAI 本机真实部署与验收

- AI 助手模拟测试通过：`14` 个测试文件、`41` 项测试。
- 本机 API 已读取 GeekAI 配置，页面正确显示提供商和 `deepseek-v4-flash-0731` 模型。
- 已通过平台页面完成真实文本问答，GeekAI 返回有效中文回复。
- 刷新页面后会话与双方消息仍存在，证明请求、响应和 PostgreSQL 持久化闭环通过。
- 未将真实 Key 写入任何受版本控制文件；当前 Key 已在对话中暴露，后续必须轮换。

## 2026-08-08：ComfyUI 真实服务第一批适配

- 本机 HTTPS ComfyUI 0.27.0 已接入，使用 CA 证书完成严格 TLS 校验，不关闭证书验证。
- 新增跨平台环境启动器，先加载 `apps/platform-api/.env` 再创建 Node.js 子进程，解决 `NODE_EXTRA_CA_CERTS` 在进程启动后加载不生效的问题；API、Worker、迁移、种子和预检统一使用该入口。
- 本机 `.env` 已从 Git 跟踪中移除并加入忽略规则，配置模板与部署文档新增 HTTPS CA 说明。
- 真实服务预检从“仅检查节点类型”扩展为同时检查静态枚举值，并将只存在于前端控件的附加字段作为兼容性提示输出；18 项能力全部通过阻断项预检。
- 第一批真实推理通过：文生图、单图设计编辑、文本生成、多视图生三维，输出分别登记为项目图片、文本和 GLB 资产。
- 多视图生三维在极低测试参数下产生空网格，平台正确返回 `COMFYUI_OUTPUT_MISSING`；使用工作流默认 `20 steps / 3072` 后成功登记约 24.4 MiB GLB。同步移除 `SaveGLB` 已废弃的 `image` 输入。
- 第二批 8 项真实推理通过：LoRA、多图融合、图片理解、放大修复、单视角镜头控制、多视角镜头生成、Base 双图编辑和 KV 双图编辑；多视角镜头一次登记 5 张图片。
- 第三批屏幕捕获、单图/参考图遮罩、智能扩图直接通过；两项 EasyMark 分区首次被真实服务拒绝，原因是当前 `sum_stack_flux2_Klein` 要求显式 `context`。
- 修复两项分区工作流：移除 `Anything Everywhere` 隐式广播并直接连接 `sum_load_adv` 上下文；编号分区的 `SaveImage` 改为保存采样器生成图而非缩放原图；移除旧前端预览节点和过期尺寸字段。
- 最终结果：18/18 项能力均完成真实 GPU 推理和项目资产回写。验证覆盖单/多资产上传、Data URL、Alpha 遮罩、EasyMark 笔画、批量图片、内联文本、GLB、长队列轮询和 Worker 租约。
- 完整门禁通过：`pnpm lint`、`pnpm typecheck:rail`、42 项 Platform API 与 9 项 Web 单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 集成验收和 `pnpm build:rail`。

## 2026-08-08：应用中心筛选、可见性管理与卡片修复

- 修复应用卡片短名称水印与状态徽标重叠：水印移到卡片头部下方并限制宽度，状态与隐藏徽标保持独立层级。
- 应用中心增加名称/描述/服务方搜索、应用状态筛选，并保留类别筛选；管理员额外获得用户可见性筛选。
- 新增 `012_application_visibility.sql`：`applications.visible` 默认启用，新增仅管理员拥有的 `platform:application:write` 权限。
- 新增管理员应用可见性 API 和审计事件 `application.visibility.update`。普通用户的应用列表不返回隐藏应用，并且直接请求隐藏能力详情或创建任务同样返回 404；管理员仍能查看、筛选和进入隐藏应用。
- 前端管理员可在每张卡片持久化切换“用户可见”，隐藏卡片使用独立视觉状态；筛选无结果时显示明确空状态。
- 验证：API/Web 类型检查通过；Platform API 42 项、Web 10 项单元测试通过；真实 PostgreSQL/MinIO 集成验收覆盖管理员隐藏/恢复、普通用户列表隔离、写权限拒绝、能力详情和任务创建防绕过。
- 浏览器：1000×1000 管理员页面共 24 张卡片，状态徽标与短名称水印边界交叠为 0；名称搜索、状态筛选、可见性开关及恢复通过，控制台无错误。

## 2026-08-08：工作流可视化交互、结果暂存与跨能力流转

- 保持 `apps/platform-api/workflows/comfyui/` 内 18 份原始 API JSON 不变，只调整平台参数渲染、输入组件和结果生命周期。
- 工作区默认展示基础业务参数并收纳高级参数，补充参数总数、说明、数值范围、默认值和高级下拉项。
- 图片输入明确拆分为“从项目资产选择”和“本地导入”；新增资产缩略图选择器和全屏缩放预览。
- 遮罩与分区重绘升级为全屏 Canvas 编辑器，支持画笔、橡皮、颜色、粗细、撤销、重做、清除和缩放；实时捕捉支持屏幕/摄像头预览与可视化选区裁剪。
- 新增 `013_workflow_output_staging.sql`。Worker 输出默认 `saved_at = NULL`，仅在任务结果区可见；用户点击保存后才进入资产中心。
- 任务接口返回项目范围内的全部暂存输出；结果可下载、放大或手动加入资产。未加入资产时后端不允许当作其他工作流输入。
- 资产中心新增后端强制项目写权限的软删除接口、对象清理和 `asset.delete` 审计；历史任务外键与血缘记录继续保留。
- 自动化验收覆盖暂存结果不进入资产列表、任务接口可读取、确认保存后进入列表、删除后不可访问，以及 Worker 输出保持暂存状态。

## 2026-08-10：Linux 本地开发环境管理脚本

- 根目录新增 `rail-platform.sh`，统一提供 `check_env`、`start`、`status`、`stop`、`restart`、`logs` 和 `help`。
- 新增 `.nvmrc` 固定 Node.js `24.16.0`；脚本自动加载 NVM，并校验项目允许的 Node 版本和锁定的 pnpm `11.16.0`。
- `start` 在后台复用 `pnpm dev:rail`，保留原有 Compose、迁移、种子、API、Worker 和 Web 启动链路；PID 和日志写入 Git 忽略的 `.rail-platform-runtime/`。
- 每次 `start/restart` 生成带年月日时分秒的独立日志 `dev-YYYYMMDD-HHMMSS.log`，文件头记录完整启动时间和时区；稳定入口 `dev.log` 始终指向本次日志，历史日志不再被覆盖。
- `status` 同时检查受管进程、Compose 服务、Web/API HTTP 健康以及 MinIO/Mailpit 端口。
- `stop` 使用进程组停止 Node 开发服务并执行不带 `-v` 的 Compose down，保留 PostgreSQL 和 MinIO 数据；`--keep-infra` 可只停止 Node 进程。

## 2026-08-09：工作区状态、完整参数、资产预览与精确流转

- 执行链路横线默认保持静态，仅当当前应用存在 `queued`、`running` 或 `cancelling` 任务时显示运行动效，减少错误的“正在工作”感知。
- 动态应用页签使用应用/能力名称；重新进入应用时不恢复旧的已完成结果，但仍恢复后台正在运行的任务。
- 新增参数同步器与 `parameter-schemas.generated.json`，根据真实 ComfyUI `/object_info` 为 18 份 API JSON 补齐每个非连线输入；完整性测试强制未映射数为 0。
- `ScreenShare` 实现与参考 ComfyUI webUI 一致的屏幕/摄像头、拖拽选区、裁剪实时预览、`refresh_rate`、画面差异检测和串行实时任务；停止时关闭媒体轨道并取消本页活跃任务。
- 资产预览 API 按项目权限返回内联文本或图片/视频/音频/PDF 短时地址；资产中心支持图片放大、文本阅读与媒体播放。
- 新增 `014_workflow_asset_transfers.sql`。流转记录按用户、项目、目标工作流和输入位持久化；多输入工作流中用户必须明确选择“基础空间”、“样式参考”或“材质参考”等语义输入，刷新后仍回填到同一位置，任务创建后标记为已消费。
- 后端同时强制“只有已登记项目资产才能流转”；暂存结果会返回 `WORKFLOW_OUTPUT_NOT_SAVED`，前端只在用户明确确认“加入资产并流转”后连续完成两步。
- 最终门禁通过：`pnpm lint`、API/Web 类型检查、Platform API 48 项测试、Web 12 项状态测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、真实 Chromium 页面验收和 `pnpm build:rail`。浏览器验收同时使用真实 ComfyUI 配置创建并停止一次 ScreenShare 实时任务；临时账号、项目、对象和流转记录均完成清理。

## 2026-08-09：工作区草稿、专用控制器与 Worker 协议同步

- 高级参数首次进入时默认折叠；基础参数、专用控制器和高级参数仍共同使用同一份公开参数 Schema 与任务提交契约。
- 新增 `015_workflow_workspace_drafts.sql` 和 `/workflow-drafts` 接口。当前用户在指定项目和应用中的参数、图片输入位及 EasyMark 数据持久化到 PostgreSQL；再次点击应用标签、切换回来或刷新页面后恢复，已完成任务结果仍不自动恢复到工作区。
- 草稿接口校验用户项目范围、应用可见性、公开参数键、精确输入位、资产登记状态和类型兼容性；已删除、跨项目、暂存或重复占位的资产不会恢复。
- Qwen 单视角和五镜头工作流增加轨道式镜头可视化控制器，水平角、俯仰角和镜头距离仍写入各自真实 ComfyUI 节点。
- EasyMark 分区编辑与参考 webUI 协议对齐，支持自由画笔、方框、色块、橡皮、六种颜色、透明度和 1–6 编号，序列化结果直接写入 `brush_data`。
- ScreenShare 输入改为工作区内直接展示“共享屏幕、摄像头、Set Area、Live On”，保留选区裁剪、变化检测、串行提交和停止清理。
- 修复开发环境 API 热更新但 Worker 不重载的问题：`dev:rail` 现在使用 `tsx watch` 启动 Worker；工作流参数协议变化会自动重启 Worker。即使出现版本不一致，也只返回稳定的中文错误，不再把 Zod 联合类型明细显示给用户。
- 验证覆盖参数 Schema、草稿纯函数、真实 PostgreSQL/MinIO 草稿读写与隔离、模拟 ComfyUI Worker，以及 Chromium 中高级参数折叠、直接实时捕获、角度草稿恢复、图片输入恢复和 EasyMark 编号序列化。
- 最终门禁通过：`pnpm lint`、API/Web 类型检查、Platform API 55 项测试、Web 12 项状态测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、真实 Chromium 页面验收和 `pnpm build:rail`。浏览器验收产出了实时捕获、镜头控制、分区打标和草稿恢复四组截图，运行期没有 JavaScript 页面错误。

## 2026-08-10：工作流结果自适应与统一 ComfyUI 遮罩编辑器

- 保持 `apps/platform-api/workflows/comfyui/` 内 18 份原始 API JSON 完全不变，仅调整所有能力共用的前端工作区和浏览器验收。
- 图片结果区移除固定深色画布和最小高度，按图片实际宽高在可用区域内自适应完整显示；右下角不再显示“点击放大”，统一使用 ComfyUI 官方 `comfy--mask` 图标进入全屏遮罩编辑器。
- 新增共享遮罩编辑器，按 ComfyUI webUI 结构提供顶部撤销/重做、旋转、翻转、反转、清除、保存/取消，左侧遮罩/绘画/橡皮/填充/吸管工具，中央缩放画布，以及右侧笔刷和图层设置。
- 所有 `mask` 类型工作流输入与图片结果复用同一组件；输入遮罩保存后作为新的输入资产回填，结果遮罩保存后直接登记为当前用户、当前项目的图片资产，并保留“遮罩”用途标签。
- 高级参数展开后使用最高 `520px`、同时受视口高度约束的内部滚动区，不再继续拉长控制栏和中央结果画布。
- 浏览器验收新增：单图局部重绘遮罩入口共用性、文生图高级参数内部滚动、结果图片容器无额外填充、遮罩绘制保存和数据库资产登记。使用系统 Chrome 的真实页面、PostgreSQL 和 MinIO 验收通过，测试临时用户、项目、对象和资产均已清理。
- 验证通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 55 项与 Web 12 项单元/状态测试、`RAIL_BROWSER_EXECUTABLE=/usr/bin/google-chrome pnpm test:rail:browser` 和 `pnpm build:rail`。

### ComfyUI 官方遮罩功能逐项对齐

- 以本机 ComfyUI 0.27.0 前端包 Source Map 中的 `maskeditor` 实际源码为事实来源，替换第一版简化笔画模型；工具顺序、官方 SVG、顶部操作、左右面板尺寸和深色主题与当前 ComfyUI 遮罩编辑器保持一致。
- 画布改为基础图像、绘画、遮罩三个真实像素层，历史同时保存三层并支持最多 20 步撤销/重做；旋转、镜像、反转和清除均操作实际像素，变换后仍可继续编辑和保存。
- 补齐遮罩画笔、绘画画笔、橡皮、油漆桶和颜色选取五种工具。笔刷支持圆/方形、半径、透明度、硬度、间距、右键临时擦除、Shift 直线和 Alt+右键拖动调整大小/硬度。
- 油漆桶按遮罩 Alpha 连通域提供阈值和填充不透明度；颜色选取提供 Simple/HSL/LAB、连续区域/整图、实时预览、选取透明度、遮罩边界和遮罩阈值，算法参数与官方实现一致。
- 画布支持以指针为中心的滚轮缩放、空格+左键或中键平移、触控单指平移和双指缩放；左下角显示实际缩放比例与图片尺寸并可一键恢复适应窗口。
- 图层面板支持显隐、遮罩透明度、黑/白/负片混合，以及橡皮工具在遮罩层和绘画层之间切换；保存按官方 `paintedMaskedImage` 语义合成 RGB 绘画层并将遮罩 Alpha 反向写入最终 PNG，再登记为当前项目资产。
- 新增遮罩像素算法单测和浏览器验收：覆盖连通填充、颜色范围、反转/旋转/镜像、五工具设置面板、三图层切换、绘制与资产保存。原始 18 份 ComfyUI API JSON 未修改。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 55 项与 Web 15 项测试、`RAIL_BROWSER_EXECUTABLE=/usr/bin/google-chrome pnpm test:rail:browser` 和 `pnpm build:rail`；浏览器验收覆盖官方式指针画布、工具切换、撤销和最终资产登记。

## 2026-08-10：应用单实例、多轮工作区与图片差异对比

- 修复同一应用因 `transferId` 等查询参数产生重复标签的问题：应用工作区标签键固定使用应用路径，并在进入时清理会话中遗留的同路径旧标签；不同应用仍保持各自独立标签。
- 后端创建任务时按当前用户、项目和应用获取 PostgreSQL 事务级 advisory lock，并检查 `queued`、`running`、`cancelling` 状态；并发提交稳定返回 `APPLICATION_JOB_ACTIVE`，前端按钮隐藏不再是唯一保护。
- 任务查询补充 `createdBy`、`ownedByCurrentUser`、`parameters`、有序 `inputs` 和带位置的 `outputs`。工作区只将当前用户在当前项目、当前应用的任务组成会话，按时间展示全部轮次，切换标签、任务完成和刷新后均不清空。
- 每一轮以“输入气泡 + 可展开完整输入 + 执行结果”的形式展示；完整输入包含业务字段名称、提交时的参数值、输入资产顺序、名称和图片预览。历史结果继续支持多结果切换、下载、遮罩编辑、加入资产、精确流转，以及将该轮参数和资产复制回当前草稿后再次运行。
- 从已发布工作流版本的 `api_json` 中只读识别 `ImageCompare` 与 `Image Comparer (rgthree)` 节点；对应应用使用第一张业务图片输入和当前图片输出展示左右拖动对比，并可切回单独结果视图。18 份原始 ComfyUI API JSON 未修改。
- 修复遮罩编辑器在历史轮次中按需首次挂载时未初始化的问题：监听增加立即初始化，任意轮次第一次打开遮罩都能正确读取画布。
- 自动化证据：新增图片对比节点识别单测和工作区轮次筛选单测；真实 PostgreSQL/MinIO 集成验收覆盖并发拒绝、参数与有序输入快照；Chrome 验收覆盖两个历史轮次、完成后恢复、标签去重、输入展开、对比滑块、遮罩、入资产与流转。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 57 项与 Web 16 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`；验收临时用户、项目、任务、资产与对象均按本轮唯一标识清理。

## 2026-08-11：同应用多会话实例、实例级流转与字号层级

- 修正上一轮对产品层级的误解：应用是功能模板，应用实例是用户打开的一次持久化会话，一个应用允许同一用户在同一项目中创建多个实例；每个实例对应独立标签、草稿和多轮任务历史。
- 新增迁移 `016_workflow_workspace_instances.sql` 和 `/workflow-instances` 接口。旧任务、草稿和流转记录按用户、项目、应用归入兼容的“历史会话”，升级不删除历史数据。
- `jobs.workspace_instance_id` 成为任务会话边界；事务级 advisory lock 和活动任务查询均按实例执行。同一实例的第二个活动任务返回 `WORKSPACE_INSTANCE_JOB_ACTIVE`，同一应用的另一个实例可并行运行。
- 资产流转改为“目标应用 → 已有会话/新建会话 → 具体输入位”。待流转唯一约束和查询都按目标实例执行，页面不再用含糊的“已有待流转资产”表示整个应用被占用；仅当所选实例的具体输入位已有待接收资产时显示“该会话输入位已占用”。
- 应用中心和概览的应用入口每次创建新会话；任务中心可直接返回任务所属实例；路由以 `instanceId` 区分同一应用的多个标签，并只清理同一实例的旧查询参数标签。
- 工作区建立 12px 辅助信息、13px 字段标签、14px 正文、17px 面板标题的统一字号层级，参数、资产选择、专用输入组件和多轮任务卡片不再混用 8–11px 微小文字。
- 测试覆盖实例内互斥、同应用跨实例并行、草稿隔离、目标实例流转、同应用两个独立标签、历史轮次隔离、已有目标实例接收和浏览器计算字号；原始 18 份 ComfyUI API JSON 未修改。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 57 项与 Web 16 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`。浏览器验收覆盖同应用多实例标签、实例历史保留、目标实例流转、统一字号计算值及既有遮罩、镜头、分区和图像对比能力。

## 2026-08-11：遮罩工作流图像对比原图血缘修正

- 修复遮罩保存后任务只记录带 Alpha 的遮罩 PNG，导致对比组件错误将遮罩输入当作“原始输入”的问题。遮罩资产现在在 `asset_versions.metadata.derivedFromAssetId` 中登记遮罩编辑前的项目图片；重复编辑遮罩时继续保留最初原图，不形成遮罩链误判。
- 任务输入快照返回遮罩资产的原图血缘。带图片对比节点的遮罩工作流左侧固定读取本轮遮罩前原图，右侧读取当前选中的本轮生成结果，并显示“遮罩前原图 / 遮罩生成结果”；普通图片编辑工作流仍使用本轮输入图和生成结果。
- 对修复前已经完成且没有原图血缘的历史遮罩任务，不再拿遮罩 PNG 冒充原图，而是隐藏错误对比并提示“历史任务未记录遮罩前原图，无法准确对比”。原始 18 份 ComfyUI API JSON 未修改。
- 验证通过：`pnpm typecheck:rail`、Platform API 57 项与 Web 16 项测试、真实 PostgreSQL/MinIO 集成验收，以及系统 Chrome 浏览器验收。浏览器直接断言左图预签名 URL 属于原始底图且不属于遮罩资产，右图属于本轮生成结果。

## 2026-08-11：智能扩图红色扩展区适配修正生效

- 根据用户截图核查最近智能扩图任务：异常结果使用工作流 V3，V3 的持久化参数 Schema 未包含原始 API 工作流节点 367 所要求的“删除红色部分”协议；节点 368 `ImagePadForOutpaint` 产生的红色扩展标记因此被模型当作结果内容保留。最终输出映射仍为节点 `366.images`，不是误取预览或遮罩节点。
- 保持原始 `outpaint-v1.json` 不变，通过后端私有参数 `valuePrefix` 为用户设计描述自动添加“删除红色扩展标记区域、参考原图自然延展、不得保留纯色边框或遮罩痕迹”。该固定协议不会暴露为可编辑表单字段，任务台账仍只保存用户输入。
- 重新执行幂等工作流目录同步后，本地数据库生成并激活智能扩图 V4；只读核验确认节点 367 的实际物化提示词为固定协议与用户描述的拼接。旧 V3 任务及其结果不改写，用户点击“复用本轮再运行”会按当前活动 V4 创建新任务。
- 回归覆盖目录物化测试，断言节点 367 同时包含固定去红色协议和用户扩图描述；原始 18 份 ComfyUI API JSON 未修改。

## 2026-08-11：项目级统一设计会话与多应用时间线

- 将普通用户的主要入口从“逐个应用创建独立工作区实例”调整为项目内“开始设计”：左侧管理历史会话，中间按时间显示多轮、多应用结果，底部统一选择全部可见应用并配置本轮参数。未显式选择应用时默认由“文生文”执行，后续仍可替换为其他文本 API 适配器。
- 新增迁移 `017_project_design_conversations.sql` 和 `/design-conversations` 接口。设计会话按用户和项目隔离，支持创建、重命名、列表和软删除；归档只从历史栏隐藏，不删除项目资产、任务台账和审计记录。
- 旧应用工作区实例按原 UUID 幂等迁移为设计会话，旧任务和草稿完成关联回填。`workflow_workspace_instances`、`/workspace/:appKey` 和应用中心继续保留，仅供管理员调试单一能力，避免破坏已有任务历史。
- 任务执行边界改为设计会话：同一会话通过 PostgreSQL 事务级 advisory lock 和活动任务检查只允许一个生成任务；同一用户的不同会话可以并行。任务查询返回设计会话归属，时间线能够在同一会话中混合展示不同应用的历史轮次。
- 新增按“设计会话 + 应用”持久化的参数草稿。底部先展示应用名称和少量高频参数，其余公开参数进入抽屉；图片、资产、遮罩、镜头、分区、实时捕获等既有专用输入继续复用原组件和真实工作流参数契约。
- “流转到工作流”改为本会话内的“继续设计”：选择目标应用和兼容输入位后直接切换当前应用并回填资产，不再选择目标应用的第几次会话。暂存结果仍必须由用户明确加入项目资产后才能继续设计，保持资产作为能力交换协议的业务规则。
- 应用中心更名为“应用调试中心”并限制管理员访问；项目列表、平台概览和应用快捷入口统一进入 `/design`。开始设计页面隐藏右下角全局 AI 助手，其他页面暂时保留。
- 原始 18 份 ComfyUI API JSON 未修改。数据库迁移后只读核验：40 个旧工作区会话完成迁移，78 个历史任务全部具备设计会话范围，26 份旧草稿完成回填。
- 完整门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 59 项与 Web 17 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`。浏览器验收覆盖左侧会话、三轮混合应用历史、18 项能力选择、旧管理员调试工作区以及开始设计页不重复显示全局 AI 助手。

## 2026-08-11：开始设计沉浸式工作台与两态统一输入器

- 根据最终交互截图重构 `/design`：页面不再嵌套后台深色导航、顶部页签和第二层侧栏，只保留项目选择、历史会话、对话时间线和固定底部输入器。旧应用实例迁移记录通过接口标记为 `legacy`，不再挤占普通用户历史栏，仍可在任务中心追溯。
- 统一输入器严格区分两种状态：未选应用时显示“添加、常用应用、更多、发送”，业务执行仍默认使用 `text-chat`；选定应用后隐藏其他应用，显示可取消的应用标签、最多四项主要参数、输入素材、高级参数和发送。完整公开参数及图片、遮罩、区域、镜头和实时捕获等专用输入继续进入同一参数抽屉。
- “继续设计”保持在当前会话内选择目标应用和兼容输入位，结果经用户确认登记为项目资产后直接回填；历史轮次仍保留下载、加入资产、遮罩、图像对比和复用运行能力。
- 浏览器验收新增 1600×1000 全屏边界、普通/旧版历史分离、未选应用默认文生文但不显示已选标签、常用应用与“更多”、选定后隐藏其他应用、可取消应用及未选/已选双态截图断言。原始 18 份 ComfyUI API JSON 未修改。

## 2026-08-11：统一输入器快捷参数、素材消息与生成停止

- 将底部输入器正文与应用入口字号放大，边框、应用标签、交互焦点和发送按钮统一使用平台红色系，不再使用外部 AI 产品的蓝色视觉。
- 新增通用快捷参数弹层：下拉选项、比例、数值、随机种子、开关和短文本可在底部原位修改；只有“更多”打开 18 项能力各自的完整参数面板。
- “+”与“素材”只打开上传/项目资产选择窗口。已选图片在发送前显示缩略图、输入位和名称；发送后按用户消息形式显示独立大图与文本气泡，完整输入详情仍可展开。非图片素材保留类型和名称卡片。
- 任务运行时，发送按钮切换为灰底方形停止标记；点击调用现有取消 API，完成后恢复发送状态。后端的同会话单活动任务约束保持不变。
- 扩展真实 Chrome 验收，对红色边框/按钮计算样式、最小字号、快捷弹层、完整面板、发送前图片、发送后图片、停止交互和 18 项应用数量进行断言，并产出对应截图。本轮未修改数据库、API 契约或 18 份原始 ComfyUI API JSON。
- 最终验证通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 59 项与 Web 17 项单元/状态测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`。浏览器验收点击了真实停止接口并验证输入器恢复为可发送状态。

## 2026-08-11：历史输入悬浮操作、Markdown 结果与轻量工具栏

- 移除每轮用户输入下方常驻的“查看本轮完整输入”长条。复制输入和查看参数改为在输入消息悬浮或键盘聚焦时出现的图标；完整资产与参数进入独立、可滚动弹窗，移动端保持入口常显。
- ComfyUI Worker 将新生成的文本结果统一归一化为 `.md` 文件名和 `text/markdown` MIME，包含 ComfyUI 返回的已有文本文件与内联文本节点；原始内容不重写，18 份原始 API JSON 不修改。
- 新增安全的本地 Markdown 阅读组件，以 Vue 文本节点渲染标题、段落、列表、引用、代码块和可折叠思考过程，不使用未净化 HTML。文本结果增加复制图标，复制的是完整 Markdown 原文。
- 下载/查看、加入资产、继续设计和复用运行统一改为有可访问名称的图标按钮，鼠标悬浮显示具体功能；共享任务卡片同时覆盖普通用户“开始设计”和管理员单能力调试页面，原有下载、资产、流转、遮罩、对比与复用事件保持不变。
- 单元测试覆盖内联文本和外部 `.txt` 输出的 Markdown 元数据归一化；真实 Chrome 验收覆盖输入操作默认隐藏、悬浮出现、参数弹窗、Markdown 标题与列表、复制原文、五项图标操作和 Tooltip，并生成输入悬浮、Markdown 结果两张专项截图。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 60 项与 Web 17 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`；Git 路径审计确认 18 份原始 ComfyUI API JSON 变更为 0。

## 2026-08-11：设计会话输入闭环、自动命名与轻量运行反馈

- 修复正在运行会话的编辑、删除图标换行问题：活动状态与操作区固定同行显示，长标题只在标题区域截断。成功创建任务后立即清空本轮文本、图片和输入位素材，保留当前应用及其他参数；创建失败时保留输入，便于修正后重试。
- 任务运行卡移除大面积技术进度框、`ComfyUI 正在执行工作流` 和 Prompt ID，只显示三点“生成中”轻量反馈；底部发送按钮同步切换为停止方块。文本输出取消额外左缩进，时间线与底部输入器共用同一内容宽度。
- 历史轮次的图片输入增加遮罩图标与点击编辑入口，复用完整 ComfyUI 风格遮罩编辑器。保存编辑结果时登记为当前项目资产并保留原图血缘，暂存输出尚未登记前不伪造派生资产关系。
- 统一输入器可从当前项目资产选择 `.md` / `text/markdown` 文本，读取短时授权下载地址后只提取可读 Markdown 文本，过滤图片、HTML 图片标签和链接目标；图片仍以缩略图和文本气泡形式在发送前后直接显示。
- 参数较多时不再截断为四项：快捷参数区横向滚动，“更多”固定在输入器右侧且始终可见。会话列表操作保持同行，输入器与历史内容使用统一宽度和视觉层级。
- 新增迁移 `018_design_conversation_titles.sql`。仍为默认名称且从未手工改名的会话，在首次有效任务创建事务中按业务文本自动生成最多 20 个字符的标题；手工改名通过 `title_manually_edited` 标记保护，后续运行不得覆盖。没有有效文本时保留“新设计会话”。
- 单元测试覆盖标题提取、截断及 Markdown 文本清理；真实集成验收覆盖自动命名和手工名称保护；系统 Chrome 验收逐项覆盖会话操作同行、输入清空、三点生成状态、输入图片遮罩、左右间距、Markdown 资产输入、输入器等宽、参数可见性与自动命名，并生成专项截图。
- 原始 18 份 ComfyUI API JSON 未修改。最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 19 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器九项交互验收和 `pnpm build:rail`。生成中专项截图可直接看到三点状态、停止按钮及对齐后的统一输入器。

## 2026-08-11：三维结果可视化、资产改名排序与会话末轮定位

- 新增共享 Three.js 三维查看器，设计会话结果和资产详情均可直接解析 GLB/GLTF、OBJ、FBX、STL、PLY；提供与 ComfyUI 查看语义一致的场景、模型、摄影机、灯光、控制、导出和全屏工具，并支持轨道旋转、缩放、平移、网格、线框、自动旋转和原文件下载。加载失败时显示明确错误，不再用静态“3D 模型”占位伪装为可视化成功。
- 资产详情增加名称编辑，后端 `PATCH /assets/:id` 校验当前用户写权限和项目范围，并记录 `asset.rename` 审计事件。资产列表新增创建时间、名称和文件类型的升降序选择，排序在服务端使用白名单字段执行，不依赖前端当前页重排。
- 资产预览接口允许经过项目权限校验的 `model3d` 对象生成短时读取地址；设计任务结果按资产 MIME 与文件扩展名选择加载器，同一共享组件也用于资产详情，避免两套查看行为不一致。
- 进入或切换设计会话时自动定位到最新一轮，并在异步图片、Markdown 或三维查看器完成布局后进行一次位置修正；不同会话各自保留持久化历史。任务运行卡仅保留动态三点，移除“生成中”文字，停止按钮和任务状态台账保持不变。
- 真实 PostgreSQL/MinIO 集成验收覆盖改名持久化、跨用户拒绝、六种服务端排序组合和三维文件预览；系统 Chrome 验收上传并解析真实 GLB，断言 WebGL 画布、模型网格/顶点、六项工具、资产详情改名、排序请求、会话末轮定位和纯三点运行反馈，并逐图核对三维查看器已占满结果区域。
- 本轮无数据库迁移，18 份原始 ComfyUI API JSON 变更为 0。最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 20 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器验收和 `pnpm build:rail`。

## 2026-08-12：全局图标离线渲染修复

- 定位到结果操作按钮、导航、会话和其他页面的空白图标并非 CSS 遮挡：`@iconify/vue` 收到字符串图标名后尝试从 Iconify 公网 API 动态下载，服务器或用户网络无法访问时只留下按钮和 Tooltip 空壳。
- 将完整 Lucide 图标集作为 Web 运行依赖，在应用挂载前注册到 Iconify 本地存储；导航、会话侧栏、输入器、应用参数、资产中心、结果工具栏和三维查看器不再依赖浏览器访问外部图标服务。
- 将少量 MDI、Fluent、Element Plus、Ant Design 在线前缀替换为语义一致的 Lucide 图标；修正 `scan-image`、`box-off`、`badge-sparkles` 三个 Lucide 集合中不存在的名称，并为数据库中可能保留的旧名称注册本地兼容别名。
- Chrome 验收主动阻断 `api.iconify.design`、`api.simplesvg.com` 和 `api.unisvg.com`，断言新建会话、搜索、历史会话、输入器以及结果区五个操作按钮均包含可见 SVG 路径，且整轮验收没有任何图标公网请求。
- 本轮无数据库迁移，18 份原始 ComfyUI API JSON 变更为 0。门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 20 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、离线图标 Chrome 浏览器验收和 `pnpm build:rail`。

## 2026-08-12：分区编号、输入标记快照与输出对比闭环

- 修复 Ant Design 弹窗 Teleport 后无法继承工作区主题变量，导致当前选中的 1–6 分区编号白字落在透明背景上、视觉上消失的问题；选中编号现在使用稳定的平台红色回退，六个编号始终保留，活动项只改变高亮状态。
- 新增迁移 `019_job_input_annotations.sql`，为 `job_inputs` 增加可选 `annotation_asset_id`。`asset_id` 继续保存并提供给 ComfyUI Worker 的标记前原图，标记图只作为同项目图片资产快照关联；创建任务时校验输入位置、图片类型、项目范围、保存状态和 `derivedFromAssetId` 血缘，避免错配其他原图。
- 用户提交带分区参数的任务前，浏览器按原始尺寸合成原图与 EasyMark 笔画并登记标记图；普通用户开始设计与管理员调试工作区复用同一逻辑。历史轮次并列显示“标记前原图”和“分区标记图”，标记图支持弹层放大，完整输入弹窗也保留两份输入说明。
- 分区工作流输出固定启用“标记前原图 / 分区生成结果”左右滑动对比，即使能力元数据仍在异步加载，也可通过任务输入的标记快照恢复正确语义；原有遮罩血缘对比继续使用遮罩编辑前原图。18 份原始 ComfyUI API JSON 未修改。
- 验证覆盖 EasyMark 序列化单测、真实 PostgreSQL/MinIO 中原图与标记图关联的创建及刷新恢复、浏览器六个编号可见性与颜色对比、输入双图、标记图放大、分区输出前后图来源、既有遮罩对比和 18 项工作流页面回归。迁移在已有数据库上成功升级。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 22 项测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 浏览器全量验收和 `pnpm build:rail`。

## 2026-08-12：历史轮次修改重发、资产文件管理与单层项目空间

- 历史轮次输入悬浮区增加“修改并重新发送”入口。用户只编辑该轮业务文本，平台从任务台账恢复原应用、全部参数、有序输入资产及遮罩/分区数据，并创建新的任务轮次；原任务参数和结果保持不可变。重发前重新检查所有历史资产仍属于当前项目且可用，失效资产明确阻断。
- 新增 `020_asset_folders.sql`：资产文件夹、父子关系及 `assets.folder_id` 全部持久化到 PostgreSQL。资产中心支持面包屑、多级文件夹、重命名、网格/列表和多选；批量移动只修改目录归属，批量复制为每项创建独立资产版本和对象键，数据库失败时补偿删除新对象；批量删除与非空文件夹递归删除均采用软删除并保留任务血缘和审计。
- 新增 `021_project_user_pins.sql`：项目置顶是当前用户个人偏好。原“平台概览”与“项目空间”合并到 `/projects`，旧地址只重定向；页面纵向展示全部可访问项目及资产、任务、成员统计，支持搜索、创建、负责人/管理员改名、置顶以及进入资产或开始设计。项目空间页隐藏重复的当前项目下拉框，其他项目页面继续保留。
- 权限与范围验证覆盖文件夹、目标目录、批量资产和项目负责人；对象资产复制使用 S3 服务端复制，复制件删除不影响源对象。审计新增文件夹创建/改名/删除、资产批量操作、项目改名和项目置顶事件，不记录文件正文或任务输入正文。
- 自动化覆盖 Pinia 文件夹与批量状态、项目改名/置顶，真实 PostgreSQL/MinIO 中多级目录、独立对象复制、移动、递归软删除、跨用户拒绝，以及系统 Chrome 中双项目展示、搜索/改名/置顶、历史遮罩任务修改重发、原轮不可变、两级目录、列表视图、复制和移动。18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：项目成员、统一业务 ID 与成员归属管理

- 新增 `022_public_ids_project_members_and_job_archival.sql`。内部外键继续使用 UUID；用户、资产和任务分别获得唯一 `USR-*`、`AST-*`、`TSK-*` 业务 ID，项目沿用 `CR-*`。迁移在已有数据上回填、建立唯一索引并为新记录配置序列默认值。用户 ID 同步显示在个人中心、右上角用户信息和管理员用户列表。
- 项目卡片支持同时修改名称和说明；项目资产、累计任务、运行任务和项目成员四项统计均可点击。前三项先切换项目上下文再进入对应筛选页，成员项展示角色、用户 ID、部门和个人资产/任务贡献。只有 `projects.owner_id` 对应的项目创建者可通过启用用户的唯一 ID 邀请编辑或只读成员，管理员身份不会绕过该邀请规则。
- 资产继续以 `owner_id` 绑定具体项目成员，并在网格/列表/详情显示资产 ID 与成员 ID；查询增加成员筛选和创建人排序，既有搜索、类型/时间/名称排序、文件夹、网格/列表与批量移动/复制/软删除保持不变。来源任务展示 `TSK-*`，不向用户暴露内部任务 UUID。
- 任务继续以 `created_by` 绑定具体成员；任务中心增加业务 ID、成员/状态/关键词筛选、创建时间/名称/创建人/状态排序、多选和批量删除。删除写入 `archived_at/archived_by`，只从任务中心隐藏，任务输入输出、项目资产和审计记录保持可追溯；运行中或正在取消的任务必须先取消，不能归档。
- 产品侧栏、路由和页面代码均移除“应用调试中心”，`/applications` 不再是可访问页面；应用目录、适配器配置和历史任务数据作为底层执行能力继续保留。
- 验证通过 `pnpm lint`、`pnpm typecheck:rail`、Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收和系统 Chrome 全量浏览器验收。浏览器额外核对项目名称/说明编辑、成员面板、统计导航、资产成员筛选、任务成员筛选/排序、业务 ID 及调试中心菜单消失。18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：项目软删除与列表可读性收尾

- 新增 `023_project_archival_actor.sql` 和 `DELETE /api/v1/projects/:id`。普通用户仅能删除自己创建的项目，管理员可以删除任意项目；存在排队、运行或取消中任务时明确拒绝。删除采用软归档并记录操作者，同时清除项目置顶和失效的当前项目偏好，资产、任务、设计会话、成员关系和审计记录均不物理删除。
- 项目卡片把删除按钮放在编辑按钮之后，移除底部与统计入口重复的“查看资产”。长项目名称和说明允许安全换行，不覆盖右上角操作区。
- 资产中心与任务中心的批量操作栏均增加明确“取消选择”。失败任务列表显示可读摘要并限制在列宽内，完整原始错误改由可滚动详情弹窗承载。
- 管理员用户列表逐项标明姓名、用户 ID、用户名和邮箱；角色卡片的“系统角色”、数据范围及类似长文本增加收缩、截断和换行约束，避免越过卡片边界。18 份原始 ComfyUI API JSON 未修改。
- 验证通过 `pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。浏览器额外验证项目软删除确认/隐藏、两处取消选择、失败日志行宽与详情弹窗、用户字段名称和角色卡片边界，控制台脚本错误为 0。

## 2026-08-12：项目编辑弹窗与管理员邀请权限修正

- 项目说明字符计数改为弹窗正文中的独立布局元素，不再由文本域的浮动计数占用底部按钮区域；浏览器验收直接比较计数和 footer 的几何边界，阻断再次遮挡。
- 项目成员邀请权限由“仅创建者”调整为“项目创建者或平台管理员”。成员列表接口和邀请写接口使用同一角色判断；管理员无需先成为项目成员，也可在他人创建的未归档项目中邀请启用用户。普通编辑成员和只读成员仍不能邀请，自我邀请仍被拒绝。
- 真实集成验收新增第四个临时普通账号，覆盖非项目创建者管理员读取 `canInvite`、成功邀请、被邀请账号获得项目访问权，以及普通成员邀请被拒绝的完整边界。
- “开始设计”侧栏取消显示旧应用记录数量及“已移至任务中心”提示；旧任务、迁移关联和任务中心回溯数据保持不变。
- 最终验证通过 `pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 四账号集成验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。

## 2026-08-12：项目列表排序与卡片视觉简化

- 项目列表新增与资产中心一致的排序选择，支持更新时间、创建时间和名称正反序。`GET /projects` 使用 Zod 白名单校验排序字段并在 PostgreSQL 执行排序，置顶项目始终优先；项目响应补充真实 `createdAt`，不依赖浏览器对当前结果临时重排。
- 项目卡片由大面积暗色背景改为浅色信息卡，移除“输入资产—开始设计—成果入库”三段装饰流程。卡片底部只保留一个“开始设计”主按钮，四个统计入口以及置顶、编辑、删除功能保持不变。
- 集成验收覆盖项目名称正反序及创建/更新时间查询；浏览器验收覆盖排序请求参数与实际卡片顺序、浅色背景、三段流程消失和单一“开始设计”入口。
- 最终验证通过 `pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。

## 2026-08-12：图片放大查看抓手拖拽

- 共享 `ImageLightbox` 增加抓手提示和 Pointer Events 平移交互。图片放大超过适配比例后，视口使用 `grab/grabbing` 光标，按住鼠标或触控笔即可拖动查看细节；平移范围按缩放后图像与视口尺寸约束，不会把图片完全拖出可视区域。
- 缩小到适配比例、点击“原始比例”、切换图片或重新打开弹窗都会同时复位缩放和平移。视口隐藏滚动条，资产中心、工作流输入标记图和任务结果等使用共享查看器的位置同步获得该能力。
- 浏览器验收断言抓手入口、放大后的光标、隐藏滚动条及实际拖拽后的 transform 位移。
- 最终验证通过 `pnpm lint`、`pnpm typecheck:rail`、Platform API 62 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：长文本输入器与对比模式操作收敛

- “开始设计”输入器改为豆包式有限高度：短文本在 2–8 行内自适应，超过约 220px 后只在文本域内部滚动，不再撑满整个会话页面；底部素材、应用快捷入口和发送按钮始终固定在输入器底部并保持可见。
- 输入正文统一提升到 17px/500 字重，应用与功能入口提升到 15px/600 字重；发送按钮继续沿用平台红色和原有外形，白色上箭头增大到 22px 并提高线条粗细，保证层级清楚但不破坏整体节奏。
- 图片处于左右滑动“对比”模式时完全隐藏遮罩编辑按钮，避免对合成对比视图执行含义不明确的编辑操作；切回单张“结果”模式后恢复既有遮罩入口，输入图片和普通结果的遮罩功能不受影响。
- 系统 Chrome 验收使用 28 行长文本核对输入框最大高度、内部滚动、底部工具栏可见性、字号/字重和发送箭头，并分别断言对比模式无遮罩按钮、结果模式恢复一个遮罩按钮；专项截图已逐项人工核对。

## 2026-08-12：项目成员移除、头像裁剪与项目列表视觉收敛

- 新增项目成员移除接口。项目创建者和平台管理员可以移除非创建者成员；创建者的“移除”仍在同一对齐列显示但置灰，列表不再混用图标和不同字号。移除只删除项目访问关系及个人置顶/当前项目偏好，成员已经创建的资产、任务、设计会话和审计记录完整保留。
- 新增 `024_user_avatars.sql` 和真实头像上传 API。PNG、JPEG、WebP 先在个人中心的沉浸式 1:1 裁剪器中完成拖动、缩放、90 度旋转和复位，再以 512×512 PNG 上传；API 仍独立校验大小、文件签名和方形尺寸，写入 MinIO 私有对象后更新用户记录，并在数据库失败或替换成功时执行相应对象清理。上传成功后个人页、顶栏和用户菜单同步刷新。
- 项目卡片建立主标题、正文和辅助信息三级文字色，项目编号改为灰色；统计数字放大加粗、辅助说明降灰，运行任务大于零时使用平台红强调。卡片整体支持悬浮与键盘焦点，主体可直接进入项目；置顶、编辑、删除操作增加悬浮面与 Tooltip，统计分隔线淡化，日期统一为 `YYYY-MM-DD HH:mm`，搜索占位精简为“搜索项目”。
- 项目空间统一 12–14px 圆角和 16–24px 间距栅格，补齐无项目/无搜索结果空状态与创建按钮禁用态。浏览器专项截图已与需求图逐项比对，浅色层级、操作区、统计区和头像裁剪区均未出现遮挡或越界。
- 自动化新增头像签名/尺寸单测、真实 MinIO 方形约束与成员移除数据保留集成验收，以及项目视觉层级、日期、hover、Tooltip、空状态、禁用态、成员对齐移除和头像裁剪的系统 Chrome 验收。18 份原始 ComfyUI API JSON 未修改。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 24 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。

## 2026-08-12：设计会话头像同步修复

- 修复“开始设计”会话侧栏底部仍使用姓名首字而未读取用户头像的问题。该位置现在与个人中心和顶栏共用响应式 `userStore.userInfo.avatar`，上传成功后无需重新登录或刷新即可同步；未设置头像时仍保留姓名首字兜底。
- 会话头像使用固定圆形裁切和 `object-fit: cover`，不会因原图比例破坏侧栏布局。系统 Chrome 头像上传流程继续进入设计会话，断言侧栏使用本轮刚上传的同一头像地址。

## 2026-08-12：资产收藏目录与悬浮星标闭环

- 新增 `025_asset_favorite_virtual_folder.sql`。收藏 API 在同一数据库事务中幂等创建或复用项目根级“收藏”系统文件夹并登记个人收藏；收藏内容通过 `asset_favorites` 关联查询形成当前用户的软链接视图，不复制对象、不修改 `assets.folder_id`，不同用户的收藏互相隔离。
- 取消收藏只删除当前用户的软链接，资产原文件夹、版本、对象和任务血缘全部不变。收藏系统目录不允许上传、文本登记、批量移动/复制、创建子目录、重命名或删除，避免它被误当成普通资产目录。
- 未收藏星标默认隐藏，仅在资产卡片悬浮或键盘聚焦时出现；已收藏星标以平台红选中态常驻。收藏与多选控件统一为 30×30 角标容器和 16px 图标，保持位置、类型和交互反馈一致。
- Pinia 单测覆盖收藏后的目录与原列表刷新；真实 PostgreSQL 集成验收覆盖首次创建、重复收藏幂等、个人软链接查询、原目录不变、取消收藏清除链接和跨用户拒绝；系统 Chrome 验收覆盖默认隐藏、悬浮显示、与选择控件等尺寸、原卡片保留、收藏目录引用及选中态常驻。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；同时复验设计会话侧栏头像已同步本轮上传头像。

## 2026-08-12：资产多选角标视觉一致性修复

- 资产多选与收藏统一使用同一 30×30 白色圆角容器；多选内部改为 16px 黑色描边圆圈并严格水平、垂直居中，选中后只改变圆圈填充和勾选状态，不再呈现偏移的方形控件。
- 移除图片预览左上角与多选位置重叠的半透明格式角标。文件类型仍在卡片正文中明确显示，未悬浮且未选择时左上角现在完全为空，不再残留灰色矩形；网格和列表视图共用同一规则。
- 系统 Chrome 验收增加默认透明度、旧格式角标不存在、收藏/多选等尺寸、圆形边框色与几何中心断言，覆盖未悬浮、悬浮和已选中状态。

## 2026-08-12：设计会话返回入口文案修正

- 设计会话侧栏用户区的返回按钮仍指向现有 `/projects` 项目空间，但旧 Tooltip 和无障碍名称错误显示“返回平台概览”。现统一更正为“返回项目空间”，与单层项目空间的信息架构和实际跳转目标保持一致。
- 全仓检索清理该旧提示，系统 Chrome 验收增加按钮无障碍名称和 `title` 属性断言，避免后续再次出现目标与提示不一致。

## 2026-08-12：设计工作台首页与核心闭环可视化

- 新增登录默认入口 `/home`“设计工作台”，项目空间继续作为项目创建、搜索、排序和维护页面。首页与项目空间均隐藏重复的顶栏项目下拉框，首页自身以紧凑选择器明确当前项目。
- 新增 `/api/v1/dashboard` 真实聚合接口。接口按登录用户的项目范围汇总可访问项目、运行/异常任务、未读通知、当前项目资产/设计会话/成果、可见 AI 应用、七日任务趋势、状态分布、资产构成及最近会话、任务、项目和成果；管理员可见范围与普通成员隔离继续由服务端校验。
- 首页用“项目资产 → 开始设计 → AI 应用 → 任务执行 → 设计成果 → 继续设计”的关系图解释平台核心能力，辅以堆叠趋势图、状态环图、资产横条图和紧凑最近工作列表，避免大段产品说明。所有节点、图例和列表均携带目标项目上下文进入资产、设计或任务模块。
- 无项目用户展示明确创建项目空状态；首页图片预览继续通过项目范围预览 API 获取短时地址，不暴露对象存储私有键。新增真实集成验收覆盖有项目聚合数据和无项目空状态，系统 Chrome 验收覆盖默认路由、闭环节点、三类图表及重复项目选择器消失。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；首页专项截图已人工核对闭环、图表、字号、留白和可点击状态。

## 2026-08-12：设计工作台动态闭环与内容边界优化

- 移除首页标题下方说明、项目选择/快捷按钮和四项顶部汇总区，保留“设计工作台”清晰主标题，进入页面后直接展示平台核心业务闭环，避免与项目空间和下方图表重复。
- “平台设计闭环”由横向串行卡片改为平台红、粉、灰色体系的原生 SVG/CSS 环形动态图。项目资产、开始设计、AI 应用、任务执行和设计成果作为固定可点击节点，中心表达持续设计与验证复盘，环形流向层缓慢顺时针旋转；悬浮暂停，并遵循系统减少动态效果偏好。
- 任务趋势、任务状态、继续设计、资产构成、任务动态、最近项目和最近设计成果统一增加卡片内滚动边界及细滚动条；窄屏时环形关系图和趋势图只在自身区域横向滚动，不再撑破卡片或页面。
- 浏览器验收增加顶部冗余区不存在、五个闭环节点顺序、旋转动画和七类内容区 `overflow` 约束检查；产品需求、架构和质量门禁同步更新。18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：设计闭环方案 A 轻量圆环优化

- 根据视觉复核将厚重分段转盘收敛为固定双细环：移除整圈渐变色块、旋转、虚线连接和额外回流说明，五个业务节点按“项目资产 → 开始设计 → AI 应用 → 任务执行 → 设计成果”直接贴合圆环分布，中心仅保留“持续设计”和“输入 · 生成 · 验证 · 入库”。
- 圆环、节点、图标和文字全部保持静止，仅五个方向箭头按业务顺序依次高亮并产生轻微光晕；系统要求减少动态效果时停止箭头动画并保持清晰静态箭头。节点仍携带当前项目上下文跳转，窄屏仍只在闭环卡片内部横向滚动。
- 浏览器验收由“整圈必须旋转”改为双重约束：外圈不得存在动画，五个箭头必须具有不同延迟的流动动画，避免后续视觉回退。18 份原始 ComfyUI API JSON 未修改。

## 2026-08-12：首页闭环数据能量流与视觉放大

- 删除“平台设计闭环”卡片右上角的当前项目名称与编号，避免重复信息占用首页核心视觉区域；当前项目仍由真实聚合 API 决定，五个节点跳转继续携带该项目上下文。
- 将圆环由 340px 放大到 410px，节点图标由 54px 放大到 64px，标题与数量分别提升到 18px、13.5px；同步增大中心信息、节点标签间距和闭环卡片高度，让五项核心能力在首页首屏更加醒目舒展。
- 完全移除五个三角方向箭头，改为固定双层轨道：外层点阵、内层点阵与局部高亮能量束按不同速度连续流动，圆环和节点本体保持静止；减少动态偏好下停止所有轨道动画并保留清晰静态关系。
- 浏览器验收新增右上角文字缺失、圆环尺寸、字号、标签距离、无三角箭头、四层数据能量流均启用及外圈固定断言。真实系统 Chrome 全流程验收通过，截图为 `/tmp/rail-workflow-browser-acceptance-dashboard.png`；目标 ESLint、Stylelint 通过，18 份原始 ComfyUI API JSON 未修改。
- 视觉复核发现局部高亮能量束的长尾运动形态类似蠕动条，已完整移除渐变长束与白色拖尾，替换为三个等距、固定直径、无尾迹的小型数据脉冲沿轨道匀速移动；浏览器验收增加长条能量束不得存在的回归断言。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；首页专项截图已人工核对节点位置、文字层级、固定外圈和箭头流向。

## 2026-08-12：首页闭环层级与任务列表一致性修复

- “项目资产”调整为文字和数量位于圆环外、图标贴合圆环，与其余四个节点保持一致；五个节点标题统一提升到 16px/700，数量提升到 12px/500，并同步放大节点图标，强化首页核心入口层级。
- 定位“首页显示运行任务、任务中心为空”为数据刷新时机问题：首页聚合接口读取实时数据库，任务中心此前可能继续显示 Pinia 初始化时的旧任务列表。任务中心现于每次进入页面时单独重新拉取当前项目任务，不额外刷新资产或目录，确保 `queued`、`running`、`cancelling` 三种活动状态与首页统计一致。
- 本地只读排查确认 `CR-2026-0183` 的 `TSK-00000177` 是早期系统 Chrome 验收残留的真实 `running` 测试记录，并非首页伪造数字；本次代码修复不擅自删除用户环境数据。浏览器验收新增五节点字号和“项目资产”外置布局断言。
- 最终验证通过：目标 ESLint/Stylelint、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；首页与任务中心专项截图已人工确认。

## 2026-08-12：首页跨项目口径与资产详情深链

- 统一设计工作台数据口径：项目资产、资产类型、已入库设计成果按当前账号可访问的全部未归档项目汇总；任务趋势、状态和运行数按当前账号在这些可访问项目中创建的任务汇总。被移出或归档项目的数据不再残留在首页统计中，管理员仍按其平台范围查看全部未归档项目。
- 最近项目改为稳定的四段同行布局，分别显示项目名称、项目资产及最多三类类型摘要、项目任务及编号、运行状态或更新时间；长名称与类型摘要均在所在列截断，不再把编号、状态挤到不同行。
- 最近设计成果不再进入资产中心根列表。新增受身份和项目范围保护的 `GET /api/v1/assets/:id`，首页先切换到成果所属项目，再使用 `?assetId=` 打开具体资产详情；详情 URL 可刷新、可回退，目标资产即使位于子文件夹也能读取，越权访问仍返回 404。
- 集成验收补充跨项目资产汇总、项目级类型拆分、资产详情深链和跨用户拒绝；18 份原始 ComfyUI API JSON 未修改。
- 全项目回归进一步发现资产详情抽屉的文本/三维预览会与列表刷新和深链路由产生异步竞态。文本阅读和三维模型预览现分别由按资产 ID 驱动的独立组件加载；切换资产时旧请求不能覆盖新详情，文本正文不再停留在加载占位，三维查看器关闭时停止动画、清空场景并显式释放 WebGL 上下文。
- 系统 Chrome 全量浏览器验收覆盖首页跨项目统计、项目级资产类型摘要、最近成果精确详情、文本正文、图片抓手拖拽、3D 模型与其余设计链路并全部通过；专项截图为 `/tmp/rail-workflow-browser-acceptance-dashboard.png`。最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元/状态测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。

## 2026-08-13：应用资产目录选择与遗留活动任务治理

- 将应用输入、完整参数抽屉、旧工作区和 Markdown 输入共用的资产选择器改为按当前项目独立读取。弹窗可逐级进入持久化文件夹和个人收藏目录，通过面包屑返回，并在当前目录按名称、业务编号或标签搜索；兼容类型筛选及创建时间、名称、类型正反序均复用资产 API 的白名单排序。
- 任务中心不再让活动任务只显示一个必然失败的删除入口。`queued`、`running` 任务显示“取消任务”：存在 `job_executions` 的真实 ComfyUI 执行进入 `cancelling` 并等待 Worker 确认；没有执行记录的孤立台账任务立即转为 `cancelled`，随后才显示软删除。取消审计补充取消模式和原状态，不删除任务、资产或审计台账。
- 任务列表 API 同步返回是否存在持久化外部执行记录。活动任务没有 `job_executions` 时，页面明确提示“无外部执行，可取消后删除”，避免把测试台账或异常孤立记录误认为真实 ComfyUI 作业；从“进行中”筛选取消成功后自动回到全部任务，以便用户立刻完成软删除。
- 定位 `TSK-00000177 / 浏览器活跃任务 / 32%` 为系统 Chrome 自动验收中断后遗留的测试台账，数据库确认其没有 Worker 执行记录和外部任务编号，不是真实 ComfyUI 运行。浏览器脚本为测试任务增加显式标记，并在每次启动前清理超过 30 分钟的验收项目及无引用验收账号，避免异常退出再次污染开发数据。
- 浏览器验收将 Markdown 输入资产放入真实子目录，覆盖进入文件夹、面包屑、类型筛选、排序、子目录选择回填和孤立活动任务取消闭环；验收启动时已清理截图中的 `TSK-00000177` 测试项目，复查数据库确认该任务不存在。
- 最终门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 25 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收、系统 Chrome 全量浏览器验收和 `pnpm build:rail`；18 份原始 ComfyUI API JSON 保持只读。

## 2026-08-13：镜头预设控件水平对齐

- 修复多视角镜头完整参数面板中“水平、垂直、距离”三组预设因标签和选项宽度不同而上下错位的问题。共享镜头控制组件改为固定两行网格：标签与下拉框统一处于首行，当前数值统一处于次行，恢复默认按钮独立占位；镜头 1 至镜头 5 及单角度能力共用该修复。
- 角度预设、拖拽环绕、滚轮距离、提示词映射和 ComfyUI 参数协议均未改变，18 份原始 API JSON 未修改。浏览器验收新增三组下拉框与数值行纵坐标误差不超过 1px 的断言。
- 验证通过：目标 ESLint/Stylelint、Web 28 项单元测试、`@rail/web` 类型检查；在本地真实设计会话的完整参数抽屉中读取计算布局，三组下拉框 `y=999.734375`、三组数值 `y=1026.734375`，确认保持同一水平线。

## 2026-08-23：甲方模块意见前端第一版适配

- 在 `codex/customer-feedback-frontend` 分支先建立原始需求基线：原样保存甲方 DOCX、22 张 PNG 和 1 个 WDP 资源，源文件与仓库副本 SHA-256 均为 `3ae9c58db091988165fb157a2c654400c7d3a6e742802abf1f0f7ee5e45a5dac`。独立 `COMMITLOG.md` 按本地提交逐条记录中文动机、范围和边界，未推送远程。
- 新增可扩展设计模式目录，将客室零部件、CMF、客室效果和报告映射到平台应用键；页面只消费 API 返回的可见应用与已发布能力契约。同类能力或版本通过目录追加，报告与 LoRA 训练缺少契约时明确待接入，不创建任务、不伪造资产。
- 设计页增加四模式切换、场景提示与动态占位；URL 应用入口和结果继续设计可自动匹配模式。现有项目隔离、会话草稿、任务互斥、参数 Schema、失败状态、资产登记、审计和 ComfyUI Worker 边界均未旁路，18 份原始工作流 JSON 修改为 0。
- 共享结果卡片增加 2/3/4+ 图片网格与 `+N` 聚合；共享图片查看器增加上一张、下一张和键盘方向键，保留缩放、拖拽、遮罩对比、3D、下载、资产保存、重新运行与继续设计。
- 首页增加六个快捷入口；新设计复用真实项目创建接口，历史弹窗使用真实最近会话，资产入口保持当前项目上下文，训练/报告入口按真实能力状态展示。平台标题与侧栏名称统一为“客运装备内装模块化分区快速设计平台”，只覆盖品牌身份字段，不清除用户主题和布局缓存；甲方未提供独立授权 PNG 标识，继续使用仓库现有标识。
- 自动化验证通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 31 项单元测试、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker 端到端验收和 `pnpm build:rail`。真实浏览器复验首页六入口、新建/历史弹窗、品牌缓存迁移、CMF/零部件模式、专属 3D/多角度能力和待接入不跳转；控制台无新增错误，仅保留既有表单迁移与存储前缀警告。当前本地没有可执行 ComfyUI 服务及含多图片成功输出的会话，因此本轮未现场生成多图，网格/前后切换只完成类型、规范和生产构建验证，待具备真实输出样本后补做视觉复验。

## 2026-08-23：0820 首页布局收敛

- 将登录默认入口、侧边菜单和页签从“设计工作台”统一命名为“首页”，移除首页中的闭环图、趋势图、任务状态、最近项目和最近成果，使 `/home` 只承担平台门户职责。
- 主内容按 0820 示意图改为居中欢迎区和 3×2 六个入口；布局对齐文档，配色统一使用平台红、浅红、雾灰和灰色边界，不使用示意图中的绿、黄标注色。
- 新设计、历史设计、资产中心和项目工作区继续走真实业务链路；模型训练与报告生成未接入时保持可点击说明，不伪造外部服务成功。
- 浏览器验收脚本同步从旧看板断言转为首页命名、入口顺序、3×2 尺寸、主题配色、旧看板不存在、新设计/历史弹窗和待接入入口回归。目标 ESLint、Stylelint、`@rail/web` 类型检查及真实浏览器复核通过。

## 2026-08-23：0820 设计主界面布局收敛

- 左侧历史会话区按文档补充独立收起/展开控制，收起后结果区占满视口；品牌全称允许换行完整显示。页面仍沿用平台红、浅红、雾灰和灰色边界，没有照搬需求图中的标注色。
- 客室零部件、CMF 和客室效果三种视觉模式统一将“文本、文生图、理解、重绘、扩图、多图、放大”作为前七个快捷工具，LoRA、单图编辑、捕获、参考重绘、多角度和 3D 等既有高级能力继续由“更多”承载；只调整展示目录，不改动能力 Schema、适配器或 18 份原始 ComfyUI 工作流 JSON。
- 报告模式按 0820 文档增加报告类型、Word/PPT 交付格式、标题说明及三项图片素材布局。当前没有已发布报告能力契约，因此控件保持禁用并明确提示不能提交，不创建任务或静态结果。
- 首页模型训练入口按文档参考补充左右分栏：参数设置包括 Repeat、Epoch、触发词和预览提示词，图片区展示打标/裁剪上传位，底部保留裁剪方式、尺寸、打标模型和开始训练。当前 LoRA 服务与数据协议未定义，全部控件禁用，不创建训练任务。
- 历史结果的“继续设计”面向用户改称“深化设计”，实际资产确认、项目范围与目标应用输入逻辑保持不变。模式目录单测、目标 ESLint、Stylelint、Web 类型检查通过；真实浏览器确认工具顺序、任务栏折叠和报告待接入布局。
- 仓库级规范、97 项单元测试、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker 端到端与生产构建通过。全量 Playwright 脚本已覆盖本轮首页和设计页后，在首次真实能力提交处按预期收到 `ADAPTER_NOT_CONFIGURED`，原因是当前本地 API 未配置 ComfyUI 地址；未写入假地址或伪造成功。首页、训练布局、任务栏、工具顺序和报告布局已使用当前运行中的真实浏览器单独复验。

## 2026-08-23：0820 工作区两级侧栏结构纠正

- 复核甲方截图后确认，上一版错误地将需求图当成功能分区示意，保留了全屏沉浸式工作台并把平台功能侧栏与任务栏合并，不符合“严格一致”的要求。
- `/design` 重新接入平台基础布局：最左侧保留首页、设计生成、项目空间、资产中心、任务中心和管理入口，顶部保留当前项目、搜索、设置与用户中心；设计页面内部只提供独立任务栏、结果区、输入区和业务模式区。重复的页面内品牌、用户信息与会话标题顶栏已移除。
- 任务栏仍可单独收起，收起后结果区扩展到设计工作区左边界，但不会覆盖平台导航。为适配双侧栏后的可用宽度，结果欢迎区、时间线、输入器和模式栏补充收缩约束，窄视口不再横向溢出。
- 目标 ESLint、Stylelint 和 Web 类型检查通过；真实浏览器确认平台侧栏、顶部栏与任务栏同时存在，任务栏折叠前后坐标关系正确，主区 `scrollWidth` 与可见宽度一致。

## 2026-08-24：0820 前端迭代交接与远程同步

- 新增 `customer-feedback/20260820/ITERATION_HANDOFF.md`，集中记录原始需求、已完成开发、13 个需求提交映射、验证证据、后续 P0/P1/P2 检查清单、外部依赖、原项目问题和第一版布局纠偏经过。
- 明确分支基线：`codex/customer-feedback-frontend` 建立在 `origin/refactor-webUI@62115a040` 上，而非直接建立在 `origin/dev` 上。后续评审应以 `refactor-webUI` 为基线，或先处理其与 `dev` 的集成，避免把此前 27 个重构提交误算成本轮修改。
- 复核确认基线后的原始甲方资源和 18 份 ComfyUI API 工作流 JSON 均无改动，本轮没有数据库迁移和平台 API 变更。按用户 2026-08-24 的明确授权，将专题分支推送至 `origin/codex/customer-feedback-frontend`，不直接修改 `origin/dev`，不推送至 `upstream`。

## 2026-08-24：0820 甲方需求逐条分析与确认材料

- 以仓库内 SHA-256 已校验的甲方原始 DOCX 为唯一需求源，重新解析 15 页正文和 22 张嵌入图片；不把现有实现、交接概述或聊天口径反向写成甲方原始要求。
- 新增 `CUSTOMER_REQUIREMENTS_ANALYSIS.md` 和同内容 PDF：按章节与业务语义拆分 129 条需求，标记“明确、参考、待定义”三种成熟度，保留来源章节，并对全部 22 张原文配图逐图说明可用于确认的界面结构或能力意图。
- 集中列出 42 项 P0/P1/P2 确认问题和 10 项不合理或高风险表述，重点覆盖项目/会话/任务层级、侧栏完整范围、工具顺序、外部 API、报告、LoRA、提示词词库、三维、资产跨领域限制、Logo 授权、浏览器保存地址及非功能验收。
- PDF 使用 A4 横向布局，共 41 页；逐页渲染检查后修复长需求编号换行，最终确认中文字体、图片比例、表格、章节转场均无裁切、重叠或缺字。Markdown 图片引用、PDF 文本提取、129 个需求 ID 唯一性和 Git 差异空白检查均通过。

## 2026-08-24：本地与远程甲方反馈分支整合

- 将本地 `codex/client-feedback-20260820` 与远程 `origin/codex/customer-feedback-frontend` 的独立实现合并：以远程的首页门户、平台侧栏 + 任务栏两级设计工作区、四模式能力目录和 2/3/4+ 多图组件为交互基线，保留本地平台 API 对工作流暂存结果选择项目资产目录入库的真实链路。
- 合并后只保留 `src/modules/platform/design-modes.ts` 作为业务模式单一配置来源；移除已无运行时引用、且包含未获甲方确认提示词选项的重复 `design-modules.ts`，避免双重目录和静态词库被误认为正式业务配置。
- 浏览器验收同时覆盖三图网格与连续全屏查看、结果保存目录数据库落点、两级侧栏、全局 AI 助手、四种模式和待接入边界。核心文档同步移除泛化“2D 生 3D”能力表述，现有多视图三维仅作为已登记 ComfyUI 工作流描述。
- 合并后完整门禁通过：`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 32 项单元测试、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker 端到端、系统 Chrome 全量浏览器验收和 `pnpm build:rail`。浏览器验收生成 20 张截图，页面错误列表为空且没有请求公网 Iconify 接口。

## 2026-08-25：首页真实数据视觉重设计

- 根据用户提供的 AI 生成参考图重新设计 `/home`，只提取大幅主视觉、数据层级、六项功能卡片和最近工作区的信息组织方式；没有复制参考图中的虚构案例、平台动态、模型数量、CPU、内存或在线人数。
- 首页顶部以本地 CSS/SVG 绘制轨道客室空间线稿，四项指标直接展示 `/dashboard` 返回的可访问项目、已保存资产、当前用户设计会话和已发布应用数量；最近项目、最近设计和任务动态同样使用权限过滤后的真实持久化记录，缺少记录时展示空状态。
- 六项入口保持甲方既定顺序与真实链路：项目创建、历史会话、资产中心和项目空间继续复用现有 API/Store；LoRA 训练与报告生成缺少能力契约时继续显示“待接入”，不创建任务或伪造结果。宽屏、平板和手机分别使用六列、三列和单列重排，390px 视口检查无横向溢出。
- 浏览器验收断言同步改为验证主视觉、四项数字、六入口、三个真实最近工作面板及交互边界；README、产品需求、架构与质量门禁同步更新。`pnpm lint`、`pnpm typecheck:rail`、Platform API 65 项与 Web 32 项单元测试、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker、生产构建和系统 Chrome 全量浏览器验收全部通过；另以 1600×1000 和 390×844 视口复核标题完整成行、窄屏重排及零横向溢出。

## 2026-08-25：AI 助手切换本地 vLLM Qwen3-VL

- AI 助手外部适配器改为严格的 OpenAI Chat Completions 请求，不再发送 GeekAI 专用 `sess_id`；本机配置指向 Docker vLLM `127.0.0.1:18081`，模型名与服务端 `/v1/models` 返回的 `qwen3-vl-8b-instruct` 一致。浏览器只读取 `vLLM · qwen3-vl-8b-instruct` 状态，不获取服务地址或密钥。
- 会话、消息和附件继续写入既有 PostgreSQL/MinIO，不新增迁移。每次请求按时间顺序读取当前用户最近 50 条已完成消息；图片从私有对象存储读取并转换为 Data URL，避免 vLLM 容器无法访问宿主机 `localhost:9000`。最近图片上下文限制为 4 张、总计 20 MiB，优先保留最新轮次。
- 全局助手的文件选择器保持 `multiple`，增加图片数量提示和前后端双重限制；一条消息可发送单张或最多 4 张图片，发送后仍按消息持久化并支持同一对话连续追问。非图片附件仍可保存、预览和下载，但 Qwen3-VL 请求不会伪称已读取其正文。
- 配置模板、生产 Compose 环境透传、产品需求、架构、质量门禁和部署手册同步更新；真实密钥只保存在 Git 忽略的本机 `.env`。
- 真实联调确认 vLLM `/v1/models` 返回 `qwen3-vl-8b-instruct`；平台两轮对话依次返回“已记住。”和上一轮代号 `RAIL-4821`，数据库读取到 4 条有序消息；同一轮上传两张仓库内非敏感 PNG 后，Qwen3-VL 返回图片数量 `2` 且 `serviceError = null`。验证通过 `pnpm lint`、`pnpm typecheck:rail`、Platform API 67 项与 Web 32 项单元/状态测试、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker 端到端和 `pnpm build:rail`。登录后的真实浏览器复核确认模型标识、图片数量提示、多轮记录和最终回复均正常显示。

## 2026-08-25：AI 助手消息发送时序优化

- 修复发送后必须等待 AI 接口全部返回才显示用户输入的问题。前端现在先创建仅用于界面的乐观消息，立即清空输入框、把文字及本地附件预览插入消息流并滚动到最新位置，随后才创建会话、上传附件和请求 vLLM；服务端持久化协议、用户隔离和历史消息读取方式未改变。
- 用户消息统一显示在聊天区左侧；等待期间用户气泡标记“正在发送…”，其后显示独立的“正在生成回复…”动画占位。接口成功后以数据库中的正式用户消息和助手回复替换临时状态；发送或上传失败时移除临时气泡并恢复输入内容，避免把未落库消息伪装为成功。
- 真实浏览器发送长响应测试时，在 vLLM 尚未返回的快照中已同时读取到测试问题、“正在发送…”和“AI 正在生成回复”；完成后占位消失，正式回复持久化显示。页面控制台无新增错误，仅保留仓库既有表单迁移和存储前缀警告。
- 本次组件专项 ESLint、Stylelint、Web 类型检查、Platform API 67 项测试和生产构建通过。仓库全量 lint 与 Web 单测分别被并行工作区中新建的 `auth.test.ts` 导入顺序、以及其期望 `/home` 但当前 `auth.ts` 实际跳转 `/workspace/overview` 的不一致阻断；该文件不属于本次 AI 助手改动，未擅自改写。

## 2026-08-25：AI 助手消息角色与历史会话管理

- 用户消息恢复到聊天区右侧，并在气泡右边增加用户图标；用户与 AI 头像统一为 28×28 像素、15 像素图标字号，通过位置、图标和颜色区分角色。此前的乐观发送与 AI 等待占位时序保持不变。
- 原生历史会话下拉替换为紧凑管理面板：支持按会话名称和项目名即时筛选，按最后消息时间从新到旧或从旧到新排列，列表同时显示紧凑时间和项目名称；项目上下文区域左右留白同步缩减。
- 新增 `PATCH /api/v1/assistant/conversations/:id`。标题经过 Zod 去空格、非空和 120 字符限制，只能更新当前登录用户自己的 `ai_conversations` 记录；审计只记录标题长度，不记录标题正文。真实集成验收覆盖重命名持久化及另一用户越权返回 404。

## 2026-08-25：AI 助手 Markdown 回复与复制

- vLLM 返回的消息正文继续以原始字符串持久化和参与多轮上下文，不修改 API 协议；全局 AI 助手只在展示层将助手消息解释为 Markdown，用户消息仍按纯文本渲染。
- 复用并增强平台安全 Markdown 组件，支持标题、段落、粗体、斜体、有序/无序列表、引用、代码块、行内代码和 `http`、`https`、`mailto` 链接。实现不使用 `v-html`；不安全协议保留为纯文本，避免模型输出注入可执行 HTML 或 `javascript:` 链接。
- 每条助手消息底部增加复制图标，复制数据库保存的原始 Markdown，并显示“回复内容已复制”反馈。真实浏览器确认既有回复中的 `**第一名**` 和 `**韩愈**` 已渲染为粗体，有序列表和引用结构正确，页面存在逐条复制按钮且点击后成功提示可见；验证过程未读取剪贴板内容。
- 用户消息继续保持右侧布局和用户头像，但气泡背景、边框、文字及时间颜色改为与助手回复一致的默认主题色；深色主题同样复用双方统一的消息底色，仅通过位置、头像和气泡圆角区分角色。

## 2026-08-25：AI Toolkit LoRA 训练正式接入

- 将 `/model-training` 从全部禁用的展示页替换为真实项目训练页：支持选择或上传当前项目图片、逐图 caption、Repeat、Epoch、rank、学习率、分辨率、触发词、预览提示词、总步数校验、提交、状态轮询、停止、请求 checkpoint、增量日志、Loss 曲线和模型资产入口。
- 新增 `026_lora_training.sql`，以 `lora_training_executions` 保存平台任务和 AI Toolkit 任务映射，以 `lora_artifact_receipts` 保证模型回流幂等；应用目录的 LoRA 输入收敛为 `image`、输出为 `model`，不新增资产文件类型。
- 平台 API 新增受控 `/api/v1/lora/*` 业务接口。浏览器只提交资产 ID、caption 和白名单参数，不能提交训练服务器路径或完整 `job_config`；全部路由校验登录、权限与项目范围，审计不记录 caption 正文、训练日志或 Token。
- 独立 Worker 进程同时运行 ComfyUI 与 AI Toolkit LoRA 执行器：读取 MinIO 项目图片、创建并上传同名图片/caption 数据集、按 `TSK-*` 设置 `job_ref`、分别启动任务与 GPU 队列、同步进度、处理安全停止，并将最多 4 个 safetensors checkpoint 回存 MinIO、登记为带任务血缘的项目 `model` 资产。
- 训练模板以本机已跑通的 `flux2_klein_9b_interior_lora.yaml` 为基线，保留 Flux2 Klein 9B 权重/VAE、qfloat8、bf16、adamw8bit、flowmatch 和缓存设置；AI Toolkit 实际 `DATASETS_FOLDER` 优先从后端设置读取，环境变量作为受控回退。
- 新增 LoRA 客户端、模板和生产配置校验测试，专项 3 个文件 9 项通过；完整单元测试通过（Platform API 22 个文件 71 项、Web 8 个文件 34 项），`pnpm typecheck:rail`、真实 PostgreSQL/MinIO 集成验收、模拟 ComfyUI Worker、生产 Compose 配置检查、`pnpm build:rail` 和 Git 空白检查通过。真实浏览器已验证适配器状态、当前项目图片选择、caption、参数校验、禁用提交和 1600/800/600px 响应式布局；控制台无新增错误。当前本机 AI Toolkit 未启动且平台未配置 `LORA_API_URL`，真实 API 验证返回 `503 ADAPTER_NOT_CONFIGURED` 且数据库未产生任务或执行记录；因此真实 GPU 训练及 safetensors 回流尚未现场验证。仓库全量 lint 只被本轮开始前已存在的 `apps/web-antd/src/api/platform/assistant.ts` 格式差异阻断，LoRA 相关 ESLint、Stylelint 和格式检查均通过，未擅自改写该并行工作文件。
- 根据首页入口反馈修正接入层级：`/home` 的“开始模型训练”卡片不再依赖通用应用目录的 `capabilityCode` 判定，也不再错误显示“训练服务接入后/待接入”；首页直接请求 `/lora/status`，显示“检测中、已接入、未配置或服务异常”、真实模型/GPU 或后端原因，并始终进入正式训练页。真实浏览器在未配置环境读取到“平台 API 尚未配置 LORA_API_URL / 未配置”，点击正确进入 `/model-training`，页面错误日志为空。
- 根据训练页反馈完成本机配置与参数界面收敛：确认 AI Toolkit `http://127.0.0.1:3000/api/datasets/list` 返回 200、`DATASETS_FOLDER` 可读取，Git 忽略的 API `.env` 已配置 URL、GPU、数据集回退目录和已验证权重/VAE；重启平台后 `/lora/status` 返回 `configured=true`、`reachable=true`、GPU 0。基础模型改为后端白名单下拉参数，当前只开放已验证的 Flux2 Klein 9B，模型键写入任务、模板、审计元数据和输出资产标签，权重路径不返回浏览器；非法模型键真实返回 400，数据库确认未创建任务。训练页按参考图改为左侧参数、右侧训练集，并增加与真实模板同步的专业设置分类；真实浏览器确认连接成功、底模下拉、滑杆、专业设置弹窗和双栏布局，优化器/网络分类映射正确且页面错误日志为空。验证通过 LoRA 专项 9 项、完整 Platform API 73 项与 Web 34 项测试、类型检查、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker、目标 ESLint/Stylelint、生产构建和 Git 空白检查。仓库全量 lint 仍被并行工作区中的报告 Worker 与既有助手 API 三个非本轮文件的格式差异阻断，未擅自改写。

## 2026-08-25：报告生成模块正式接入

- 将 `/report-generation` 从禁用占位页替换为真实报告工作台，支持四类报告、DOCX/PPTX、标题、摘要、最多 8 个有序章节、每章正文与图片、章节/图片排序、当前项目资产选择和 PNG/JPEG 本地上传。设计页选择“报告生成”模式时进入专属页面。
- 新增 `027_report_generation.sql` 和受控 `/api/v1/reports/*` 接口。API 强制登录、任务写权限、项目写范围、图片登记状态、对象版本、PNG/JPEG MIME、100 MiB 总容量及章节/图片数量；通用任务接口拒绝旁路提交，审计不记录正文和图片说明。
- Worker 新增内置 OOXML 执行器，以租约领取任务、读取 MinIO 图片、生成 Word/PowerPoint、最多重试 3 次，并将输出自动登记为同项目 `document` 资产。任务创建时预分配输出资产 ID 和对象键；对象写入后数据库事务失败会补偿删除对象，取消请求不会产生输出资产。
- Word 使用 A4 封面、页眉页脚、摘要、章节、等比图片与图注；PowerPoint 使用 16:9 封面、报告概览、章节分页、最多双图布局、图注、页码和素材备注。新增 `docx`、`pptxgenjs`、`image-size` 运行依赖及渲染器单元测试。
- 样例 Word 3 页和 PowerPoint 5 页均使用专用渲染工具逐页检查；PPT 越界检测通过，并修复了首轮样例中的孤立项目符号。真实浏览器分别生成 `TSK-00001881`（DOCX）和 `TSK-00001882`（PPTX），两项均完成对象存储、任务台账、输出资产和下载入口；390×844 视口 `scrollWidth=innerWidth=390`，控制台 error 为空。
- 根据首页入口反馈移除训练服务正常时的“已接入”标签；未配置、服务异常和检测中状态仍保留明确诊断。报告生成已是平台内置能力，首页不再按外部 `capabilityCode` 显示“待接入”，操作文案由“查看状态”改为“开始生成”。专项格式检查、Web 类型检查和真实浏览器复核通过，入口可正确跳转 `/report-generation`，页面无新增控制台错误。
- 根据报告工作台反馈新增模板/AI 生成方式下拉，迁移 `028_report_generation_modes.sql` 持久化 `generation_mode`。AI 路径只由后端 Worker 调用 `REPORT_AI_API_URL`，以 multipart 发送受控文字和重复 `images` 文件字段，校验超时、上游状态、输出大小、ZIP 与 Word/PPT OOXML 关键条目；外部服务错误正文不进入任务、审计或日志。未配置时返回 `ADAPTER_NOT_CONFIGURED`，不会伪装成模板产物。
- 图片入口收敛为章节内的紧凑“添加图片”下拉，可从项目资产多选或从本地文件多选；“添加章节”移动到最后一章之后。移除顶部适配器就绪标签，最近任务新增“AI 生成/模板生成”标签，历史任务兼容显示为模板生成。
- 真实环境应用迁移后，模板任务 `TSK-00001895` 成功生成 `AST-00002543`；无图 AI 任务 `TSK-00001896` 成功生成 `AST-00002544`；从资产弹窗一次选择两张图片后，AI 任务 `TSK-00001897` 成功生成 4,381,943 字节的 `AST-00002545`，数据库确认 `generation_mode=ai`、`template_key=general` 和两项输入血缘。报告页面控制台 error 为空；添加图片按钮实测为 106×24px，本地文件输入具有 `multiple` 属性，添加章节按钮位于最后章节下方。
- 报告适配器、Schema、内置渲染器和生产配置定向 4 个文件 17 项测试通过，`pnpm typecheck:rail` 通过。外部服务仍是同步接口且没有取消端点：平台可阻止取消任务入库，但无法保证立即终止上游计算。

## 2026-08-26：报告生成页面布局与信息层级统一

- 移除报告工作台和最近任务面板原有的 1180px 最大宽度，两块内容改为与模型训练页一致的全宽自适应网格、18px 区块间距和 20px 面板内边距；1600×1000 视口下内容、工作台和任务面板均从 84px 延伸至 1567px，实际宽度 1483px，不再在右侧留下空白。
- 参照模型训练页增加 1“报告基本信息”、2“报告章节”、3“最近报告任务”三个红色圆形步骤标识，移除工作台中的英文眉题。区块标题统一为 15px/700，表单正文和输入为 13–14px，辅助信息与任务元数据统一为 12px，任务标题统一为 14px/700，保持清晰但克制的四级信息层级。
- 最近任务列表设置 420px 固定可视高度并在面板内部使用双向滚动；任务行最小宽度 720px，窄屏时不会压缩标题、状态和操作。真实浏览器验证 7 项历史任务时列表 clientHeight=420、scrollHeight=678；390×844 视口页面 scrollWidth=clientWidth=390，列表内部 clientWidth=306、scrollWidth=726，步骤顺序为 1/2/3，最终新页面控制台 error 为空。
- 本次报告页面专项 Oxfmt、ESLint、Stylelint、Web 类型检查和生产构建通过。
- 图片来源弹层进一步收敛为单行双图标：项目资产和本地上传均使用 34×34px 图标按钮，移除重复文字，保留 `aria-label` 并在鼠标悬浮时显示完整说明。生成方式标签、下拉选项和生成按钮统一为 14px/500，按钮与下拉统一为 32px 控件高度。
- 模型训练双栏以左侧参数内容高度作为整行基准，右侧训练集使用零基础高度的弹性区域跟随左侧等高，图片超出后只在训练集网格内部滚动；单栏窄屏恢复 390px 紧凑滚动高度。真实浏览器在 1280px 视口、101 张项目图片下读取左右面板高度均为 796.08px、底边偏差为 0，训练集 `clientHeight=626`、`scrollHeight=3704`、`overflow-y=auto`，页面控制台 error 为空。报告页图标菜单保持单行双入口并提供悬浮说明。

## 2026-08-26：项目空间按参考图重构

- 按用户提供的项目空间参考图重构 `/projects`，移除英文眉题，在真实项目汇总之下增加项目总数、项目资产、累计任务和运行任务四项数据卡；所有数字继续由项目 API 返回的权限范围数据聚合，不增加静态项目、假任务或浏览器本地业务数据。
- 项目列表改为统一白色容器和双列紧凑卡片，补齐项目文件夹色块、编号、说明、更新时间、成员概览、置顶/编辑/软删除操作、四项可点击统计以及唯一的“开始设计”主操作；保留卡片整体键盘入口、悬浮反馈、搜索、服务端排序、成员管理和现有失败路径。
- 按参考图增加网格/列表视图切换、12/24/48 条分页选择和空结果状态；宽度不足时汇总卡与项目卡逐级重排，手机端统计改为两列并保持操作可见。
- 浏览器验收断言同步检查四项汇总、两种视图入口、新编号层级与紧凑统计字号。目标 Vue 与浏览器脚本 ESLint、Vue Stylelint、Web 类型检查通过；真实本地浏览器确认 4 个真实项目以 2×2 网格显示，搜索、视图切换、分页和既有项目操作入口均可访问，控制台没有新增 error。
- 根据后续页面批注将四项汇总卡收敛为不可交互的语义化 `article`：移除重复跳转、箭头、悬浮与键盘焦点反馈，并将标签、数字和单位改为更紧凑的基线对齐布局，避免与下方项目统计重复表达可点击能力。
- 项目列表 API 与成员 API 在既有项目范围校验后返回最多三名成员的姓名、业务 ID 和私有对象存储短时头像 URL。项目卡和成员弹窗优先显示真实头像，无头像时显示姓名首字；头像放大到 36px，并移除没有业务含义的 `+0`。集成与浏览器验收同步覆盖真实头像 URL、头像尺寸和 `+0` 不再出现。
- 项目卡成员区按参考图收敛为最多三个重叠头像；总人数不超过三人时不显示附加数字，超过三人时显示 `+N` 圆形按钮。点击 `+N` 后才调用受项目范围保护的成员接口，展示含总人数、即时筛选、头像、姓名和账号的紧凑锚点弹层；原“项目成员”指标仍打开包含邀请、贡献与移除能力的完整管理弹窗。
- 成员头像展示区阻止项目卡点击事件继续冒泡，普通头像不再错误进入“开始设计”，只有 `+N` 保持成员弹层交互。通过真实注册与项目成员 API 创建 `demo_member_a` 至 `demo_member_d` 四个普通演示账号并以只读成员加入“录屏测试项目1”，该项目现有 6 名成员；浏览器确认卡片显示 3 个头像和 `+3`，点击普通头像仍停留 `/projects`，点击 `+3` 可读取全部 6 人并按姓名筛选，控制台 error 为空。
- 验证通过前后端类型检查、Platform API 25 个文件 82 项与 Web 8 个文件 34 项测试、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker 端到端及 `pnpm build:rail`。真实浏览器确认四张汇总卡均为不可点击且不含箭头；当前 5 个成员头像位中 3 个读取真实预签名图片、2 个显示姓名首字，项目页面控制台 error 为空。
- 根据报告页批注修复章节图片来源菜单：将未渲染的 `lucide:folder-image` 替换为项目已使用的 `lucide:folder`，菜单改为两个 34px 图标按钮的内容宽度网格，并锁定按钮最大宽度，避免下拉层跟随“添加图片”触发按钮被撑宽后在本地上传图标右侧留下多余空白。浏览器验收同步检查两个 SVG 图标、按钮尺寸和左右留白对称性。
- 最终目标 ESLint、Stylelint、前后端类型检查和生产构建通过；登录后的真实浏览器定向确认项目汇总卡标签/数字/单位为 13/28/12px、四卡均无按钮与箭头，报告菜单两个图标均显示且留白对称，两页均无新增控制台 error。全量浏览器脚本使用系统 Chrome 启动后，在到达本次页面前被既有首页 LoRA 状态断言“仍使用旧的待接入状态”阻断，本次未将其误记为全量通过。

## 2026-08-26：项目图片缩略图完整展示与资产卡片收敛

- 根据资产中心页面批注调整网格卡片的信息比例：预览区由固定 142px 改为最小 180px 的 4:3 主视觉区；正文内边距收窄，类型、标题、业务 ID、说明、标签和创建元数据降低字号与间距，说明固定为一行、标签固定为一行，元数据长文本使用省略号，避免文字区继续挤占图片空间。列表视图单独恢复 132px 紧凑预览，不影响横向浏览效率。
- 资产中心、项目资产选择器、模型训练集、首页最近资产、设计输入附件、任务输入与结果画廊、AI 助手附件中的业务图片统一使用 `object-fit: contain`，完整保留宽图和竖图边缘；项目成员头像、用户头像和车厢透视合成等必须铺满或依赖裁切的视觉继续使用 `cover`，没有进行全局粗暴替换。
- 浏览器验收新增资产卡片图片适配方式和图文比例断言。真实浏览器在 24 项资产下确认四列卡片能够完整显示座椅、车厢宽图和竖向素材，报告页项目资产选择器 23 项图片同样完整展示，模型训练页读取 24 张真实缩略图；定向检查期间控制台 error 为空。
- 根据进一步批注明确收敛网格卡片正文：卡片默认只显示类型、名称、资产业务 ID 和标签，将生成说明、创建人、版本与精确时间从网格卡片隐藏；点击卡片后的详情仍提供全部字段，列表视图也继续保留完整信息。类型与业务 ID 合并为首行，标题和标签各占一行，使正文稳定缩短为图片下方的三行紧凑信息。

## 2026-08-26：站内通知完整列表与弹层布局修正

- 顶栏通知弹层的“查看所有消息”不再跳转 `/jobs`，改为打开“全部通知”站内弹窗；弹窗直接复用当前用户真实通知 API 返回的数据，展示标题、正文、时间与未读状态，并支持单条标记已读、删除、全部标记已读和清空全部。带业务链接的通知仍可从列表点击进入对应资源。
- 通知摘要项由绝对定位操作按钮改为三列网格，头像、正文和已读/删除图标各自占位；正文增加最小宽度和任意位置换行约束，长消息不再与右侧图标重合。浏览器验收新增 URL 不变、通知弹窗可见和正文/操作区边界不重叠断言。

## 2026-08-26：设计任务栏收起控件弱化

- 根据设计页批注将任务栏边缘的双箭头、红色描边和阴影收敛为低对比单箭头；控件保持 48px 点击高度，默认透明且弱化，仅在悬停或键盘聚焦时显示轻量背景与清晰反馈。目标 ESLint、Stylelint、前后端类型检查和 Git 空白检查通过；真实浏览器确认收起/展开均可切换，页面控制台无新增 error。

## 2026-08-26：报告生成新增 Markdown 交付格式

- 报告交付格式在 DOCX、PPTX 基础上新增 Markdown（`.md`），并继续与“模板生成 / AI 生成”正交组合。状态接口、前端类型、任务参数、文件名、MIME、通知和页面按钮统一识别 `md`，数据库通过 `029_report_markdown_format.sql` 放开持久化格式约束。
- 模板方式生成 UTF-8 自包含 Markdown，保留报告标题、类型、项目、日期、编制人、摘要、目录、有序章节、正文、图片和图注，图片使用 Data URL 内嵌。AI 方式按本机服务公开的 `GeneratedFileType` 契约发送 `type=md`，只接受非空、无 NUL 的有效 UTF-8 文件；未配置或上游返回无效内容时明确失败，不回退模板伪装成功。
- Markdown 报告作为 `text` 项目资产登记，DOCX/PPTX 继续作为 `document`；三种格式共用对象存储、任务、审计、标签和输出血缘。报告应用输出契约同步更新为 `document` 与 `text`，单元、冒烟和浏览器验收覆盖范围扩展至三种格式与两种生成方式。
- `029` 迁移已在本地开发库成功应用。目标报告测试 13 项、完整 Platform API 85 项与 Web 34 项测试、前后端类型检查、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker 端到端、三格式冒烟和 `pnpm build:rail` 均通过；真实浏览器分别以模板任务 `TSK-00001946`、AI 任务 `TSK-00001948` 生成 Markdown 资产 `AST-00002611`、`AST-00002612`，数据库确认二者均为 `kind=text`、`mime_type=text/markdown`，页面控制台 error 为 0。首次 AI 联调因按错误枚举发送 `type=markdown` 获得真实 HTTP 422，随后依据服务 OpenAPI 修正为 `type=md` 并复验成功，失败任务保留为可追溯证据。仓库全量 lint 仅被本轮开始前已存在的 `apps/platform-api/api/v1/projects/[id]/members/index.get.ts` 与 `apps/web-antd/src/api/platform/assistant.ts` 两处格式差异阻断；本轮报告相关 ESLint、Stylelint、格式和 Git 空白检查均通过，未擅自改写并行工作文件。

## 2026-08-26：登录页品牌 Logo 与标题等高

- 登录页左上角品牌 Logo 从独立的 42px 固定宽度收敛为 28×28px，并将平台标题行高显式统一为 28px；图标与文字共享同一垂直中线和等高占位，保留原有 8px 间距，不影响侧栏、顶栏及用户头像中的品牌图标尺寸。
- 目标 ESLint、Stylelint、格式检查、前后端类型检查、Git 空白检查和 `pnpm build:rail` 通过。真实匿名登录页在默认视口下测得 Logo 与标题渲染高度均为 26.25px（浏览器缩放后的 28px 设计值）、垂直中心偏差为 0；390×844 视口下仍保持等高和零中心偏差，页面横向溢出为 0，独立页面控制台 error 为 0。

## 2026-08-26：侧栏补充完整平台品牌名称

- 展开侧栏的品牌位在 Logo 右侧单行显示“客运装备内装模块化分区快速设计平台”，使用 14px 字号和 32px 行高，文字行与 32px Logo 严格等高并共享垂直中线。侧栏发布宽度统一为 320px，确保完整品牌名称不换行、不裁切；文字只在展开状态渲染，不改变原有侧栏隐藏行为。平台启动时固定同步 `logo.showText=true` 与侧栏宽度，避免旧偏好缓存继续隐藏或压缩发布品牌名称。
- 浏览器验收新增品牌全文、单行状态、Logo/文字等高、中心对齐和无裁切断言；使用系统 Chrome 的真实验收已通过上述首页品牌断言，随后在既有模型训练服务状态验收处阻断，与本轮侧栏改动无关。目标 ESLint、Stylelint、格式检查、前后端类型检查、Git 空白检查和 `pnpm build:rail` 通过；失败验收未残留本轮 `rail_ui_*` 测试账号。

## 2026-08-26：平台侧栏收敛与设计工作台治理入口

- 平台侧栏固定为“首页、设计生成、模型训练、资产中心、报告生成、设计工作台”六个入口；模型训练与报告生成从隐藏路由提升为一级菜单，原“项目空间”更名为“设计工作台”。任务中心、平台管理和操作日志仅从侧栏隐藏，原路由、后端权限和持久化台账全部保留。
- 设计工作台继续承载全部可访问项目、四项真实汇总、项目搜索/排序、项目任务及成员入口，并新增“平台管理与记录”区域。管理员可进入用户与权限、工作流管理和全平台操作日志；普通用户只显示自身操作日志入口，角色差异由当前用户角色生成，服务端 RBAC 与日志范围保持不变。
- 项目累计任务和运行任务继续切换当前项目后进入 `/jobs`，因此移除全局任务菜单不会丢失项目任务能力。旧 `/workspace/overview` 继续兼容重定向 `/projects`，既有 `/administration/**`、`/audit` 和 `/jobs` 深链接不失效。
- 新增导航与角色工具单元测试，并扩展全量浏览器验收断言。真实管理员浏览器读取六项菜单纵坐标依次为 61/105/149/193/237/281，任务中心、平台管理、操作日志菜单计数均为 0；三个工作台治理按钮可分别进入原受保护页面。390×844 视口 `scrollWidth=clientWidth=390`，治理工具为单列，控制台 error 为 0。
- Platform API 25 个文件 85 项与 Web 9 个文件 37 项单元测试、前后端类型检查、生产构建、目标 ESLint/Stylelint/Oxfmt 和 Git 空白检查通过。仓库全量 lint 仅被本轮开始前已存在的项目成员 API 与助手客户端两处格式差异阻断，本轮未改写这两个并行工作文件。本次无数据库迁移或部署配置变化。

## 2026-08-26：平台品牌标志统一替换

- 依据客户提供的 Adobe Illustrator/PDF 源文件提取中国中车红色图形标志，去除“中国中车 CRRC”黑色文字和原有红底列车图案，生成透明背景、正方形视区的轻量 SVG；未引入位图缩放或外部运行时资源。
- 登录页、平台侧栏、默认头像、站内通知头像和浏览器页签图标统一复用 `/rail-logo.svg`。资源引用增加版本参数以刷新浏览器缓存，深浅色主题继续使用同一品牌红色标志。
- 平台侧栏顶部移除“客运装备内装模块化分区快速设计平台”可见文字，仅保留 32×32 像素标志；登录页的平台名称和浏览器标题保持不变。本次无数据库迁移、API 契约或部署配置变化。
- 真实登录浏览器在 `/home` 复核确认侧栏链接可见文本为空、标志尺寸为 32×32 像素、favicon 指向新版资源，页面控制台 error 为 0。
- 目标 ESLint、Oxfmt、前后端类型检查、SVG 结构校验、Platform API 25 个文件 85 项与 Web 9 个文件 37 项单元测试、生产构建和 Git 空白检查通过。仓库全量 lint 仍仅被本轮开始前已存在的项目成员 API 与助手客户端两处格式差异阻断，本次未改写这两个并行工作文件。

## 2026-08-26：设计工作流轮次信息与多图结果收敛

- 设计生成页和独立应用工作区继续共用同一轮次组件，并统一移除用户输入气泡上方的字段标签、能力版本、结果保存状态和工作流输出文件名；轮次、时间、执行状态、结果正文/图片及复制、下载、保存、流转、重跑等必要操作保持不变。
- 多图生成结果按客户参考调整为主次结构：第一张占上方整行，第二张位于左下，第三张位于右下；超过三张时第三格以 `+N` 表示第一、二张之外的剩余图片数量。点击任一预览继续打开全屏查看器，并支持按钮与键盘左右切换全部结果。
- 报告生成模式登记为独立正式页面入口，不再依赖通用 ComfyUI 能力目录判断可用性，不显示“待接入”；点击后直接进入 `/report-generation`。同步删除设计页中已经失效的报告占位弹窗与“不能提交生成”说明。
- 浏览器验收脚本补充轮次冗余信息缺失、三图主次几何布局、报告入口可用状态和正式页面跳转断言。真实管理员浏览器在含 4 张图片的任务中读取到主图 651.91×239.13px、下方两图各 321.95×119.56px，第三格显示 `+2`；首图全屏时仅显示下一张按钮，切到中间图片后左右按钮同时存在。用户输入标签、能力版本、结果标签和结果文件名计数均为 0，报告页正式表单可见，两页控制台 error 为 0。
- 目标 ESLint、Stylelint、Oxfmt、前后端类型检查、Platform API 25 个文件 85 项与 Web 9 个文件 37 项单元测试、生产构建和 Git 空白检查通过。仓库全量 lint 仍仅被本轮开始前已存在的项目成员 API 与助手客户端两处格式差异阻断，本次未改写这两个并行工作文件。

## 2026-08-26：平台图片展示背景统一

- 工作流单图、多图和前后对比结果的图片内容区由深色底改为白色；多图中的 `1/2/3` 数字角标全部移除，超过三张时仍保留 `+N`，确保用户知道还有未展示结果。
- 共用全屏图片查看器移除深色棋盘格，改为白色背景；因此设计结果、工作流输入和资产中心图片放大均同步生效，缩放、拖拽、左右切换和键盘方向键能力不变。
- 资产中心卡片与详情、模型训练集、报告章节图片、首页最近图片、资产选择器和工作流输入预览中的实际图片区域同步使用白色背景；非图片文件占位图标继续保留浅色或业务强调色，遮罩编辑、摄像捕获、区域绘制等专业画布不作为普通图片展示背景处理。
- 浏览器验收脚本增加多图白色背景、无数字角标和全屏查看器白色背景断言。真实 4 图任务读取三块预览背景和放大查看器背景均为 `rgb(255, 255, 255)`，数字角标计数为 0、`+2` 保持可见，页面控制台 error 为 0。
- 目标 ESLint、Stylelint、Oxfmt、前后端类型检查、Platform API 25 个文件 85 项与 Web 9 个文件 37 项单元测试、生产构建和 Git 空白检查通过。本次无数据库迁移、API 契约或部署配置变化。

## 2026-08-26：设计业务模式入口卡片化

- 设计输入器下方的“客室零部件生成、CMF生成、客室效果生成、报告生成”由文字胶囊入口改为同一行的宽幅主题卡片；四张背景分别使用零部件、材质样板、客室空间和报告文档视觉，统一采用深色左侧文字区与右侧主题主体，不包含文字、品牌或外部运行时资源。
- 页面进入、切换会话或从报告页返回时四项默认均不选中；只有用户显式点击普通设计模式后才显示单项红色选中反馈。内部当前能力模式、会话草稿、应用选择和报告独立路由保持原有真实逻辑，未用视觉状态替代业务状态。
- 卡片区域为四列等宽网格并保留横向溢出能力，末端预留悬浮 AI 助手间距。真实 1176×1286 浏览器读取四卡高度均为 44px、默认 active 计数为 0、四项背景 URL 均来自 `/design-modes/`；点击零部件模式后 active 计数为 1，报告入口仍进入 `/report-generation`，返回后恢复无选中，页面本次加载后无新增控制台 error。
- Web 9 个文件 37 项测试、目标 ESLint、Stylelint、Web 类型检查、生产构建和 Git 空白检查通过。本次无数据库迁移、API 契约或部署配置变化。
- 根据后续批注移除卡片区域为悬浮助手预留的右侧空白，使首末卡片与输入框左右边缘严格对齐；卡片高度由 44px 提升至 48px，标签由 12.5px/600 提升至 14px/700，方向箭头同步放大至 14px。

## 2026-08-26：CMF 专属工具、提示词模板与结果操作

- CMF 模式默认应用改为文生图，输入占位文案更新为连续纹样、颜色、面料材质、图案形式、风格和图像尺寸语义；未选择具体能力时严格显示“文生图、局部重绘、多图融合、图像放大、图片理解、提示词模板、更多”七项，其他设计模式的既有顺序不变。
- 新增 `030_cmf_design_mode_and_prompt_templates.sql`：任务增加可空 `design_mode`，历史轮次由服务端恢复提交时业务模式；新增 PostgreSQL 提示词模板目录并初始化颜色、材质、纹样类型、图案形式、设计风格和图像尺寸六类数据。模板读取对登录用户开放，维护使用 `platform:application:write`，更新审计只记录分类和选项数量，不保存正文。
- 提示词模板弹层采用左侧一级分类、右侧二级选项结构，每类可选一项并跨类组合写入编辑框，不自动执行；管理员可新增、删除、重排和编辑分类及选项。所有模板数据来自真实 API，不写入浏览器本地存储或前端静态业务数组。
- CMF 图片结果固定显示下载、添加至资产中心、重新绘制、局部重绘、多图融合、图像放大和图像理解七项操作。下载保持原受控下载行为；重新绘制复用不可变任务快照；局部重绘保存遮罩后进入对应编辑框；多图融合提供转入编辑框和弹窗上传两张参考图两条路径；图像放大在倍率确认后提交；图像理解只填入编辑框，避免立即生成不符合预期的任务。
- 历史轮次输入图片拆分为两个独立交互区：点击图片主体打开白底放大预览，右下角遮罩按钮默认隐藏、悬浮或键盘聚焦时显示，点击后才进入遮罩编辑器。提示词模板新增已选标签区，按分类顺序显示全部选择并支持逐项删除。
- 已应用 `030` 本地迁移。模板 Schema、模式目录、Platform API 26 个文件 87 项与 Web 9 个文件 37 项测试、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker、目标 ESLint/Stylelint、前后端类型检查和生产构建通过。系统 Chrome 全量浏览器验收已通过本次 CMF 七项工具、模板分类、已选标签增删、输入图片预览、遮罩悬浮入口和结果七项操作；套件后续在既有“设计输入器固定视口底部”布局断言处阻断，未将该无关断言误记为本轮失败或全量通过。

## 2026-08-26：侧栏品牌名称可读性增强

- 根据首页视觉反馈将侧栏品牌名称由 14px/600 提升至 15px/700，保持 32px 行高与 Logo 垂直对齐，并增加极轻字距；不改变侧栏导航、收起行为和品牌文案。

## 2026-08-26：零部件与客室效果生成模式完整化

- 将客室零部件、CMF、客室效果三种视觉模式统一收敛到可配置目录。客室零部件首屏固定显示“文生图、局部重绘、标记修改、多图融合、多角度生成、三维生成、图像放大、图片理解、提示词模板、更多”，客室效果固定显示“文生图、标记生成、局部重绘、部件/材质融合、平面图填色、环境更改、图像放大、图像理解、提示词模板、更多”；提示文案和默认文生图应用随模式切换。
- 新增持久化迁移 `031_component_and_cabin_prompt_templates.sql`，初始化零部件类型、角度、颜色、表面材质、造型、风格、尺寸，以及客室类型、画面角度、部件颜色、材质、风格、尺寸、光环境和窗外环境模板。三种模式共用真实模板 API、管理员维护、全量已选标签与逐项删除能力；环境更改入口只展示光环境和窗外环境两个分类。
- 图片结果操作改为依据任务持久化 `design_mode` 恢复。零部件结果提供下载、入资产、重绘、局部重绘、标记修改、多图融合、放大、理解、多角度和三维生成；客室效果结果提供下载、入资产、重绘、局部重绘、标记修改、部件/材质融合、放大、环境更改、理解和多角度生成。理解、环境及复合生成默认先带入编辑框，不因点击立即创建任务；弹窗直提交流程复用真实三图融合、单图多角度和前/左/后/右四视图契约。
- 输入器频繁工具区允许横向滚动，提示词模板与“更多”固定在右侧，确保窄屏仍可访问；结果输入图片主体继续打开白底放大预览，遮罩入口仅在悬浮或键盘聚焦时显示。
- 已在本地开发库应用 `031` 迁移。Platform API 26 个文件 87 项与 Web 9 个文件 38 项单元测试、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker、前后端类型检查、目标 Oxlint/Stylelint/Oxfmt 和生产构建通过。真实浏览器定向验收确认两种模式的工具顺序、占位提示、零部件模板标签增删、环境专用模板、四张业务卡对齐和控制台无新增 error；全量浏览器脚本使用系统 Chrome 时在本轮页面之前被既有模型训练服务状态断言阻断，本轮新增设计断言在此前独立运行中均已通过。真实 GPU 外部工作流未在本次 UI 验收中执行，最终任务成败仍由已配置适配器返回，不使用假结果。

## 2026-08-26：设计输入图片托盘与固定数量约束

- 移除设计输入器左下角常驻加号；只在当前工作流声明图片输入时，于文本编辑框上方显示与截图一致的图片托盘和末尾添加入口。添加入口可选择本地多张图片或当前项目资产中心多选，本地文件继续先经真实上传接口登记为项目资产。
- 托盘按能力 Schema 的 `assetIndex` 顺序展示，提供删除、左移和右移；排序变化立即写入当前应用草稿，提交后沿用既有有序任务输入快照。点击托盘图片主体进入共享遮罩编辑器，历史轮次图片仍保持主体放大、悬浮遮罩入口的既有交互。
- 固定需要 N 张图片的工作流直接以图片槽位数量为容量、必填槽位为提交下限：少于 N 张持续显示还需数量并阻止提交，超过 N 张提示并只保留选择顺序中的前 N 张。新增纯函数单测覆盖槽位排序、进度、重复/溢出过滤和带空槽左右交换；无需数据库迁移或新增 API。
- 验证通过：目标 Oxlint/Stylelint/Oxfmt、前后端类型检查、Platform API 26 个文件 87 项、Web 10 个文件 41 项单元测试、真实 PostgreSQL/MinIO 集成、模拟 ComfyUI Worker 和生产构建。真实浏览器定向验收确认旧加号为 0、三图工作流 `0/3` 与 `1/3` 进度、资产中心多选、左右移动入口、托盘图片遮罩编辑、不足三张阻止发送和移除后草稿复原；系统 Chrome 全量套件在本轮新增断言之前被既有“设计输入器固定显示在当前视口底部”布局断言阻断。全仓 lint 仍被并行中的报告、LoRA 与首页文件规范问题阻断，本轮涉及文件目标 lint 全部通过。
- 根据后续视觉反馈移除缩略图底部槽位名称；删除按钮收回图片内部右上角，避免横向滚动容器裁切；悬浮或键盘聚焦时在图片中央同一行显示“左移、遮罩编辑、右移”三项操作。真实浏览器读取三按钮横向偏移为 `0/24/48px` 且纵向偏移一致，删除按钮 `54×3px` 起始、尺寸 `21×21px`，完整落在 `78×78px` 图片卡片内。

## 2026-08-27：首页与设计工作台配色收敛

- 按低风险方案只调整视觉变量与卡片层级，不改变首页和设计工作台的布局、路由、权限、真实数据、项目操作或服务状态逻辑。设计工作台的项目总数、资产、累计任务、管理工具和项目文件夹统一使用品牌红；运行任务为零时使用中性灰，大于零时保留独立橙色运行状态，避免四种装饰色同时争夺注意力。
- 项目卡片四项统计取消各自完整描边和底色，改为统一顶部分隔线与内部竖向分隔；“开始设计”由实心高强调按钮降为品牌描边按钮。新建项目仍作为页面唯一实心主操作，项目搜索、排序、网格/列表切换、成员和统计入口保持原功能。
- 首页六个快捷入口移除红、紫、蓝、绿、橙、粉的装饰性色映射，图标和操作按钮统一为品牌红与浅红背景，操作按钮默认改为白底描边；训练服务的可用、异常、未配置和检测中徽标继续使用绿、红、灰等语义色。非图片资产占位预览中的蓝色渐变同步改为单品牌色渐变。
- 浏览器验收脚本新增首页入口颜色唯一性、设计工作台汇总/文件夹颜色唯一性、项目统计透明背景和轻量主操作断言。目标 ESLint、Web 类型检查、Web 10 个文件 41 项单元测试和生产构建通过；真实登录浏览器复核两页，首页六个图标色与操作色各只有一种，设计工作台汇总、管理入口和项目文件夹视觉一致。系统 Chrome 全量浏览器套件已通过本次首页断言，随后在既有模型训练服务状态断言处阻断，设计工作台改动另以真实浏览器定向复核通过。本次无数据库迁移、API 契约或部署配置变化。

## 2026-08-28：平台字体、语义图标与复制交互统一

- 在 `platform.css` 建立页面标题、分区标题、卡片标题、正文、标签、辅助信息和四级图标尺寸令牌；首页、设计工作台、资产、任务、操作日志、用户权限、模型训练与设计输入器统一复用。常规可见辅助信息不再低于 11px，分区标题统一为 16px/700，保留首页主视觉和专业画布内部标注的独立层级，不改变模块结构、路由或业务交互。
- 新增平台业务语义图标目录，统一首页、侧栏、设计工作台、资产选择器、报告、任务、项目、用户权限、工作流与操作日志等跨页面图标；文件类型图标继续由独立资产类型目录维护，避免把“资产中心”与“图片/文档文件类型”混为同一语义。
- 设计输入器所有图片工作流共用的托盘移除“输入图片、已上传、还需”常驻说明，只保留图片缩略图和添加入口；固定 N 张图片的不足、超限和提交阻止提示继续保留，避免视觉冗余削弱真实约束。
- 新增统一剪贴板工具，优先使用 Clipboard API，失败时使用浏览器兼容回退，并严格检查回退结果；只有真实写入成功才显示成功提示。设计轮次输入、Markdown 输出、全局 AI 助手和项目编号复制统一复用该实现，浏览器验收增加写入后读取系统剪贴板并比对真实输入内容的断言。
- Web 类型检查、目标 ESLint/Stylelint、剪贴板、语义图标和图片托盘共 3 个测试文件 7 项单元测试通过；真实浏览器读取首页分区标题 16px/700、辅助信息 11px、入口图标 20px，设计工作台汇总图标 24px、工具图标 20px，资产卡标题 14px/700、类型/标签/元数据 11px。本次无数据库迁移或部署配置变化。

## 2026-09-09：修复开发环境 IP 访问被来源白名单拒绝

- 域名映射联调补充：本机未跟踪的 API `.env` 已显式配置受控 HTTPS 测试域名及 localhost、IPv4/IPv6 回环和当前局域网地址。执行 `./rail-platform.sh restart --keep-infra` 重启 Web/API/Worker，保留基础设施运行。重启后逐一验证允许来源的健康 GET 200、API OPTIONS 204、空参数登录 POST 400（通过来源校验后进入参数校验）；未授权来源均 403。通过外部 HTTPS 入口实测页面和健康接口 200，登录 POST 不再返回 CORS 错误。未使用真实密码完成登录，未验证外部对象存储上传下载。本机环境文件不进入 Git。

- 根因：API 默认只允许 localhost 的 Web 来源，127.0.0.1 和本机局域网 IP 的登录请求经 Vite 代理后仍携带原始 Origin，被安全中间件拒绝。
- 新增开发来源解析器：未显式设置 CORS_ALLOWED_ORIGINS 时，按 APP_PUBLIC_URL 的协议/端口生成回环地址和本机网卡 IPv4 白名单；地址在 API 启动时读取，IP 变化后需重启 API。显式白名单优先，生产不自动发现，原有生产配置校验保持有效。
- 更新开发环境模板与部署说明，解释旧 .env 白名单、Vite allowedHosts 与 API 来源校验的区别，以及对象存储外部访问仍需独立配置。保留工作区已有域名联通配置和文档改动。
- 验证：来源与生产配置 11 项单元测试、API 类型检查、目标 ESLint/Oxlint 均通过。运行服务热更新后，localhost、127.0.0.1、本机局域网 IP 健康请求均 200；API 预检均 204 且回显对应来源，经过 Web 代理的空参数登录 POST 均进入参数校验返回 400；外部域名、错误端口、null 来源预检及 POST 均 403。不使用真实密码，未进行浏览器登录或对象存储上传验收。无数据库迁移。

## 2026-09-08：开发机跨项目架构梳理与双图交付

- 根据平台 API/Worker、ComfyUI 路由、AI Toolkit 训练契约、Presenton 文件生成代码和本机部署快照，新增 `SYSTEM_ARCHITECTURE_OVERVIEW.md` 及 `architecture-overview/` 中的 HTML、SVG、PNG、生成脚本和品牌配色记录。保留工作区原有文档及合同交付物修改。
- 明确平台包含 Web、API 和独立 Worker；报告外部服务归属 Presenton，离线配置与全局助手共享 rail-vllm 的 Qwen3-VL-8B-Instruct 服务；模板报告不依赖外部 AI 服务。区分 ComfyUI 暂存结果确认入库与 LoRA/报告自动资产回流。
- 记录平台 24 图与 Presenton 8 图的报告契约差异、上游同步取消限制，以及模型资产到 ComfyUI 自动发布闭环尚未确认。上述事项未在本次修改业务代码。
- 验证：静态图 self_check 通过；本机 Chrome 渲染两张图并逐图检查文字、连接及边界；核对 Docker 和监听端口。PostgreSQL/MinIO/Mailpit 在线，rail-vllm 已退出，相关业务端口未监听，故未进行真实推理、训练和报告生成验收。无数据库迁移、部署配置修改或服务启停；文档交付未运行业务 lint/typecheck/integration/build 门禁。

## 2026-09-10：首页精简与快捷入口布局调整

- 根据首页两条浏览器批注，移除“最近任务”“最近资产”区域及专属样式、状态文案和首页最近资产预览请求。保留真实指标、六个入口原有业务顺序与行为，以及历史设计弹窗的资产预览。
- 六个入口调整为桌面端两行三列；980px 及以下两列，720px 及以下单列。统一卡片内边距、最小高度和底部操作位置，页脚信息允许换行。
- 同步 README、产品需求、质量门禁与已有浏览器验收脚本，断言桌面三列两行且最近工作区域不存在。后端 Dashboard 契约未修改，无数据库迁移、部署或外部依赖配置变更。
- 已验证：Web 类型检查通过；Web 单元测试 10 个文件、41 项通过；目标 ESLint、Stylelint、Oxfmt 检查与 `git diff --check` 通过；Web 生产构建通过。最初样式检查发现合并预览样式后的重复选择器及属性顺序问题，修正后复查通过。
- 真实浏览器验证：1370px 三列两行、820px 两列、390px 单列；页面与六张卡片均无横向溢出，最近任务/资产已移除；新建设计表单、历史设计列表及图片预览正常，临时视口已恢复。刷新时登录状态失效并发生两条路由/登录控制台错误，重新登录后首页和弹窗检查期间无新增控制台错误；未对登录问题扩大修改。
- 验证范围：仅首页 UI 和既有入口冒烟，未创建项目或任务；未运行完整仓库 lint、API/数据库集成测试或覆盖外部生成服务的完整浏览器脚本。浏览器脚本已更新，本轮用现有本地会话完成定向交互验证。

## 2026-09-10：项目选择入口回归页面内部

- 设计生成、资产中心、模型训练、报告生成的顶栏不再重复显示当前项目；设计生成保留任务栏项目选择，其他未涉及页面沿用原顶栏规则。
- 资产中心在筛选区新增可按名称/编号搜索的项目选择，复用当前账号可访问项目及现有项目切换 API。切换关闭详情和操作弹窗、返回根目录、重置成员与勾选状态；异步成员返回使用失效标记避免覆盖新项目，并在项目加载后重新应用资产筛选。筛选区使用桌面五列、中等宽度三列、手机单列网格。
- 已通过 Web 类型检查、41 项单元测试、目标 ESLint/Stylelint 和 Web 构建；最终布局样式再经 Stylelint 检查。浏览器确认四页顶栏均无项目选择，设计页任务栏选择保留；资产从原项目文件夹切换到另一个项目后仅展示目标项目两项资产并回到根目录，再恢复原项目；1370px 和390px 筛选布局无横向溢出，最后检查控制台无错误。
- 同步项目说明、需求、质量门禁及资产浏览器断言。无后端、数据库迁移或部署变更；未运行全仓规范检查、数据库集成或完整浏览器验收脚本。
- 待确认：用户提出训练/报告“不需要对应具体项目”。已询问是本轮仅移除顶栏显示，还是将素材、任务与产物改为用户独立归属；尚未收到回复。本轮没有把隐藏项目选择描述为业务脱离项目，训练/报告后端仍要求 projectId 并校验项目权限。

## 2026-09-10：资产中心默认汇总全部项目

- 项目筛选默认值改为“全部项目”，并保留单项目筛选。使用真实项目列表逐项目调用受权限校验的资产 API，汇总包含子文件夹的已登记资产；聚合数据保存在页面缓存，不混入 Platform Store 的当前项目资产缓存。返回全部项目视图不修改已保存的当前项目偏好。
- 汇总卡片显示所属项目，支持跨项目关键词、类型、创建成员筛选及全局排序。单项目模式继续加载对应目录与成员；切换清空目录、勾选及详情状态，异步请求使用代次检查避免旧汇总覆盖新筛选。
- 收藏、重命名、单项软删除使用资产 ID 的原有受控接口，全部项目批量软删除按项目分组调用既有 API，部分失败时刷新真实剩余数据。登记、建文件夹、移动/复制先筛选具体项目，页面有明确说明，避免静默写入原来的当前项目。未新增后端接口、数据库迁移或部署配置。
- 同步首页资产入口说明、README、产品需求、质量门禁及浏览器默认筛选断言。
- 验证：Web 类型检查通过；41 项 Web 单元测试通过；目标 ESLint/Stylelint 与格式检查通过，初次样式顺序问题已修正；Web 生产构建通过；`pnpm test:rail:integration` 的真实认证/权限隔离/项目/资产/对象存储/审计及 Worker 端到端验收通过。
- 浏览器实测默认179项资产，覆盖5个有资产的可访问项目；选择“测试项目00005”显示2项，切回全部项目恢复179项；390px无横向溢出，视口恢复，控制台无错误。未对用户现有资产执行删除、重命名或收藏写操作，未运行覆盖所有业务的完整浏览器脚本及全仓lint。
- 当前沿用逐项目列表 API，适合现有数据规模；跨项目服务端分页尚未实现，不应把本轮汇总描述为已具备大数据量分页能力。

## 2026-09-10：统一登录页与首页品牌标题字号

- 移除登录后外壳品牌标题单独覆盖的15px字号、700字重和32px行高，改用登录页相同的 `text-xl leading-7 font-medium`。首页及共用顶栏页面随之统一；Logo图标与登录页实现未改动。
- 更新既有品牌浏览器断言：检查1.25rem字号、500字重、垂直居中与完整显示，移除要求文字框与图标等高的旧断言。
- Web类型检查、目标ESLint/Stylelint及Web构建通过。首页浏览器实测当前根字号15px下标题18.75px、字重500、行高26.25px，Logo与标题中心偏差0且无裁切。控制台仅有本轮调整前的登录错误记录；未执行退出/登录或完整浏览器脚本。无数据库迁移和部署变更。

## 2026-09-10：补齐品牌图标尺寸与对齐一致性

- 用户指出首页图标仍比登录页大。根因为上轮只统一标题字体，VbenLogo仍使用默认32px图标；登录页为1.75rem。登录后布局改为按主题根字号的1.75倍传入图标尺寸，与登录页等比例缩放；标题使用inline-flex垂直居中。
- 浏览器实测首页图标26.25×26.25px、文字行框26.25px，中心偏差0，标题字号18.75px且没有裁切。同步既有浏览器断言，检查图标大小与文字行框一致。目标ESLint/Stylelint、Web类型检查及Web构建通过；本轮不重复数据库或完整浏览器集成测试，无迁移及部署变更。

## 2026-09-10：三维下载四格式与系统全屏入口调整

- 需求：按用户截图同时显示 GLB、OBJ、STL、FBX 下载格式，移除系统工具栏全屏功能。
- 实现：共享 `model3d-viewer.vue` 增加四格式下载按钮、导出中禁用与明确错误提示，保留原文件下载；`model3d-export.ts` 按需加载 Three.js GLB/OBJ/STL 导出器；`model3d-fbx-export.ts` 输出真实 FBX 7.4 ASCII 静态网格，保留几何、法线、UV 与世界变换，并处理镜像绕序。源模型没有网格或包含不支持的骨骼、实例、活动形变时拒绝 FBX 转换。
- 系统全屏：`preferences.ts` 关闭功能与按钮位置，`main.ts` 启动时覆盖旧浏览器偏好。三维和图片查看器的独立全屏继续保留。
- 验证：`pnpm test:rail` 通过 API 104 项、Web 54 项；`pnpm typecheck:rail` 与 `pnpm build:rail` 通过。新增 FBX 往返读取测试验证多网格、UV、变换、镜像朝向、源几何不变及无网格失败。
- 浏览器：通过 `scripts/browser-model-export-acceptance.mjs` 实际下载四种格式并核验文件结构；390px 按钮边界、控制台错误检查、系统全屏配置及旧偏好刷新覆盖通过。验收使用临时模型和页面，不创建或清理业务数据。
- 规范：本次文件执行 Oxfmt、ESLint、Oxlint 和查看器 Stylelint；全量 `pnpm lint` 因工作区原有 23 个文件的格式问题阻断，包含提示词维护页面和既有文档/图表，本次未批量格式化它们。
- 边界：无 API、数据库或对象存储契约变更，无新增迁移、环境变量或生产服务依赖，因此未运行真实数据库集成验收。FBX 为 ASCII 静态网格，不承诺二进制 FBX、动画、骨骼或完整材质转换，目标桌面软件兼容性未验证。代码尚未提交；所有原有未提交修改保留。

## 2026-09-10：提示词扩展、维护与默认恢复

- 实现：模板应用替换当前主提示词，重复应用不叠加；服务端按语义位置组织指令、展开组合并去重，校验单选、依赖互斥、适用工作流、尺寸与长度。分类顺序用于界面展示，语义位置用于组句；无效配置返回400，过期版本保存返回409。
- 管理：独立维护弹窗支持按钮名称/完整扩展内容分离、作用对象、生成/编辑表达、启用、工作流范围、组合/依赖/互斥、模拟预览、中文搜索。恢复单项、分类或系统目录可保留自定义，完整重置显式列出删除项；恢复先预览差异、再进入编辑器，保存才持久化，另有撤销和历史恢复。
- 迁移：新增033，保留现有目录和初始历史快照，增加revision、默认基线及历史版本表。默认基线2026-09-10.1包含室内夜间照明、窗外夜景及夜间客室组合示例；管理员自行选择恢复，不自动覆盖当前配置。本地迁移已执行；其他环境部署先运行 `pnpm db:rail:migrate`，前后端配套发布，无新增环境变量。
- 验证：`pnpm test:rail`通过API105项、Web54项；`pnpm typecheck:rail`与`pnpm build:rail`通过；`pnpm test:rail:integration`通过平台真实数据库/MinIO验收和ComfyUI Worker测试适配器端到端验收。新增非法配置400、版本冲突409、普通用户草稿预览403和历史恢复验证。
- 浏览器：实际选择地铁/高铁模板，连续应用文本保持单份，换选项后覆盖；恢复差异、中文搜索和夜间组合生成/编辑两种预览通过；未保存模拟取消。390px维护弹窗无横向溢出、浮层不再遮挡，控制台无错误。未触发真实GPU生图。
- 规范：本次功能文件Oxfmt、ESLint、Oxlint和Stylelint通过；全量lint仍有既有图表/交付文档与UI提案的18个格式问题，以及工作区其他功能的Oxlint问题，不批量修改无关文件。架构、产品要求及质量门禁同步更新。
- 边界：本期是确定性模板扩展，不是LLM自动润色；复杂业务语义冲突依赖管理员维护互斥关系，不能自动推理任意自然语言矛盾。当前目录的旧短句保留，可由管理员逐步补全。未提交或推送代码，保留工作区已有修改。

## 2026-09-10：资产中心 C 版开发与真实验收

- 需求：将用户确认的 C 版落实到现有资产中心，减少常驻信息，按 CMF、零部件、客室、报告组织资产，支持层级文件夹、模糊/精准搜索、任务等条件筛选与正倒序。
- 前端：资产中心增加内部模块栏、目录面包屑及文件夹卡片、搜索匹配切换、筛选抽屉、排序、网格/列表及分页；保留登记、收藏、多选、移动/复制、预览与详情。项目汇总仅请求可访问项目，筛选结果使用页面独立状态；修正详情 URL 变化导致页面重建、目录上下文丢失。工作区资产选择及设计结果归档目录显示分类前缀。
- 后端：新增 `034_asset_generation_categories.sql`，为资产与目录增加独立业务分类；子目录继承分类，同名根目录按模块隔离。资产按目标分类目录、显式分类或来源任务模式归类；旧报告来源可回填，其余不确定资产进入未分类。文件类型仍为既有八类。查询增加参数校验、模糊/精准/任务/类型/时间/收藏筛选和白名单排序，保留项目权限校验。
- 验证通过：`pnpm db:rail:migrate`（本机已应用034）、`pnpm typecheck:rail`、`pnpm test:rail`（API107项、Web59项）、`pnpm test:rail:integration`（真实数据库/对象存储/权限隔离及Worker协议验收）、`pnpm build:rail`。
- 专项验收：`RAIL_BROWSER_EXECUTABLE=/usr/bin/google-chrome pnpm exec tsx scripts/asset-browser-acceptance.ts` 通过，涵盖分类继承、同名目录、跨项目/只读访问限制、真实MinIO上传、任务来源分类、字面通配符/精准搜索、移动复制及收藏隔离；浏览器覆盖登录、目录持久化、登记文本、跨模块移动、搜索筛选、排序、列表/网格、详情上下文及390px无横向溢出，控制台无错误。临时测试账号、项目和对象按本轮ID清理。
- 规范检查：本次资产中心核心文件的 Oxfmt、ESLint、Oxlint、Stylelint 通过；全仓 `pnpm lint` 仍被其他既有图表/交付及工作台提案格式问题阻断。共同修改的 `design/index.vue` 另有其他功能的两项 Oxlint 问题，本次仅增加目录分类标签，不覆盖其他任务的代码。
- 交付证据：`docs/rail-platform/ui-proposals/20260910-assets/verification/` 保存真实桌面、筛选抽屉、手机截图和验收结果；架构、产品需求、质量门禁及README同步更新。截图为临时验收资产，不作为运行时业务数据。
- 部署与边界：其他环境须先运行数据库迁移，再发布API/Worker/Web；无新增环境变量或外部模型配置。当前为接口筛选、前端分页，服务端分页尚未实现；完整回滚需按架构文档处理新增分类同名目录，不能直接恢复旧唯一索引。未提交或推送代码，保留其他工作区改动。

## 2026-09-10：资产中心图片预览操作工具栏

- 需求：资产中心图片放大后，右下角提供与设计结果相同的八个图标操作：重新绘制、局部重绘、标记修改、部件/材质融合、图像放大、环境更改、图像理解、多角度生成，悬浮显示名称。
- 实现：`asset-image-actions.ts`复用设计结果操作目录和工作流映射；资产页通过共享ImageLightbox的actions插槽展示工具栏，防止重复点击创建会话。未接入工作流和无来源任务的重绘禁用并解释原因，不重复提供“添加资产”。
- 交接：资产页创建图片所属项目的新设计会话，设计页经真实资产API重新解析项目并验证会话归属，复用既有遮罩、融合、倍率、多角度弹窗和图像编辑输入器。原会话草稿不被覆盖；来源参数复用只填入编辑框，不自动运行。路由保留图片操作上下文，刷新可回到同一编辑会话，不重复创建会话。
- 验证：`pnpm test:rail`通过API107项、Web59项；`pnpm typecheck:rail`与`pnpm build:rail`通过。相关代码ESLint、Oxlint、Stylelint与Oxfmt检查通过。
- 浏览器与真实持久化：`RAIL_BROWSER_EXECUTABLE=/usr/bin/google-chrome pnpm exec tsx --env-file=apps/platform-api/.env scripts/asset-image-actions-acceptance.ts`通过八个操作、禁用提示、390px工具栏、从其他当前项目进入图片项目、输入资产与参数草稿持久化及零页面/控制台异常。重绘使用明确的测试任务夹具；只测试编辑入口，不触发真实GPU。临时账号、项目、会话及对象均按本轮标识清理。截图与结果见`ui-proposals/20260910-asset-image-actions/`。
- 边界：无后端接口、数据库迁移或环境变量变更，复用现有项目权限与API；没有生成记录的上传图片不能执行“重新绘制”，其他已配置图片处理能力正常可用。全量lint的既有工作区问题未在此任务批量修复。代码未提交或推送，保留全部原有修改。

## 2026-09-10：修复大图保存目录弹窗被遮挡

- 根因：图片查看器使用2200层级，保存目录弹窗使用默认层级；结果操作对save保留大图，造成目录弹窗显示在预览后面。
- 修复：`workflow-run-card.vue`统一保存入口，先关闭图片预览再发出save事件；业务模式工具栏与旧版通用结果工具栏共用此处理，下载仍留在预览中。目录选择与保存API不变。
- 验证：`scripts/image-output-save-acceptance.ts`使用临时账号、项目和两张暂存输出，浏览器确认选中第二张后保存弹窗无遮挡、保存按钮实际可点击、取消不入库、指定目录只保存正确图片；旧版无designMode任务根目录保存也通过，页面与控制台无错误。记录位于`ui-proposals/20260910-image-output-save/`，测试结束清理本轮数据及对象，不执行生图。
- Web类型检查、生产构建、目标ESLint/Oxlint通过。此次为前端弹窗流程修正，无迁移、后端权限或部署配置变化；未重复全量数据库集成和全量lint，现有工作区修改保留，未提交或推送。

## 2026-09-10：按「01 均衡总览」实施设计工作台

- 用户确认五类内容及 01 布局后，将 `/projects` 切换为真实工作台：上层我的设计 + 项目资产，中层最近任务，下层最近设计成果 + 最近保存资产；管理与记录移至页尾。没有引入提案示例数据，也没有将设计会话与单次任务混为同一列表。
- 新增 `apps/platform-api/api/v1/workbench/index.get.ts` 和 `utils/domain/workbench/query.ts`：五区独立聚合、稳定分页、活动任务筛选，先校验可访问项目集合，再对个人会话/执行/成果限定当前用户。上传使用版本完成时间，生成成果使用生成时间，保存结果使用真实登记时间；只读项目返回不可写标记，所有实际写操作继续经过既有权限校验和审计。
- 新增 Web `api/platform/workbench.ts`、`views/platform/workbench/index.vue`、`section-items.vue` 和 `asset-thumbnail.vue`。落地继续/新建设计、项目入口、任务进度/错误/成果、取消任务、真实缩略图与多类型预览、保存到目录、下载及图片深化。无有效原会话的深化会先创建当前用户会话；暂存成果复用前必须确认保存，未配置能力明确提示。五区支持各自加载、空态、失败重试和分页，页面可见时每 15 秒刷新，离开页面停止轮询。
- 调整 `router/routes/modules/platform.ts`、`views/platform/overview/index.vue`、`layouts/basic.vue`：旧项目管理复用于 `/projects/manage`，保留创建、编辑、置顶、成员、删除等操作及固定平台外壳。`views/platform/assets/index.vue` 增加目标项目/目录/模块深链，支持跨项目打开和刷新恢复；保留这些文件中的其他既有修改。
- 新增 `scripts/workbench-acceptance.ts`，使用随机测试账号、独立项目、真实 MinIO 文件及明确的数据库执行夹具；不提交外部生成任务，只清理本轮数据。API 验收覆盖管理员个人范围、项目隔离、只读拒绝、分页、最新轮次、活动任务、暂存状态和时间排序。浏览器验证五区布局、分页、取消、预览、目录保存与跳转、深化画布、下载内容、新建/继续设计、项目管理、单区失败重试和 1024/736/390px 响应式。
- 验收中确认 MinIO 开发 CORS 只允许 `http://localhost:5666`；脚本最初使用 `127.0.0.1` 导致遮罩画布跨域读取失败，已将专项验收默认地址改为项目标准 localhost 地址，验证画布实际加载成功且没有页面/控制台错误。没有更改开发环境 CORS 配置。
- 验证结果：`pnpm typecheck:rail` 通过；`pnpm test:rail` 通过（API 107 + Web 59，共 166 项）；`pnpm test:rail:integration` 在加载 API 本地环境后通过，包括 ComfyUI Worker 测试适配器至 MinIO 的完整链路；`pnpm build:rail` 通过。本次新增运行代码/验收脚本的 ESLint、类型感知 Oxlint、Stylelint 及格式检查通过，`git diff --check` 通过。
- 全仓库 `pnpm lint` 未通过：最终检查报告 27 个既有文件格式问题，主要是历史报告/架构图、此前效果图以及其他工作中的验收脚本；未批量格式化无关文件。不得将本条记录理解为全仓库 lint 全绿。
- 交付截图和专项结果见 `docs/rail-platform/ui-proposals/20260910-workbench-confirmed/verification/`；同步更新需求、架构、质量门禁及提案 README。本次无数据库迁移、无新环境变量，部署需同时更新 Web/API；既有资产分类迁移 034 和对象存储访问配置须已就绪。外部生成服务本身未在本次验收中运行。

## 2026-09-10：AI 助手极简界面、独立会话与可拖动浮层

- 按用户确认的圆润双眼造型开发矢量图标，主色取自平台Logo源文件 `#BB1B21`，用于标题、消息头像和悬浮入口。去掉项目栏、模型常驻说明、条纹、欢迎文案与计数脚注；历史、新建、关闭集中于顶部，清空移入历史面板，保留附件、消息状态及错误提示。
- 新建会话前后端均取消项目关联，旧客户端项目字段忽略；历史会话和附件保留，用户隔离、消息上下文和私有对象存储继续使用真实平台链路。无数据库迁移或新增环境变量。
- 补充用户交互规划后实现：路由path变化自动收起，查询参数变化不触发；会话、草稿、待发附件及发送请求保留。入口与标题区支持Pointer Events拖动，5px阈值防误触；按钮、输入区不拖动，多指不接管，发送时禁止新建/切换会话。默认右下角，位置仅本次页面生命周期记忆；四周至少10px，面板与入口分别限制边界，窗口及VisualViewport变化校正尺寸/坐标，支持横竖屏。
- 改动文件：`components/assistant/ai-assistant.vue`、新增 `assistant-logo.svg` 和 `floating-position.ts`、前端 `api/platform/assistant.ts`、后端 `assistant/conversations/index.post.ts`；新增 `store/assistant-position.test.ts` 和 `scripts/assistant-browser-acceptance.ts`。相关产品需求、架构和质量门禁同步更新。
- 验证：`pnpm typecheck:rail`、`pnpm test:rail`（API107项、Web61项）、`pnpm test:rail:integration`（真实数据库、对象存储、AI会话权限及Worker协议）和 `pnpm build:rail` 通过。本次目标ESLint、Oxlint、Stylelint通过；全仓 `pnpm lint` 仍受其他工作区文件的Oxlint问题阻断，未批量改写其他任务代码。
- 专项真实浏览器与附件验收由 `pnpm exec tsx scripts/assistant-browser-acceptance.ts` 执行，结果和桌面/手机/入口截图保存在 `ui-proposals/20260910-ai-assistant/verification/`；验证路由收起、草稿保留、拖动边界和防误开、历史搜索、真实消息与附件持久化及跨用户拒绝。临时数据按本轮用户和对象ID清理，不修改用户现有会话。
- 限制：本轮外部AI服务返回失败，验证的是错误如实显示与消息持久化，不能声称模型回复成功。手机为浏览器视口模拟，未进行真机软键盘验收；VisualViewport监听已实现。旧会话数据库项目字段不回写清空，但不再参与前端上下文绑定。未提交或推送代码，保留其他未提交改动。

## 2026-09-10：修复助手头像、消息外框、历史排版及Logo拖动

- 用户反馈四项显示/交互问题。用户头像原有布局缺少居中，改为中性圆形底、居中人形图标；双方消息移除边框、阴影和底色，失败消息保留文字状态提示。
- 历史列表根因为 `.rail-ai-actions button` 作用于嵌套历史面板的所有按钮，将标题、排序和新建按钮限制为32px。改为仅选中顶部直接子按钮；历史标题占满可用宽度、单行省略，日期独立一行，列表采用可滚动纵向排列。
- Logo拖动根因为图片可能触发浏览器原生拖拽，打断Pointer Events。所有助手Logo禁用原生draggable，拖动起点阻止默认行为；保留左键按住移动、5px防误触、边界校正和拖动松手不打开。
- 改动 `components/assistant/ai-assistant.vue`，扩展 `scripts/assistant-browser-acceptance.ts`：使用至少8条真实临时会话检查标题宽度/日期位置，检查消息边框阴影、头像中心，并直接按住Logo图片验证移动距离。真实API、对象存储与浏览器专项验收通过；目标ESLint、Oxlint、Stylelint、`pnpm typecheck:rail`、`pnpm build:rail`及差异空白检查通过。此次未重复全仓单元/集成门禁，专项已包含真实会话/附件链路。
- 新截图 `ui-proposals/20260910-ai-assistant/verification/assistant-history.png` 与 `assistant-messages.png` 已人工查看。外部服务仍返回 `AI_ASSISTANT_UNAVAILABLE`，失败路径通过，不声明成功回复。无迁移、无新增配置、未提交或推送，保留其他工作区改动。

## 2026-09-10：历史列表居中、逐条删除和发送箭头居中

- 历史菜单改为相对助手面板定位，两侧16px等距，取消原来相对历史按钮的固定320px窄宽；短视口限制最大高度。每行增加独立删除按钮，保留重命名；发送与附件按钮显式使用网格居中并清零内边距，避免图标偏移。
- 新增 `api/v1/assistant/conversations/[id].delete.ts` 和前端删除API，校验用户归属，事务锁定并删除会话及级联消息/附件，事务后清理对象并写入审计。用户确认后执行，取消不删除；删除当前会话复位、删除其他会话保留当前内容。
- 修改 `components/assistant/ai-assistant.vue`、`api/platform/assistant.ts` 和专项验收脚本；新增中心位置像素断言、逐条删除取消/确认、其他会话保留、越权404及真实MinIO对象删除断言。截图仍在 `ui-proposals/20260910-ai-assistant/verification/`。
- `pnpm typecheck:rail`、`pnpm test:rail:integration`和`pnpm build:rail`通过。无数据库迁移、无新增配置。外部AI服务仍不可用，本轮不宣称模型回复成功。未提交或推送代码。
- 最终专项验收通过：菜单中心偏差小于1px、宽度与面板相差小于36px，发送图标双向中心偏差小于1px；删除取消与确认、当前会话删除复位、其他会话保留、越权404及MinIO对象404验证通过。目标ESLint、Oxlint、Stylelint和格式检查通过。验收脚本等待删除接口完成后读取结果，避免将菜单提前关闭误判为删除已完成。

## 2026-09-10：助手空白欢迎语及Logo尺寸统一

- 新建/空白对话在内容区中央仅显示“有什么我能帮你的吗？”，已有消息时隐藏。
- 标题和悬浮入口的Logo视觉尺寸统一为平台标志使用的1.75rem（当前根字号下26.25px）；悬浮入口保留56px透明点击/拖动区域，阴影跟随缩小后的图形，不改变既有位置和边界算法。
- 修改 `components/assistant/ai-assistant.vue` 与专项浏览器脚本，新增欢迎语及标题/入口Logo尺寸断言。真实浏览器专项验收、类型检查、目标ESLint/Oxlint/Stylelint及格式检查通过；人工查看桌面截图。此次为局部文案和尺寸调整，不重复生产构建或全仓门禁，无API/迁移变更，未提交或推送。外部AI服务仍不可用，未将失败回复算作模型成功。

## 2026-09-10：助手Logo适度放大及完整日期

- 按用户最新反馈，标题与悬浮入口Logo由约26px调整为32px，继续保留原点击/拖动区域。
- 消息与历史会话时间复用同一个格式化函数，按浏览器本地时间显示完整年/月/日和24小时制时分；例如 `2026/09/10 20:10`，不更改服务端时间数据。
- 修改助手组件及专项浏览器脚本，补充32px尺寸、历史日期和消息完整日期断言。本次为显示调整，无API、数据库或配置变更，未提交或推送。
- 验证：类型检查、目标ESLint/Oxlint/Stylelint与格式检查通过；浏览器完整日期和尺寸断言通过，专项交互回归最终通过。首轮草稿保留断言失败，未改运行时代码重跑通过；未将首轮记录描述为成功。人工查看完整日期截图。局部显示调整未重复生产构建，全仓门禁不作本轮通过声明。

## 2026-09-10：助手输入框紧凑调整

- 按用户圈选的输入区调整：基础高度60px降至52px，输入文字上下内边距18px降至14px，附件/发送按钮36px降至32px，图标字号18px降至16px并保持居中。
- 仅修改助手组件CSS，不改变Logo或发送逻辑，无API/数据库变更。格式与目标Stylelint检查通过；局部样式调整不重复类型检查、单元测试及生产构建。
- 已查看真实页面截图，输入区更紧凑且按钮居中。专项脚本完成尺寸/对齐相关前置检查，但后续既有切页收起用例在等待入口重新显示时超时，因此不声明本轮完整浏览器回归通过；本轮未修改路由或浮层状态逻辑。

## 2026-09-10：设计页任务栏适度收窄

- 按用户圈选，将设计页桌面任务/会话侧栏宽度从276px调整为248px（收窄28px），继续使用同一宽度变量驱动布局和折叠按钮定位。900px以下的224px响应式规则不变。
- 仅修改 `views/platform/design/index.vue` 的CSS变量。当前用户浏览器实测侧栏宽248px，clientWidth与scrollWidth均247px，无横向溢出；标题省略、项目选择、新建按钮和历史列表显示正常。格式与目标Stylelint检查通过；局部尺寸调整未重复全量测试及构建，无数据库/配置变更，未提交或推送。

## 2026-09-10：统一底色夜览主题 V2 落地

- 按已确认效果图统一平台外壳、资产分类栏、内容区和资产卡片为 `#191c20`，取消资产浏览容器嵌套边框与卡片底板，保留留白、文字层次、暗红选中态及键盘焦点提示。图片不反色、不加滤镜。
- 新增 `apps/web-antd/src/styles/platform-dark.css`，在 `bootstrap.ts` 引入；`app.vue` 按夜览状态覆盖 Ant Design 配色。平台页面与公共预览、AI 助手组件的固定中性色改为带浅色回退值的主题变量，覆盖工作台、设计、资产、训练、报告、任务及管理页面。浅色模式保留原有配色。
- 验证：`pnpm typecheck:rail`、`pnpm test:rail`（API 108 + Web 64，共 172 项）、`pnpm build:rail` 通过；专项真实浏览器脚本通过三层严格同色、图片样式、抽屉、AI 助手、刷新持久化、七个主要页面大面积白底检查、390px 响应式及切回浅色，页面与控制台无错误。
- 全量 `pnpm lint` 仍受既有文档效果图及其他任务文件的格式问题阻断；本次主题文件与验收脚本单独检查。证据见 `ui-proposals/20260910-dark-theme/verification/`。
- 本次仅调整主题展示，无 API 契约、数据库迁移或环境变量变化；未调用外部生成服务，浏览器验收仅清理本轮临时账号、项目与文件。未为纯样式改动重复运行完整 API 集成套件。

## 2026-09-10：精简外壳工具并固定樱花粉主题

- 根据浏览器标注隐藏全局偏好设置入口、标签栏右侧的更多、刷新和最大化按钮；保留顶部日间/夜览切换。
- 修改 `apps/web-antd/src/preferences.ts` 的默认配置，并在 `apps/web-antd/src/main.ts` 初始化后固定相同选项，覆盖历史缓存，不要求用户清空本地数据。默认内置主题为 `pink`（樱花粉），主色为 `hsl(347 77% 60%)`，保留用户当前明暗模式。
- 验证：上述两个文件 ESLint、Oxfmt 通过，`pnpm typecheck:rail`、`pnpm build:rail` 通过；在现有设计会话的真实浏览器中刷新，确认设置及标签栏三个按钮均消失、夜览及会话内容正常保留。
- 此次为两处应用配置调整，无数据库迁移、API 或外部服务变化，不新增重复配置断言的单元测试；未重复执行完整集成验收及已有格式问题阻断的全仓 lint。

## 2026-09-10：移除顶部搜索快捷键提示

- 按用户标注，从共享 `widgets/global-search/global-search.vue` 的搜索触发入口移除Command/Ctrl+K提示及其占位。搜索按钮和原键盘快捷键逻辑保持可用。
- 格式和目标ESLint检查通过；当前浏览器顶部实测仅显示平台标题与“搜索”，无kbd提示。局部模板删除未重复全量测试或构建，无API/数据库变更，未提交或推送。

## 2026-09-10：工作台等宽布局、项目弹窗与图片操作统一

- “我的项目资产”改名“我的项目”，与“我的设计”等宽；最近保存资产改为与最近设计成果等宽的缩略图卡片。删除工作台文字尾部箭头及成果卡片“深化”入口。
- 工作台项目入口打开“我的项目”弹窗，复用现有真实项目管理组件，保留搜索、排序、新建、编辑和成员等操作。旧 `/projects/manage` 重定向至工作台弹窗，兼容 `create=true`，关闭时清理弹窗查询参数。
- 成果和保存资产图片统一复用 ImageLightbox，与资产中心共享八个图片操作配置和能力检查；右下角图标打开既有设计编辑流程。未保存成果先确认入库，写权限不足或能力不可用时禁用入口并给出原因。非图片格式继续使用原预览。
- 修改工作台 index.vue、section-items.vue、overview/index.vue 与平台路由，更新专项验收脚本和产品需求。无需新增数据库迁移或环境配置。
- 验证：`pnpm typecheck:rail`、`pnpm test:rail`（API 108、Web 65项）和 `pnpm build:rail` 通过；目标 ESLint、Stylelint通过。浏览器实测左右面板各541.5px，项目列表与新建表单弹窗、旧路径兼容及关闭后回到 `/projects` 正常；大图加载、缩放及右下角局部重绘进入真实遮罩编辑器正常，控制台无error。未提交生成任务或修改现有项目数据。
- 本轮为前端交互调整，未重复运行数据库集成套件；更新的完整workbench浏览器脚本未执行，本轮采用当前真实浏览器验证。全仓lint存在其他任务遗留格式问题，不声明通过。

## 2026-09-10：工作台面板对齐与项目入口精简

- 保持桌面左右各占一半的网格，将同一行面板由顶部独立高度改为自动拉伸等高，上下边缘对齐。设计卡片按容器宽度自动排列，单卡最小300px，窄容器单列；900px以下外层仍纵向排列。
- 移除“我的项目”面板底部“新建项目”“管理项目”及分隔线，项目管理统一通过“查看全部”弹窗进入，弹窗内新建等功能保留。
- 修改 workbench/index.vue、section-items.vue；目标 ESLint、Stylelint通过，格式已整理。本轮仅布局与重复入口精简，无API/数据库变化，未重复单元测试、构建或集成套件。
- 真实浏览器验证：上层左右面板均541.5×391.36px，下层左右面板均541.5×356.67px，项目页脚已移除，控制台无error。

## 2026-09-10：设计新会话引导精简

- 按圈选简化设计页空白会话：仅保留“今天想设计什么？”和“在下方输入想法，开始设计”，移除装饰图标、英文标识、长说明和三个示例按钮，同时清理对应计算属性、点击函数及样式。
- 使用紧凑居中的两行文字与自适应留白，底部输入区、业务模式及项目/会话逻辑保持原行为。修改 views/platform/design/index.vue，无API、数据迁移或配置变化。
- Web类型检查、目标ESLint与Stylelint通过，格式已整理；真实浏览器截图确认仅显示两行引导、示例按钮为0。局部展示精简未重复单元测试、生产构建及数据库集成验收。
